Phase 18.5: Claude and Codex Integration Examples
=================================================

[DONE] P18.5-T001 Add Claude-style artifact consumption example
[DONE] P18.5-T002 Add Codex-style coding-agent handoff example
[DONE] P18.5-T003 Document when to use read-only vs file-authoritative adapter modes

Why this exists
---------------
- Adapter capabilities are easier to understand through concrete host-flavored examples than through interfaces alone.
- Claude/Codex-style hosts are the most obvious consumers of session-memory and playbook artifacts.

Write tests first
-----------------
- examples remain aligned with the supported adapter contract
- docs distinguish read-only compiled artifact usage from writable adapter flows
- unsupported host expectations are called out explicitly

Implementation steps
--------------------
1. Add narrow, realistic read-only or file-assisted examples before any writable host example
2. Reuse compiled artifact surfaces from earlier phases
3. Keep example claims aligned with what the adapter actually supports

Acceptance
----------
- Host integration story is concrete and non-ambiguous
- Examples do not overstate adapter capabilities


Implementation Notes
--------------------
- added runnable Claude-style and Codex-style host examples on the `goodmemory/host` read path
- README now documents `file-assisted` versus `file-authoritative` mode boundaries explicitly
- current examples intentionally stay read-only and do not imply writable adapter support
