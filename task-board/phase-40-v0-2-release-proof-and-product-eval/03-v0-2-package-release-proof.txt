Phase 40.3: v0.2 Package and Release Proof
==========================================

Status
------
[DONE] v0.2 package metadata, release docs, CI matrix, and package smokes are
implemented.

Goal
----
Prove the package can be released as `0.2.0` with stable public boundaries.

Scope
-----
- Bump package version to `0.2.0`.
- Run pack dry run and inspect packaged files.
- Install the tarball into external consumer projects.
- Prove `goodmemory`, `goodmemory/ai-sdk`, and `goodmemory/host` resolve without
  repo-internal imports.
- Add Node 20 / 22 / 24 package-boundary smokes.
- Confirm release workflow uses the latest accepted gate.

Acceptance
----------
- Package-boundary smoke passes on Node 20, Node 22, and Node 24.
- External tarball consumers do not import `src/` or private paths.
- README, docs, examples, package metadata, and release checklist agree on
  `0.2.0`.
- Release proof includes `bun run test:ci`, the latest accepted quality gate,
  and pack/install evidence.

Accepted Evidence
-----------------
- Package version: `0.2.0`
- Public install docs updated to `goodmemory@0.2.0` and
  `goodmemory-0.2.0.tgz`.
- CI package-boundary matrix covers Node 20, Node 22, and Node 24.
- Regression:
  `bun test tests/release/release.test.ts --test-name-pattern "package metadata exposes|v0.2 package metadata|ci workflow runs the node package boundary matrix|readme links the canonical docs|readme ships a Simplified Chinese product entrypoint|phase-27 canonical guides"`
- Pack and external consumer proof:
  `bun test tests/release/release.test.ts --test-name-pattern "package export targets|packs a tarball|package-boundary reference consumer smoke uses package-name imports only"`
- Node package-boundary proof:
  `bun test tests/release/node-package-boundary.test.ts`

Notes
-----
- The local Node package-boundary proof is a current-runtime smoke. Node 20,
  Node 22, and Node 24 enforcement is carried by the CI matrix.
- The release workflow still runs Phase 39 as the latest accepted release gate
  until P40-T006 creates and accepts the Phase 40 gate.
