Phase 67: Public Benchmark Performance and Claim Promotion
=========================================================

Status
------

[ACTIVE] Phase 67 turns GoodMemory's internal benchmark evidence into honest,
reproducible, public-claim-ready scores. The discipline: no internal number
reaches a public README row until it passes the public benchmark claim gate
(P67-A). This phase opens no LoCoMo retrieval work (owned by board 70) and does
not own the Phase 63 BEAM answer-pack hardening lane beyond public-claim
boundary wording; P67-D is now the BEAM public-claim re-anchor, not a request to
run more BEAM answer-generation work.


Goal
----

Turn internal benchmark evidence into honest, reproducible, public-claim-ready
scores. Every claimed benchmark must have a claim report with dataset source,
license, command, commit, model, scorer, and baseline; executionFailures 0; no
same-model judge for public semantic-judge claims; fitted AND generalization
recall reported together for BEAM.


Initial Key Finding (P67-A, 2026-06-24)
---------------------------------------

The claim gate's first verdict over all five benchmarks on 2026-06-24: 0
publicly claimable, each blocked for a precise reason.

- LongMemEval (0.908), ImplicitMemBench (0.711), BEAM (0.695): blocked by a
  SAME-MODEL judge (gpt-5.5 answer + gpt-5.5 judge; .env EVAL and JUDGE are both
  gpt-5.5 @ ai.gurkiai.com) plus unverified provenance/commit.
- MemoryAgentBench (CR 0.959 / AR 0.67): cleanest methodology — deterministic
  upstream match-mode scoring, NO judge, so no same-model bias — but blocked by
  incomplete coverage (TTL 0/30, LRU 1/6) and a missing baseline.
- LoCoMo (0.020): a retrieval boundary, not a performance result.

P67-A INFRA CONSTRAINT: there was no independent judge model available for that
first verdict (only gpt-5.5 was served). So the same-model-judge blocker on
LongMemEval/BEAM could not be cleared by a judge swap at that point. The two
viable JUDGE-FREE public-claim routes were:
  1. MemoryAgentBench — complete TTL/LRU coverage + add a baseline (P67-C); it is
     already deterministic and judge-free.
  2. LongMemEval deterministic subset — report only the judge-free methods
     (abstention/contains/exact/expected_alternative/numeric_count), excluding the
     semantic_judge questions, as a separate honest sub-score (P67-B).
Those were the fastest honest paths; the independent-judge path was infra-blocked
like LoCoMo's neural-embedding path.

Current verdict after BEAM/LoCoMo promotion (2026-07-05): BEAM, LoCoMo, LongMemEval, and MemoryAgentBench are publicly claimable and README-promoted through `benchmark-claims/*.json` plus `gate:public-benchmark-claim --strict`.
ImplicitMemBench remains internal / blocked evidence because reproducibility,
dataset-source/license, and same-model judge-bias blockers are still present.


Tasks
-----

[DONE] P67-A Public benchmark claim gate (commit 3565877,
  scripts/run-public-benchmark-claim-gate.ts, gate:public-benchmark-claim).
  BenchmarkClaimReport schema + pure rule engine + over-claim detection; committed
  declarations under benchmark-claims/<name>.json; gitignored gate report under
  reports/release/claims/. Unit-tested (7/7). First run: 5 declarations, all
  consistent, 0 over-claiming, 0 publicly claimable.
  CLI guard update (2026-07-04): `gate:public-benchmark-claim` now rejects
  duplicate `--strict` and `--claims-dir` flags before reading claim
  declarations, keeping strict-mode and declaration-source selection
  unambiguous. This does not change the claimable benchmark set.

[DONE 2026-07-05] P67-A2 Official-protocol rescore CLI guard. The pinned
  package command is `eval:official-rescore`; the underlying comparability
  rescore entrypoint (`scripts/rescore-official-protocols.ts`) now rejects
  duplicate scalar benchmark/source/output/judge selectors before loading
  existing LongMemEval, LoCoMo, or BEAM answer reports for official-protocol
  judging: `--benchmark`, `--reference`, `--report`, `--root`, `--rubrics`,
  `--concurrency`, `--limit`, and `--run-id`. `--run-id` must be a single path
  segment; `--concurrency` and `--limit` must be canonical positive integers.
  This is input and output-directory hardening for the planned independent-judge
  comparability rescore only; it does not run a judge, change scores, or change
  public-claim boundaries.

[DONE 2026-07-02] P67-B LongMemEval public-claim replay. The v0.3.5
  `goodmemory-rules-only` replay produced a judge-free deterministic-subset
  public claim: 0.720 (360/500) versus no-memory 0.068 over the full 500
  questions, `executionFailures: 0`, MIT dataset provenance verified, commit
  `6f9152c`, and same-model semantic-judge rescues excluded from the claimed
  score. `benchmark-claims/longmemeval.json` now has `publicClaimAllowed: true`
  and passes `gate:public-benchmark-claim`; the old with-judge LongMemEval
  number remains internal diagnostic evidence only.
  CLI guard update (2026-07-04): `eval:phase-62-deterministic-subset` rejects
  duplicate `--report-path`, `--run-id`, `--output-dir`, `--claim-profile`, and
  `--baseline-profile` values before resolving the source report, keeping the
  judge-free helper's source/output/profile selection unambiguous. This does
  not change the 0.720 public claim.

[DONE 2026-06-25] P67-C MemoryAgentBench task-specific answer harness. The
  deterministic, judge-free task-specific harness completed the four-competency
  run with `executionFailures: 0` over 259 questions. Public claim scope is CR
  0.959 and TTL 0.767 versus no-memory 0.000 baselines; AR 0.890 and LRU 0.518
  are deliberately excluded because no-memory baselines are higher / multiple-
  choice leakage dominates. `benchmark-claims/memoryagentbench.json` now has
  `publicClaimAllowed: true` and passes `gate:public-benchmark-claim`.

[DONE 2026-07-05] P67-D BEAM public-claim re-anchor. The old internal >=0.75
  target was an improvement bar for the internal binary judge before a public
  BEAM reference existed; it is not the public-claim threshold. The public claim standard is now: official protocol + above the public reference + full disclosures.
  `benchmark-claims/beam.json` passes `gate:public-benchmark-claim`
  with official-protocol 0.802 versus the 0.49 public reference, while retaining
  the strict internal binary track (0.7225), cross-version same-family judge
  disclosure, fitted/generalization recall disclosure, no-pack ablation, and the
  instruction_following weak spot. Remaining conflict_update /
  instruction_following / temporal_order answer-gap work stays in the BEAM
  hardening lane and does not block the scoped public claim.

[SUPERSEDED] P67-E LoCoMo neural semantic retrieval probe (P65-R003). Resolved
  NEGATIVE 2026-06-27 (bf7373f: neural == BM25) and superseded by the semantic
  candidate-generation UNION lane (P65-R004/R005, board 70) which broke the
  additive ceiling. See board 70 for the union evidence trail.

[DONE 2026-07-02] P67-E2 LongMemEval HYBRID-profile judge-free measurement.
  Full 500 rerun of the goodmemory-hybrid profile (provider embedding additive +
  assisted extraction + Postgres; shard-resumable across a machine reboot;
  run-p67e-longmemeval-hybrid-current): judge-free deterministic subset
  0.722 (361/500), executionFailures 0; with-judge diagnostic 0.890.
  VERDICT: hybrid TIES the embedding-free rules-only public claim (0.720,
  360/500) at +1 question — the paid stack does not beat rules-only on the
  judge-free metric (LongMemEval evidence-session recall is already ~0.95, so
  the remaining gap is answer-side, not retrieval-side). Per the standing
  decision this does NOT add a second README row; it is banked as internal
  evidence that the public rules-only row UNDERSTATES nothing. Command:
  GOODMEMORY_ASSISTED_EXTRACTOR_PROVIDER=openai bun run eval:phase-62-full500
  -- --benchmark-root ~/.goodmemory-longmemeval --profile goodmemory-hybrid
  --shard-concurrency 4 --run-id run-p67e-longmemeval-hybrid-current
  --resume-existing-shards -> eval:phase-62-deterministic-subset --
  --report-path <runDir>/report.json --claim-profile goodmemory-hybrid.


Acceptance Criteria
-------------------

- Every claimed benchmark has a claim declaration (benchmark-claims/<name>.json).
- Every declaration has dataset source, license, command, commit, package version,
  model, scorer, and baseline.
- executionFailures = 0.
- No same-model judge for public semantic-judge claims.
- No additional MemoryAgentBench AR/LRU scope, LoCoMo default/category scope, or
  BEAM binary-track scope is promoted unless an updated declaration passes the
  claim gate; current public scopes are MemoryAgentBench CR/TTL, LoCoMo P4
  opt-in full-10, and BEAM official-protocol 100K.
- BEAM always reports fitted and generalization recall together.
- The claim gate (gate:public-benchmark-claim --strict) is consistent (no
  over-claiming) before any README benchmark row changes.


Source And Evidence Pointers
----------------------------

- Claim gate: scripts/run-public-benchmark-claim-gate.ts (gate:public-benchmark-claim)
- Declarations: benchmark-claims/*.json
- Gate report (gitignored): reports/release/claims/claim-gate-report.json + summary.md
- Release readiness (Phase 66): scripts/run-v0-3-release-readiness.ts
- LoCoMo paused boundary: task-board/70-phase-65-...txt
