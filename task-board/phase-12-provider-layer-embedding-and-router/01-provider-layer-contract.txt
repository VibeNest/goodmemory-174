Phase 12.1: Provider Layer Contract
===================================

[DONE] P12.1-T001 Freeze provider-layer goals
[DONE] P12.1-T002 Define Vercel AI SDK based text/object generation adapters
[DONE] P12.1-T003 Decide which capabilities stay outside `createGoodMemory()` until behavior lands

Why this exists
---------------
- `llm` and `embedding` belong in the architecture, but they should only enter the public runtime contract when they power actual library behavior.

Write tests first
-----------------
- provider config parsing works for OpenAI and Anthropic
- deterministic fallback remains available
- unsupported providers fail fast

Implementation steps
--------------------
1. Keep `src/llm/vercel-ai-sdk.ts` as the only live-model entry point
2. Split text generation, object generation, and judge generation clearly
3. Add provider metadata to eval/report artifacts

Acceptance
----------
- Provider-backed capabilities have one consistent access layer
- Live vs fallback mode is explicit everywhere

Progress notes
--------------
- Added an internal `src/provider/layer.ts` contract that wraps Vercel AI SDK text/judge factories behind one provider layer surface
- `run-eval` now builds runtime metadata through the provider layer and persists layer/provider/model details into eval artifacts
- Public `GoodMemoryConfig` remains unchanged: provider-backed capabilities stay outside `createGoodMemory()` until real library behavior lands
