Phase 12.6: Eval Expansion
==========================

[DONE] P12.6-T001 Add mode labels for rules-only vs hybrid vs llm-assisted
[DONE] P12.6-T002 Add eval slices that isolate embedding impact
[DONE] P12.6-T003 Add eval slices that isolate router impact
[DONE] P12.6-T004 Add regression summary by strategy

Why this exists
---------------
- Provider-backed features should ship only if eval proves they help.

Write tests first
-----------------
- reports show strategy labels
- strategy comparisons preserve same persona/scenario set
- fallback and live paths remain distinguishable

Implementation steps
--------------------
1. Extend eval suite inputs with strategy selection
2. Produce A/B/C summaries when multiple strategies are run
3. Save per-strategy failures and raw recall artifacts

Current implementation notes
----------------------------
- `runGoodMemoryScenario()` now accepts `strategy` and threads it into `memory.recall()`
- router strategy now affects real recall behavior for supported paths (`rules-only` vs semantic `hybrid`), not only trace metadata
- strategy labels, dedicated comparison slices, and regression summaries remain TODO in this phase

Acceptance
----------
- Strategy changes are measurable, not anecdotal
- Release decisions can be made from eval evidence

Current implementation notes
----------------------------
- eval answer packages now carry strategy labels separately from baseline/goodmemory mode, so per-strategy runs stay visible in persisted artifacts
- eval suite can replay the same persona/scenario set across multiple requested strategies in one run, suffixing case ids only when strategy fanout is active
- report summaries now include `strategySummary.byStrategy` plus `embeddingImpact` and `routerImpact` comparison slices with scenario-coverage checks
- strategy regressions are summarized directly in report artifacts, so provider-backed retrieval changes can be judged by strategy instead of anecdote
