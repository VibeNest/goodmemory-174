Phase 36.3: Rules DSL and Message Annotations
=============================================

Goal
----
Give integrators a public deterministic way to define domain write rules without writing full extractors.


Tasks
-----
[DONE] Add root-exported rule helpers under a stable namespace such as `rememberRules`.
[DONE] Support rules that can target:
  - profile
  - preference
  - reference
  - fact
  - feedback
[DONE] Support regex, predicate, and direct mapper rule inputs.
[DONE] Add `ruleId` to generated candidates and remember traces.
[DONE] Implement `MessageAnnotation` processing before classification.
[DONE] Support annotation values:
  - `remember: "always" | "never" | "auto"`
  - `kindHint`
  - `metadataPatch`
  - `confirmed`
  - `verified`
  - `reason`
[DONE] Ensure `remember: "never"` suppresses both deterministic and assisted candidates for the annotated message.
[DONE] Ensure `remember: "always"` can raise an otherwise valid low-confidence candidate into the write path without bypassing policy/redaction.


Design Notes
------------
The DSL should stay small and map to the existing candidate model. It is not a second storage taxonomy.

Example target shape:

```ts
rememberRules.fact(/my top priority this quarter is (.+)/i, {
  id: "life-goal-priority",
  category: "goal",
  tags: ["life_coach", "long_term_goal"],
  content: ({ match }) => match[1],
});
```


Acceptance
----------
- rules produce normal `MemoryCandidate`s
- annotations can suppress and enrich generated candidates
- trace output can explain which rule or annotation affected a write
- policy hooks still run after rule/annotation processing
