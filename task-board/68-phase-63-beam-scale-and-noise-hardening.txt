Phase 63: BEAM Scale And Noise Hardening
========================================

[WIP] Phase 63 is the BEAM leg of Sequential Benchmark Hardening. This file is
the executable phase entrypoint, not a run-history dump. Use git history and
generated reports for provenance.

Status
------

- Active benchmark: BEAM 100K.
- Current mode: answer-gap hardening with recall/noise follow-up.
- Current outcome wording: accepted measured full-run checkpoint, not
  performance closure. Latest accepted checkpoint is the answer-hardening
  evidence-pack run: 278/400 answer accuracy (0.695), up from the no-pack
  baseline 224/400 (0.56) and prior evidence-pack checkpoint 261/400
  (0.6525), but 122/400 wrong answers remain.
- Current data root: `/private/tmp/BEAM`.
- Public reporting remains deferred until LongMemEval, BEAM, MemoryAgentBench,
  and LoCoMo all have accepted evidence that is mature enough for external
  claims.

Accepted Inputs
---------------

- Paper/dataset source: BEAM by Tavakoli et al.; upstream dataset
  `Mohammadta/BEAM`, license `cc-by-sa-4.0`.
- Local export: `/private/tmp/BEAM/100K.json`, 20 rows, 400 probing questions.
  The prep script supports Hugging Face rows and GitHub raw fallback.
- External-root prep input guard: `prepare:phase-63-beam` rejects duplicate
  scalar source/output/fetch selectors before fetching rows or writing the BEAM
  root: `--dataset`, `--github-api-root`, `--github-concurrency`,
  `--github-raw-root`, `--length`, `--offset`, `--output-root`, `--source`,
  and `--split`.
- Smoke harness: `run-phase63-beam-smoke-current`, gate `run-20260518003000`,
  `executionFailures: 0`.
- Gate input guard: `gate:phase-63` rejects duplicate `--output-dir` and
  `--run-id` before running smoke gate checks; `gate:phase-63-beam-closure`
  rejects duplicate `--closure-report`, `--output-dir`, and `--run-id` before
  reading closure evidence or writing gate artifacts. Both gate commands require
  `--run-id` to be a single path segment so gate artifacts stay under the
  intended quality-gates directory.
- Initial real adapter run: `run-phase63-beam-100k-full-initial-20260518T000335Z`,
  all four profiles, `executionFailures: 0`. This proves ingestion and report
  contracts only.

Latest Accepted Diagnostic
--------------------------

Run: `run-phase63-beam-100k-recall-diagnostic-rules-project-card-total-count-current-20260615T200000Z`

Baseline: `run-phase63-beam-100k-recall-diagnostic-rules-cover-letter-submission-count-current-20260615T193000Z`

- evidence-chat recall: 0.9620612564274538
- missed-recall cases: 20/355
- wrong-recall/noise cases: 167/400
- zero-recall cases: 0
- global hit/missing/noise ids: 1022/72/810 -> 1023/71/807
- retained target: `3:multi_session_reasoning:1`, recovered from recall 0.5
  to 1 while shedding three noise chats and adding no negative deltas.
- boundary: this is scenario-fitted recall evidence. The README-level dual
  metric keeps the fitted 0.9621 separate from the generalization floor 0.6822.
- General-lever recall remeasure is routed through
  `eval:phase-63-general-levers`. By default it disables registered narrow gates
  and measures the BEAM generalization floor, not the fitted scenario-retained
  path; use `--keep-gates` only when explicitly remeasuring the fitted path.
  The runner keeps non-provider `floor` / `bm25` arms embedding-free even when
  provider embedding env is present, preserves provider semantic-union behavior
  for union arms, and includes non-default `--semantic-topk` values in default
  union run ids so derived evidence directories do not hide widened union
  budgets behind the default `union16` name. It also requires output
  `--run-id` values to be a single path segment before invoking the recall
  diagnostic, so general-lever report directories stay under the intended
  output tree.

Latest Accepted Measured Full-Run Checkpoint
--------------------------------------------

Run: `run-phase63-beam-100k-live-closure-gpt55-evidence-pack-answer-hardening-current`

Prerequisite diagnostic (zero-failure): `run-phase63-beam-100k-recall-diagnostic-rules-postmerge-current`

Gate: `run-phase63-beam-closure-gate-gpt55-evidence-pack-answer-hardening-current` -- accepted

Models: gpt-5.5 answer generation + gpt-5.5 semantic judge
(OpenAI-compatible ai.gurkiai.com)

- profile: `goodmemory-rules-only`
- scale: 100K, all 400 cases
- answer context: `--evidence-pack` via `src/answer/evidencePack.ts`
- answer accuracy: 278/400 (0.695)
- prior evidence-pack checkpoint: 261/400 (0.6525), same 0.9621 recall
- prior no-pack baseline: 224/400 (0.56), same 0.9621 recall
- evidence-chat recall: 0.9620612564274538
- missed-recall cases: 20/355
- wrong-answer cases: 122/400
- wrong-recall/noise cases: 167/400
- executionFailures: 0
- boundary: pipeline/gate closed, performance not closed, public benchmark
  claim not closed. The gate accepts coverage plus zero failures; it does not
  enforce a numeric accuracy threshold.

Answer-Gap Evidence And Tooling
-------------------------------

- `src/answer/evidencePack.ts` is the general answer-time shaping layer. It is
  internal and does not change the public API.
- `scripts/analyze-phase-63-live-answer-gap.ts` attributes wrong answers by
  recall status and answer-time repair family.
- Existing baseline artifact:
  `run-phase63-beam-live-answer-gap-baseline-current` analyzes the no-pack
  0.56 run: 176 wrong, including 103 full-recall-clean, 41 full-recall-noisy,
  and 18 missing-evidence cases.
- Latest local evidence-pack artifact:
  `run-phase63-beam-live-answer-gap-evidence-pack-current` analyzes the 0.6525
  evidence-pack run: 139 wrong, including 76 full-recall-clean, 34
  full-recall-noisy, 16 missing-evidence, 8 abstention, and 5 unknown cases.
- Latest answer-hardening artifact:
  `run-phase63-beam-live-answer-gap-answer-hardening-current` analyzes the
  0.695 answer-hardening run: 122 wrong, including 58 full-recall-clean, 37
  full-recall-noisy, 15 missing-evidence, 7 abstention, and 5 unknown cases.
- `scripts/run-phase-63-beam-live-ablation.ts` is available for LLM-ready
  ablations. Existing artifacts show `retrieved-evidence-pack` at 0.6625 and
  `full-context` at 0.195 with many failures, so dumping the whole conversation
  into the model is not a viable fix.
- Answer-gap and ablation CLIs reject duplicate scalar source/output/mode
  selectors before reading live reports or writing derived evidence:
  `--benchmark-root`, `--live-report`, `--output-dir`, `--output-path` where
  supported, `--mode`, `--profile`, `--limit`, `--run-id`, and `--scale`. This
  now also includes answer-gap output/source protection against candidate BEAM
  source files under `--benchmark-root`, and answer-gap / ablation output
  `--run-id` values must be a single path segment before deriving default output
  paths. This is evidence-input and output-directory hardening only; it does not
  change BEAM scores, recall, checkpoint status, or public-claim boundaries.
- BEAM source-root env fallbacks are canonical across root-prep, smoke/full,
  recall diagnostic, live slice, live closure, answer-gap, and ablation
  evidence: `GOODMEMORY_BEAM_ROOT` must not be empty or whitespace-padded before
  it is used as an output-root or benchmark-root fallback. Explicit
  `--output-root` / `--benchmark-root` CLI values still take precedence. This is
  source-root integrity hardening only; it does not change BEAM scores, recall,
  checkpoint status, or public-claim boundaries. Unit coverage lives in
  tests/unit/prepare-phase-63-beam-data.test.ts,
  tests/unit/run-phase-63.script.test.ts,
  tests/unit/run-phase-63.beam-recall-diagnostic.test.ts,
  tests/unit/run-phase-63.beam-live-slice.test.ts,
  tests/unit/run-phase-63.beam-live-closure.test.ts,
  tests/unit/analyze-phase-63-live-answer-gap.test.ts, and
  tests/unit/run-phase-63.beam-live-ablation.test.ts.
- Base `eval:phase-63` smoke/full eval rejects duplicate scalar source/output/run
  selectors before report generation: `--benchmark-root`, `--limit`, `--mode`,
  `--offset`, `--output-dir`, `--run-id`, and `--scale`. Repeated multi-value
  selectors such as `--case-id`, `--profile`, and `--question-type` remain
  repeatable. Output `--run-id` values must also be a single path segment before
  deriving smoke/full report directories. This is smoke/full evidence-input and
  evidence-directory hardening only; it does not change BEAM scores, recall,
  checkpoint status, or public-claim boundaries.
- Recall diagnostic and recall-analysis CLIs reject duplicate scalar
  source/report/output/run selectors before running diagnostics or comparing
  reports. The diagnostic runner keeps repeated `--profile` support while
  making `--benchmark-root`, `--limit`, `--output-dir`, `--run-id`, and
  `--scale` single-valued and requires output `--run-id` values to be a single
  path segment; the analyzer makes `--baseline-report-path`, `--baseline-run-id`,
  `--benchmark-root`, `--output-dir`, `--output-path`, `--profile`,
  `--report-path`, `--run-id`, and `--source-turn-limit` single-valued, and
  requires `--run-id` / `--baseline-run-id` values to be single path segments
  before resolving default report paths. This is recall/noise evidence-input and
  report-directory hardening only; it does not change BEAM scores, recall,
  checkpoint status, or public-claim boundaries.

Current Failure Surface
-----------------------

- BEAM answer capability is not closed: 122/400 answers are still wrong after
  the answer-hardening evidence pack.
- Top repair families after the preference bucket split: conflict_update 29
  (dominant full-recall-clean), instruction_following 27 (dominant
  full-recall-noisy), temporal_order 23 (dominant full-recall-clean),
  aggregate_count 15, summarization 9, preference_following 8, abstention 7,
  and the remaining judge_or_expected_answer 3.
- Live-measured repair landed in `src/answer/evidencePack.ts` and the BEAM
  live-slice context builder: optional
  question-type metadata now routes event/temporal questions to timeline
  evidence, contradiction/update/CR questions to current-value framing, and
  summarization questions to summary framing. The pack also adds order
  answer-shape guidance for requested item counts/topics, value-bearing count
  tables, multi-session facet framing, instruction standing/latest constraint
  framing, contradiction guidance that avoids yes/no-only answers, local
  contradiction minimal-pair extraction that suppresses adjacent implementation
  noise, selective assistant companion evidence for synthesis/instruction-like
  questions, and narrow answer-operation guardrails. The live proof improved
  hard-slice accuracy to 10/12 and full-run accuracy to 278/400.
- The next live-measured repair lane is conflict/update resolution first, then
  instruction noise budgeting and temporal-order granularity, not another BEAM
  case-specific recall gate.
- Use noise budgeting for instruction_following only if the live slice still
  shows full-recall-noisy failures after the instruction constraint framing.
- Treat the remaining judge_or_expected_answer 3 as expected-answer/judge-review
  candidates unless a focused live slice shows a generic extraction failure:
  one expected answer includes a slot not present in the retrieved evidence, one
  is near-equivalent but omits an "intended actions" phrase, and one is factually
  equivalent with an extra person name.
- Do not resume BEAM case-specific gates until the analyzer shows a recall-side
  reason to do so.

Working Loop
------------

1. Use `run-phase63-beam-live-answer-gap-answer-hardening-current` as the next
   repair source of truth.
2. Patch the highest-value answer-time lane generically, then validate in a
   focused live slice before another full BEAM rerun.
3. Validate the same answer-time behavior against Phase 64 MemoryAgentBench CR
   before treating a BEAM gain as general.
4. Run the full BEAM live measured run only after a focused live gain; gate it
   only if the full run has `executionFailures: 0`.

Do not treat a small live slice, one selector repair, or the 0.695 measured
checkpoint as BEAM performance closure.

Current local pre-live hardening in this worktree targets the analyzer's top
answer-time lanes: conflict/update current-value ledgers with target
date/time/quantity cues from the latest candidate, contradiction minimal-pair
extraction that suppresses adjacent implementation noise, instruction
constraint/support separation with bounded topic-matched evidence plus concrete
answer-content cues for versioned dependencies and named tools, and
temporal-order milestone cues with code-block stripping plus question-target
timeline anchors that keep source order while suppressing adjacent project
noise, plus aggregate/date count ledgers that separate endpoint dates, duration
labels, and numeric
quantities, plus source-ordered summary coverage checklists for user themes and
assistant guidance, plus value-bearing summary anchors, plus required summary
source-coverage audits for late topic shifts, plus analyzer-level
source-coverage warnings for expected cues found outside declared source chats,
plus abstention target checks that reject adjacent-but-insufficient evidence,
plus topic-specific missing-context abstention wording in focused live prompts,
plus preference-following constraint packs that treat user preferences as
response requirements. This is deterministic and unit-verified only; it is not a
new accepted BEAM checkpoint until a focused live slice and any later full live
measured run prove it.

Pre-live infrastructure check in this worktree: `/private/tmp/BEAM/100K.json`
was rebuilt from GitHub raw via `prepare:phase-63-beam` (20 rows),
`eval:phase-63` and `gate:phase-63` passed with `executionFailures: 0`, and
`run-phase63-beam-100k-recall-diagnostic-rules-prelive-current` compared against
`run-phase63-beam-100k-recall-diagnostic-rules-postmerge-current` with
`caseDeltaCount: 0`, evidence-chat recall 0.9620612564274538, missed 20/355,
wrong-recall/noise 167/400, and hit/missing/noise 1023/71/807. This proves
refreshed root/recall readiness only; answer performance still requires a
focused live slice.

Focused summarization live check in this worktree:
`run-phase63-beam-live-slice-summary-value-anchors-prelive-current` stayed at
1/9 correct with `executionFailures: 0`, matching the prior summarization
coverage slice. The analyzer rerun
`run-phase63-beam-live-answer-gap-summary-value-anchors-current` found 8/9
wrong-answer cases in summarization, with source-coverage warnings on 5/8 wrong
cases and 21 cues. Representative cases include `9:summarization:1` expecting a
freelance-contract budget stage outside its declared source chats and
`14:summarization:1` expecting rental/subscription/discount/library/dietary
details outside its source chats. Do not run full BEAM from this result; route
summarization next to source-coverage / expected-answer compatibility review.
The full source-coverage audit
`run-phase63-beam-live-answer-gap-answer-hardening-source-coverage-current`
keeps the accepted answer-hardening shape at 122 wrong answers, then adds source
coverage status counts: 95 covered-or-no-warning, 15
expected-cues-outside-source, and 12 no-declared-source-ids. Warning buckets are
temporal_order 9 cases / 16 cues, summarization 5 / 21, aggregate_count 2 / 4,
and conflict_update 1 / 2. Manual summarization audit: `9:summarization:1`,
`14:summarization:1`, and `19:summarization:1` are declared-source mismatches;
`20:summarization:1` and `20:summarization:2` have no declared source ids. Treat
the summarization warnings as high-signal source/expected-answer review inputs;
the temporal/count warning buckets are heuristic inspection queues before
calling those lanes purely answer-pack failures. The live-slice runner now
supports `--answer-gap-source-coverage-status covered-or-no-warning`; use it for
answer-pack-only focused slices so source metadata mismatches do not dilute the
measurement. Repeated live-slice selectors are now validated before files are
read: `--answer-gap-bucket`, `--answer-gap-source-coverage-status`, and
`--case-id` reject missing/flag-as-value, empty, whitespace-padded, duplicate,
or unknown answer-gap bucket/status values. This keeps focused BEAM live slices
from silently running the wrong answer-gap queue or matching no cases because of
a CLI typo. `analyze:phase-63-recall-diagnostic` now rejects output paths that
would overwrite either its analyzed report or baseline report before reading
inputs, and rejects output paths that would overwrite a BEAM source file under
`--benchmark-root` before reading benchmark rows. This keeps recall/noise
workbench evidence from destroying source reports or benchmark source files.
`analyze:phase-63-beam` also rejects duplicate scalar source/output/run
selectors (`--report-path`, `--output-path`, `--run-id`) before reading inputs
or writing miss-case analysis, and rejects output paths that would overwrite its
source `--report-path` before reading inputs. It also requires `--run-id` to be
a single path segment before resolving the default source report path, keeping
early miss-case analysis evidence from ambiguous CLI selectors, source-report
path traversal, or source-report destruction.
`eval:phase-63-live-slice` and `eval:phase-63-live-closure` also
reject duplicate `--evidence-pack` and `--resume` mode switches plus duplicate
scalar source/output selectors where supported (`--answer-gap-report`,
`--benchmark-root`, `--case-selection`, `--limit`, `--output-dir`, `--profile`,
`--recall-report`, `--run-id`, `--scale`) before report generation, and require
`--run-id` to be a single path segment, keeping answer-context mode, source
report, benchmark root, output location, run id, live evidence directory, and
checkpoint/resume semantics unambiguous. `scripts/analyze-phase-63-live-answer-gap.ts`
and `scripts/run-phase-63-beam-live-ablation.ts` also reject output report paths
that would overwrite the input `--live-report` before reading benchmark or
live-report sources, and the answer-gap analyzer rejects explicit output paths
that would overwrite a BEAM source file under `--benchmark-root`. This keeps
answer-gap and ablation routing evidence from destroying the source live report
or benchmark source, or from escaping intended output directories through
malformed run ids, without changing BEAM scores, recall, or closure boundaries.
`GOODMEMORY_BEAM_ROOT` fallbacks are now rejected when empty or whitespace-padded
before root-prep, smoke/full, recall, live-slice, live-closure, answer-gap, or
ablation evidence can resolve an output root or benchmark root; explicit CLI
roots keep precedence. This prevents malformed env-derived roots from being
recorded as BEAM evidence provenance without changing scores, recall, or closure
boundaries.
For summarization, the corrected
filtered answer-pack candidate set
is only `12:summarization:1` and `14:summarization:2`; `18:summarization:1` and
`18:summarization:2` are correctly classified as `no-declared-source-ids`. The
corrected filtered live slice
`run-phase63-beam-live-slice-summary-source-covered-v2-prelive-current` measured
1/2 with `executionFailures: 0` and evidence recall 1.0: `12:summarization:1`
passed, while `14:summarization:2` still failed because the source evidence is
about movie recommendations and personal details while the expected answer asks
for a generic project lifecycle.

Acceptance Checks
-----------------

- Documentation stays compact and evidence-linked.
- `git diff --check` passes after documentation-only changes.
- For future evidence-pack code work: focused tests, `bun run typecheck`, and
  `git diff --check` must pass before any live run.
- For future live measured runs: full 400-case run, same-profile zero-failure recall
  diagnostic, `executionFailures: 0`, and a closure gate artifact are required.

Commands
--------

```text
bun run prepare:phase-63-beam -- --output-root /private/tmp/BEAM --split 100K --length 100 --source github-raw --github-concurrency 8
bun run eval:phase-63-recall-diagnostic -- --benchmark-root /private/tmp/BEAM --profile <goodmemory-rules-only|goodmemory-hybrid> --run-id <run-id>
bun run eval:phase-63-general-levers -- --benchmark-root /private/tmp/BEAM --arm <floor|bm25|union16|bm25-union16> --output-dir <reports-dir> --run-id <run-id>
bun run analyze:phase-63-recall-diagnostic -- --report-path <report> --baseline-report-path <baseline> --benchmark-root /private/tmp/BEAM
bun run scripts/analyze-phase-63-live-answer-gap.ts --benchmark-root /private/tmp/BEAM --live-report reports/eval/research/phase-63/beam/run-phase63-beam-100k-live-closure-gpt55-evidence-pack-answer-hardening-current/live-slice-report.json --run-id run-phase63-beam-live-answer-gap-answer-hardening-current
bun run scripts/run-phase-63-beam-live-ablation.ts --benchmark-root /private/tmp/BEAM --live-report <live-slice-report.json> --mode <gold-evidence-only|retrieved-hit-only|retrieved-raw-uncompressed|full-context|gold-evidence-pack|retrieved-evidence-pack> --run-id <run-id>
bun run eval:phase-63-live-slice -- --benchmark-root /private/tmp/BEAM --recall-report <recall-diagnostic.json> --profile <goodmemory-rules-only|goodmemory-hybrid> --evidence-pack --answer-gap-report reports/eval/research/phase-63/beam/run-phase63-beam-live-answer-gap-answer-hardening-source-coverage-current/live-answer-gap-analysis.json --answer-gap-bucket preference_following --answer-gap-bucket abstention --answer-gap-bucket summarization --answer-gap-source-coverage-status covered-or-no-warning --run-id <run-id>
bun run eval:phase-63-live-closure -- --benchmark-root /private/tmp/BEAM --recall-report <recall-diagnostic.json> --profile <goodmemory-rules-only|goodmemory-hybrid> --run-id <run-id>
bun run gate:phase-63-beam-closure -- --closure-report <phase-63-beam-closure-report.json> --run-id <gate-run-id>
git diff --check
```

Breakdown
---------

- `task-board/phase-63-beam-scale-and-noise-hardening/00-README.txt`
