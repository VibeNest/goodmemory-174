Phase 63: BEAM Scale And Noise Hardening
========================================

Status
------

[WIP] Phase 63 starts the BEAM leg of the sequential external benchmark
hardening plan after Phase 62 LongMemEval closed with clean internal evidence.

This phase is internal research hardening, not the final public benchmark
report. It should carry forward the generic LongMemEval repairs while testing
GoodMemory under BEAM's larger-scale context, noise, and retrieval-stability
pressure.


Driver
------

The selected external benchmark order remains:

1. LongMemEval
2. BEAM
3. MemoryAgentBench
4. LoCoMo

BEAM goes second because LongMemEval has now proven the long-term QA and
multi-session memory loop can beat the latest accepted full-context reference
on the current internal harness. BEAM should stress the next failure mode:
large context scale, distractor/noise filtering, token budget pressure, and
recall stability under many irrelevant memories.


Accepted Scope
--------------

- Identify and record the authoritative BEAM upstream source, license, data
  access path, and benchmark task shape.
- Add small synthetic smoke fixtures that match BEAM's shape without vendoring
  full upstream data.
- Add an external `--benchmark-root` full-run adapter, following the Phase 62
  LongMemEval pattern.
- Compare at least:
  - `baseline-no-memory`
  - `baseline-full-context`
  - `goodmemory-rules-only`
  - `goodmemory-hybrid`
- Track answer correctness, evidence/session recall where applicable,
  distractor/noise selection, token/context pressure, and execution failures.
- Produce a BEAM miss-case analysis before changing GoodMemory behavior.


Out Of Scope
------------

- final public benchmark report
- MemoryAgentBench or LoCoMo adapters
- README-level claim that GoodMemory is strong on BEAM
- vendoring full BEAM data
- benchmark-specific runtime hacks
- public API or public config widening


Tasks
-----

[DONE] P63-T001 Identify BEAM source, license, data format, and local full-run
  requirements.
[DONE] P63-T002 Add BEAM synthetic smoke fixtures and attribution.
[DONE] P63-T003 Add BEAM adapter/report contract and smoke runner.
[DONE] P63-T004 Add BEAM gate for smoke harness integrity.
[DONE] P63-T005 Run an initial full BEAM comparison through external
  `--benchmark-root`.
[DONE] P63-T006 Write BEAM miss-case analysis grouped by scale/noise/token
  pressure and recall/answer failures.
[WIP] P63-T007 Implement only generic GoodMemory repairs from BEAM misses and
  rerun BEAM before moving to MemoryAgentBench.


Commands
--------

```text
mkdir -p /private/tmp/BEAM
bun run prepare:phase-63-beam -- --output-root /private/tmp/BEAM --split 100K --offset 0 --length 20
# Fallback when Hugging Face datasets-server rows are unavailable.
bun run prepare:phase-63-beam -- --output-root /private/tmp/BEAM --split 100K --length 100 --source github-raw
bun run eval:phase-63
bun run gate:phase-63
bun run analyze:phase-63-beam -- --run-id run-phase63-beam-100k-full-initial-20260518T000335Z
bun run eval:phase-63-recall-diagnostic -- --benchmark-root /private/tmp/BEAM --profile goodmemory-rules-only --run-id run-phase63-beam-100k-recall-diagnostic-rules-full-20260518T005500Z
bun run eval:phase-63-live-slice -- --benchmark-root /private/tmp/BEAM --recall-report reports/eval/research/phase-63/beam/run-phase63-beam-100k-recall-diagnostic-rules-full-20260518T005500Z/recall-diagnostic.json --profile goodmemory-rules-only --limit 3 --run-id run-phase63-beam-100k-live-slice-rules-initial3-escalated-20260518T014500Z

# Full/external root contract for the prepared BEAM 100K JSON export.
bun run eval:phase-63 -- --mode full --benchmark-root /private/tmp/BEAM --scale 100K
```


Breakdown Folder
----------------

- `task-board/phase-63-beam-scale-and-noise-hardening/00-README.txt`


Source Intake
-------------

- Paper:
  Tavakoli et al., "Beyond a Million Tokens: Benchmarking and Enhancing
  Long-Term Memory in LLMs", arXiv:2510.27246. The paper introduces BEAM as
  100 long coherent conversations with 2,000 validated probing questions and
  conversations up to 10M tokens.
- Dataset:
  `https://huggingface.co/datasets/Mohammadta/BEAM`
  - license: `cc-by-sa-4.0`
  - format: Parquet
  - splits visible on Hugging Face: `100K` (20 rows), `500K` (35 rows), and
    `1M` (35 rows), 90 rows total
  - useful columns include `conversation_id`, `conversation_seed`,
    `narratives`, `user_profile`, `conversation_plan`, `user_questions`,
    `chat`, and `probing_questions`
- 10M dataset:
  `https://huggingface.co/datasets/Mohammadta/BEAM-10M`
- Initial adapter strategy:
  start with the 100K split for smoke/full-run shape validation, then extend to
  500K/1M and only then decide whether 10M is feasible for local research
  runs. Keep all full upstream files external to the repo.
- Local data preparation:
  `prepare:phase-63-beam` exports the Hugging Face datasets-server rows API
  response to an external root such as `/private/tmp/BEAM/100K.json`, fails if
  rows are partial or truncated, and keeps upstream data out of git.
  When `datasets-server.huggingface.co` is unavailable, the same script also
  supports `--source github-raw`, which reconstructs the 100K export from the
  upstream GitHub conversation folders (`chat.json`, `topic.json`,
  `plan_new.txt`, `user_messages.json`,
  `probing_questions/probing_questions.json`, `labels.txt`, `main_spec.txt`,
  and `relationships.txt`). Treat GitHub-raw reruns as their own source cohort:
  the shape is complete and loader-compatible, but tiny retrieval drift can
  occur versus an older Hugging Face rows export. External BEAM fetches use
  bounded `curl` retry, connect-timeout, and max-time flags so transient rows
  or raw-source failures do not require manual command rewriting.


Smoke Harness Evidence
----------------------

- Synthetic shape-compatible fixture:
  `fixtures/external-benchmarks/beam/beam_100k_smoke.json`
- Attribution:
  `fixtures/external-benchmarks/ATTRIBUTION.md`
- Adapter/report contract:
  `src/eval/beam.ts`
- Runner and gate:
  `scripts/run-phase-63-eval.ts`, `scripts/run-phase-63-gate.ts`
- Report analysis:
  `scripts/analyze-phase-63-beam-report.ts`
- Real recall diagnostic:
  `scripts/run-phase-63-beam-recall-diagnostic.ts`
- Live answer-generation/judge slice:
  `scripts/run-phase-63-beam-live-slice.ts`
- External-root preparation:
  `scripts/prepare-phase-63-beam-data.ts`
- Accepted local smoke run:
  `run-phase63-beam-smoke-current`
  - 3 synthetic questions across specific, preference, and abstention cases
  - four profiles compared: `baseline-no-memory`, `baseline-full-context`,
    `goodmemory-rules-only`, `goodmemory-hybrid`
  - `executionFailures: 0`
- Accepted local gate:
  `run-20260518003000`, status `accepted`
- Verification:
  `bun test tests/unit/analyze-phase-63-beam-report.test.ts tests/unit/beam.test.ts tests/unit/prepare-phase-63-beam-data.test.ts tests/unit/run-phase-63.script.test.ts tests/unit/run-phase-63.gate.test.ts`
  and `bun run gate:phase-63`


Initial External-Root Evidence
------------------------------

- External root:
  `/private/tmp/BEAM`
- Prepared data:
  `/private/tmp/BEAM/100K.json`
  - dataset: `Mohammadta/BEAM`
  - split: `100K`
  - rows: 20/20
  - generated metadata: `/private/tmp/BEAM/phase-63-beam-export-metadata.json`
- Accepted initial full run:
  `run-phase63-beam-100k-full-initial-20260518T000335Z`
  - mode: `full`
  - total cases: 400
  - profiles compared: `baseline-no-memory`, `baseline-full-context`,
    `goodmemory-rules-only`, `goodmemory-hybrid`
  - `executionFailures: 0`
  - `baseline-no-memory`: 40/400, evidence-chat recall 0.0 over 355 evidence
    cases
  - `baseline-full-context`: 400/400, evidence-chat recall 1.0, wrong-recall
    cases 400 because it retrieves every chat turn
  - `goodmemory-rules-only`: 400/400, evidence-chat recall 1.0, wrong-recall
    cases 0
  - `goodmemory-hybrid`: 400/400, evidence-chat recall 1.0, wrong-recall cases
    0
- Boundary:
  this is external-root adapter and evidence-contract proof over real BEAM 100K
  rows. It is not the final BEAM score because the current Phase 63 profile
  hypotheses still use deterministic oracle answers/evidence ids rather than
  live GoodMemory answer generation and live judging.


Initial Miss/Boundary Analysis
------------------------------

- Analysis artifact:
  `reports/eval/research/phase-63/beam/run-phase63-beam-100k-full-initial-20260518T000335Z/miss-case-analysis.json`
- Status:
  `needs-live-retrieval-analysis`
- Dataset buckets:
  - 400 total 100K cases across 10 BEAM categories, 40 each
  - 360 answerable cases, 40 abstention cases
  - 355 answerable cases with source/evidence chat ids
  - 5 answerable cases without source chat ids, requiring a source-id-blind
    answer-quality bucket in live analysis
- Failure/noise buckets:
  - `baseline-no-memory`: 360 answer failures and 355 missed-recall cases; this
    is the expected lower-bound control
  - `baseline-full-context`: 400/400 answer-correct but 400 wrong-recall/noise
    cases, average 286.6 retrieved chat ids per case, average 283.865
    distractor ids per case, and max 392 retrieved ids; this is the token/noise
    pressure baseline
  - `goodmemory-rules-only` and `goodmemory-hybrid`: 400/400 with no recall
    misses in this report, but only because the current full profile uses
    deterministic oracle answers/evidence ids
- Decision:
  do not implement GoodMemory behavior repairs from this oracle report. The next
  P63-T007 step must add or run a real GoodMemory BEAM recall diagnostic that
  seeds chat turns and measures retrieved chat ids without oracle retrieval,
  then run a small live answer-generation/judge slice on representative misses.


Real Recall Diagnostic Evidence
-------------------------------

- Full rules-only diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-20260518T005500Z`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.11625896794910878
  - missed-recall cases: 340/355
  - wrong-recall/noise cases: 362/400
  - average retrieved chat ids: 2.545
  - average distractor chat ids: 2.32
- Initial hybrid diagnostic slice:
  `run-phase63-beam-100k-recall-diagnostic-hybrid-initial20-20260518T010000Z`
  covers 20 cases with evidence-chat recall 0.12962962962962962, missed-recall
  cases 17/18, wrong-recall/noise cases 20/20, and `executionFailures: 0`.
- Decision:
  the deterministic oracle GoodMemory rows from the initial full comparison are
  not representative of real recall. P63-T007 now has a concrete failure
  surface: low evidence retrieval plus high noise under real GoodMemory recall.
  The next step is a small live answer-generation/judge slice over
  representative diagnostic misses before any generic mechanism repair.


Initial Live Slice Evidence
---------------------------

- Sandbox-network dry run:
  `run-phase63-beam-100k-live-slice-rules-initial3-20260518T014000Z`
  preserved the same three selected miss cases but all three failed at
  `answer_generation` with provider connection errors. This is environment
  evidence only.
- Authorized live slice:
  `run-phase63-beam-100k-live-slice-rules-initial3-escalated-20260518T014500Z`
  - source recall report:
    `run-phase63-beam-100k-recall-diagnostic-rules-full-20260518T005500Z`
  - profile: `goodmemory-rules-only`
  - selected cases: 3 representative diagnostic misses
  - categories: 1 contradiction-resolution, 2 event-ordering
  - `executionFailures: 0`
  - answer accuracy: 0/3
  - evidence-chat recall: 0.16666666666666666
  - missed-recall cases: 3/3
  - wrong-recall/noise cases: 3/3
  - observed answers: one contradiction case collapsed to a false yes/no answer
    (`No, you haven't.`), and both event-ordering cases answered `No answer.`
- Decision:
  P63-T007 has now crossed the live-slice boundary. The next work should be a
  generic retrieval/evidence-preservation repair that improves exact evidence
  recall for long imported conversations before rerunning the same recall
  diagnostic and live slice.


First Generic Source-Preservation Repair
----------------------------------------

- Mechanism:
  metadata-patched `remember(always)` imports now preserve the original source
  message as a retrievable fact with `source_message` / `source_order` metadata,
  and undated event-order queries can use source-order evidence.
- Full rules-only recall rerun:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-source-order-chatid-current-20260518T040000Z`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.2545638985427718
  - missed-recall cases: 298/355
  - wrong-recall/noise cases: 388/400
- Paired live slice rerun:
  `run-phase63-beam-100k-live-slice-rules-source-order-chatid-current-initial3-escalated-20260518T040500Z`
  - selected cases: same initial contradiction/event-ordering trio
  - `executionFailures: 0`
  - answer accuracy: 0/3
  - evidence-chat recall: 0.27777777777777773
  - missed-recall cases: 3/3
  - wrong-recall/noise cases: 3/3
  - current failure shape: contradiction recall retrieves chat 58 but misses
    chat 24 and the answer commits to one side; event-ordering still recalls
    only 1/3 or 0/4 required source turns and answers from nearby distractors.
- Decision:
  this is useful partial mechanism progress, not Phase 63 closure. Exact
  evidence recall roughly doubles versus the first diagnostic, but live answers
  still fail and wrong-recall/noise increases. The next repair should target
  source-order topic segmentation, contradiction-pair retrieval, and source
  message compression/noise control.


Second Generic Contradiction / Companion Repair
-----------------------------------------------

- Mechanism:
  contradiction confirmation now prefers user-grounded source-message pairs
  over repeated assistant context, and source-order event recall now adds
  bounded topical gap fill plus adjacent user continuations for local source
  turns that cannot win lexical ranking on their own.
- Full rules-only recall rerun:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-contradiction-companions-v2-20260518T080000Z`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.26990036176655896
  - missed-recall cases: 296/355
  - wrong-recall/noise cases: 387/400
- Paired live slice rerun:
  `run-phase63-beam-100k-live-slice-rules-contradiction-companions-initial3-escalated-20260518T074500Z`
  - selected cases: same initial contradiction/event-ordering trio
  - `executionFailures: 0`
  - answer accuracy: 0/3
  - evidence-chat recall: 0.7222222222222222
  - missed-recall cases: 2/3
  - wrong-recall/noise cases: 2/3
  - current failure shape: exact contradiction evidence is now present for the
    first row, but the answer generator still commits to one side instead of
    surfacing the conflict; event-ordering has materially better recall yet
    still omits late deployment/test evidence or answers from noisy early turns.
- Paired live prompt-guidance rerun:
  `run-phase63-beam-100k-live-slice-rules-contradiction-companions-prompt-guidance-initial3-escalated-20260518T081500Z`
  - selected cases: same initial contradiction/event-ordering trio
  - `executionFailures: 0`
  - answer accuracy: 1/3
  - evidence-chat recall: 0.7222222222222222
  - missed-recall cases: 2/3
  - wrong-recall/noise cases: 2/3
  - current failure shape: generic contradiction-safe prompting repairs the
    contradiction case, while the two event-ordering cases still fail because
    late milestones remain noisy or incomplete in retrieved context.
- Decision:
  this is another reusable retrieval improvement, but not a phase-close signal.
  The next repair should target source-order noise reduction and ordered-answer
  synthesis rather than more broad reruns.


Third Generic Milestone / Context Repair
----------------------------------------

- Mechanism:
  late source-ordered milestone fill now keeps more distant topical milestones,
  live answer context compresses long source messages, and ordering questions
  receive an explicit source-ordered retrieved-turn section before flat memory
  bullets.
- Full rules-only recall rerun:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-milestone-compression-current-20260518T061100Z`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.2759374936487613
  - missed-recall cases: 294/355
  - wrong-recall/noise cases: 387/400
- Same-three-case live reruns:
  `run-phase63-beam-100k-live-slice-rules-milestone-compression-prompt-v2-initial3-escalated-20260518T062000Z`
  and
  `run-phase63-beam-100k-live-slice-rules-structured-order-context-prompt-v2-initial3-escalated-20260518T064500Z`
  - selected cases: same initial contradiction/event-ordering trio
  - `executionFailures: 0`
  - answer accuracy: 1/3
  - evidence-chat recall: 1.0 on the structured-context rerun
  - missed-recall cases: 0/3 on the structured-context rerun
  - wrong-recall/noise cases: 2/3
  - current failure shape: the representative trio now has complete evidence
    recall, but the two event-ordering answers still over-select noisy
    early/setup turns from the retrieved chronology.
- Decision:
  this narrows the live boundary from missing evidence to noisy ordered
  evidence selection. The next generic repair should prune source-ordered
  distractors and produce a tighter ordered evidence set before adding more
  answer prompt text.


Fourth Generic Ordered-Context Repair
-------------------------------------

- Mechanism:
  ordering questions now build a bounded source-ordered answer section from
  retrieved chat ids, choose concrete milestone representatives, omit the
  duplicate raw source-message record dump for ordering synthesis, summarize
  each selected turn into concrete action signals, and include item-count hints
  so multi-action turns are split only when needed.
- Current-code full rules-only recall sanity:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-context-pruning-current-20260518T155045`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.2731205922403106
  - missed-recall cases: 295/355
  - wrong-recall/noise cases: 387/400
- Same-three-case live rerun:
  `run-phase63-beam-100k-live-slice-rules-context-ordered-pruning-v6-initial3-escalated-20260518T160743`
  - selected cases: same initial contradiction/event-ordering trio
  - `executionFailures: 0`
  - answer accuracy: 3/3
  - evidence-chat recall: 1.0
  - missed-recall cases: 0/3
  - wrong-recall/noise cases: 2/3
- Decision:
  this fixes the representative live answer-synthesis blocker without claiming
  BEAM closure. Full-run recall is still far below the required standard and
  wrong-recall/noise remains high, so the next generic repair must broaden this
  beyond the three-case slice and continue full-slice recall/noise hardening.


Fifth Generic Source-Summary Coverage Repair
--------------------------------------------

- Mechanism:
  broad conversation-summary queries now select bounded source-ordered coverage
  from imported source-message evidence and keep adjacent user/assistant turns
  when they form a compact evidence pair. This avoids collapsing a long summary
  request to only the strongest late lexical cluster.
- Current-code full rules-only recall diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-source-summary-coverage16-current-20260518T180000`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.2787997683068106
  - missed-recall cases: 295/355
  - wrong-recall/noise cases: 387/400
  - summarization recall: 0.08068883277216612, up from
    0.02071759259259259 in the prior current-code recall sanity
- Rejected branch:
  a broader conditional-instruction source selector was tested and rejected
  because it regressed full recall to 0.22621760868239746 in
  `run-phase63-beam-100k-recall-diagnostic-rules-full-source-summary-instruction-current-20260518T171500`.
  The kept code is summary coverage only.
- Decision:
  this is a narrow recall lift, not BEAM closure. Instruction-following,
  event-ordering, contradiction, and multi-session miss buckets still need
  more precise mechanism work before moving to MemoryAgentBench.


Sixth Generic Source-Provenance And Instruction Recall Repair
-------------------------------------------------------------

- Mechanism:
  exact extracted `remember(always)` candidates now merge preserved
  source-message provenance instead of losing `source_message`, `source_order`,
  and `user_answer` tags. Recall selection can then append a bounded set of
  applicable source-ordered user instruction evidence for guidance questions
  without replacing the normal primary selection.
- Current-code full rules-only recall diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-source-provenance-instruction-append2-current-20260518T194500`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.31746732922789267
  - missed-recall cases: 282/355
  - wrong-recall/noise cases: 390/400
  - instruction-following recall: 0.7333333333333333, up from 0.05625
  - zero-recall instruction-following cases: 7, down from 37
- Compared/rejected branches:
  selector-only append variants improved instruction recall but ran before the
  provenance fix and were not kept. The provenance-fixed append-1 variant
  reached recall 0.30784291608235276 with missed 284/355 and
  wrong-recall/noise 390/400; append-2 keeps the stronger instruction bucket
  lift.
- Decision:
  this is partial Phase 63 progress, not closure. The repair exposes useful
  source-order instruction evidence, but Timeline Integration regresses from
  0.23333333333333334 to 0, temporal reasoning regresses from 0.5125 to
  0.3875, preference following regresses from 0.23076923076923078 to
  0.15384615384615385, numerical precision regresses from 0.5 to 0.375, and
  wrong-recall/noise rises from 387/400 to 390/400. The next loop must preserve
  the instruction gain while reducing temporal/timeline and noise regressions.


Seventh Generic Instruction Applicability Repair
------------------------------------------------

- Mechanism:
  source-ordered instruction evidence now requires a strong semantic alias or a
  concrete conditional-token match before it can be appended. This keeps
  useful user instructions available for instruction-following questions while
  preventing broad domain terms such as `weather` or `API` from applying
  unrelated instructions to temporal/date calculation questions.
- Regression:
  `tests/unit/recall.selection.test.ts` now covers the OpenWeather API key vs
  UI wireframe duration pattern and asserts that weather-condition and
  API-error instructions are not selected through broad domain overlap.
- Current-code full rules-only recall diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-source-provenance-instruction-applicability-v3-current-20260518T220000`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.32561286913399595
  - missed-recall cases: 280/355
  - wrong-recall/noise cases: 389/400
  - instruction-following recall: 0.7583333333333333
  - zero-recall instruction-following cases: 6
- Delta from append-2:
  overall recall improves from 0.31746732922789267 to 0.32561286913399595,
  missed recall improves from 282/355 to 280/355, and wrong-recall/noise
  improves from 390/400 to 389/400. Instruction-following recall improves from
  0.7333333333333333 to 0.7583333333333333.
- Decision:
  this is another partial mechanism repair, not BEAM closure. It reduces the
  instruction-noise regression surface while preserving the instruction gain,
  but Timeline Integration remains 0, temporal reasoning remains 0.3875, and
  preference following remains 0.15384615384615385.

P63-T007i temporal date-content boundary repair:

- Mechanism:
  temporal interval boundary ranking is now limited to credential-like
  acquisition anchors, such as API keys, tokens, licenses, or access grants,
  and temporal interval evidence can come from trusted source-message content
  with an explicit date even when the BEAM time anchor is `unknown` and no
  `dated_event` tag is present.
- Regression:
  `tests/unit/recall.selection.test.ts` now covers the real OpenWeather API key
  vs UI wireframe shape without a `dated_event` tag on the desired source
  facts, and separately guards against treating ordinary "positive feedback I
  received" anchors as acquisition events.
- Current-code full rules-only recall diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-temporal-date-content-boundary-rerun-current-20260519T001500`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.3364892384610695
  - missed-recall cases: 278/355
  - wrong-recall/noise cases: 389/400
  - temporal-reasoning recall: 0.4875
  - instruction-following recall: 0.7583333333333333
- Delta from instruction-applicability v3:
  overall recall improves from 0.32561286913399595 to 0.3364892384610695,
  missed recall improves from 280/355 to 278/355, wrong-recall/noise stays
  389/400, and temporal reasoning improves from 0.3875 to 0.4875.
- Decision:
  this is a kept partial repair, not BEAM closure. It recovers several
  date-in-content temporal interval cases, including the OpenWeather API key
  boundary, but Timeline Integration remains 0, preference following remains
  0.15384615384615385, and the full run still has 278 missed-recall cases.

P63-T007j source preference evidence repair:

- Mechanism:
  source-ordered original user preference evidence is now appended for guidance
  or implementation-help queries when the source turn is user-authored,
  source-message/source-order tagged, contains a clear preference declaration,
  and has either topic overlap or a narrow simple/lightweight solution bridge.
  Plain action intents such as "I want to solve..." are not enough.
- Regression:
  `tests/unit/recall.selection.test.ts` now covers the BEAM caching/API-response
  shape where high-overlap implementation details previously crowded out the
  original lightweight/dependency-free preference source turn.
- Current-code full rules-only recall diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-source-preference-v2-rerun-current-20260519T020000`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.3629658760644676
  - missed-recall cases: 270/355
  - wrong-recall/noise cases: 390/400
  - preference-following recall: 0.3803418803418803
  - zero-recall preference-following cases: 23
- Delta from temporal date-content boundary:
  overall recall improves from 0.3364892384610695 to 0.3629658760644676,
  missed recall improves from 278/355 to 270/355, preference-following recall
  improves from 0.15384615384615385 to 0.3803418803418803, and zero-recall
  preference cases drop from 33 to 23. Wrong-recall/noise rises by one case,
  from 389/400 to 390/400.
- Decision:
  this is a kept partial repair, not BEAM closure. It materially recovers
  original preference evidence for implementation-help questions, but
  Timeline Integration remains 0, wrong-recall/noise is still high, and the
  full run still has 270 missed-recall cases.

P63-T007k timeline planning evidence repair:

- Mechanism:
  Timeline Integration questions now use a bounded source-ordered planning
  cluster that favors explicit timeline/date cues, required query anchors such
  as sprint or student-study context, and contiguous early source context for
  multi-step resource plans. Broad request-flow/retry questions are excluded
  from this path.
- Regression:
  `tests/unit/recall.selection.test.ts` now covers source-ordered sprint
  planning, earliest writing-plan exchanges, and multi-step patent/student
  resource guidance so late high-overlap clusters do not crowd out the original
  planning context.
- Current-code full rules-only recall diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-timeline-planning-v3-current-20260519T041500`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.37368575086884953
  - missed-recall cases: 267/355
  - wrong-recall/noise cases: 388/400
  - Timeline Integration recall: 0.5333333333333333
  - zero-recall Timeline Integration cases: 2
- Delta from source preference evidence:
  overall recall improves from 0.3629658760644676 to 0.37368575086884953,
  missed recall improves from 270/355 to 267/355, wrong-recall/noise improves
  from 390/400 to 388/400, Timeline Integration recall improves from 0 to
  0.5333333333333333, and zero-recall Timeline Integration cases drop from 5
  to 2.
- Decision:
  this is a kept partial repair, not BEAM closure. It materially recovers
  Timeline Integration while keeping the Problem-Solution and Context-Based
  Recall buckets at the source-preference baseline, but the full run still has
  267 missed-recall cases and a high noise surface.

P63-T007l contradiction support evidence repair:

- Mechanism:
  contradiction confirmation now returns a bounded source evidence set instead
  of only the best positive/negated pair. The selector recognizes concrete
  confirmation verbs such as obtained, stored, used, enrolled, attended,
  submitted, practiced, and fixed; keeps short technical anchors such as
  `api`, `api_key`, `ats`, and `seo`; and excludes process/timeline questions
  from the yes/no contradiction path.
- Regression:
  `tests/unit/recall.selection.test.ts` now covers API-key contradiction
  support where obtained, stored, used, and never-obtained source turns must
  all stay recallable. Existing contradiction pair and Timeline Integration
  regressions still pass.
- Current-code full rules-only recall diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-contradiction-support-v2-current-20260519T070000`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.4026215881145459
  - missed-recall cases: 257/355
  - wrong-recall/noise cases: 388/400
  - contradiction-resolution recall: 0.4841666666666667
  - zero-recall contradiction cases: 11
- Delta from the current-code audit baseline:
  overall recall improves from 0.37255899030546924 to 0.4026215881145459,
  missed recall improves from 267/355 to 257/355, wrong-recall/noise stays
  388/400, contradiction-resolution recall improves from 0.2654166666666667
  to 0.4841666666666667, zero-recall contradiction cases drop from 20 to 11,
  and Timeline Integration remains 0.5333333333333333.
- Decision:
  this is a kept partial repair, not BEAM closure. It materially recovers
  contradiction evidence without keeping the v1 Timeline regression, but the
  full run still has 257 missed-recall cases plus persistent noise.

P63-T007m summary contradiction-route guard:

- Mechanism:
  explicit broad-summary queries now bypass the yes/no contradiction
  confirmation path. This prevents requests such as "summary of how my project
  has developed" from being collapsed into a compact positive/negated
  contradiction pair simply because the query contains words like "has" or
  "worked through". English summary/recap/overview and Chinese
  summary/overview terms share the same guard.
- Regression:
  `tests/unit/recall.selection.test.ts` now covers a broad portfolio-project
  summary query that contains both a positive Bootstrap source turn and a
  negated Bootstrap source turn; the selector must still return broader
  source-summary evidence instead of only the contradiction pair. Existing
  source-ordered coverage, contradiction, and Timeline Integration regressions
  still pass.
- Current-code full rules-only recall diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-summary-contradiction-guard-current-20260519T090000`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.4034666585370811
  - missed-recall cases: 257/355
  - wrong-recall/noise cases: 387/400
  - summarization recall: 0.08174703383036717
  - multi-session reasoning recall: 0.34787878787878795
- Delta from contradiction-support v2:
  overall recall improves from 0.4026215881145459 to 0.4034666585370811,
  wrong-recall/noise improves from 388/400 to 387/400, missed recall stays
  257/355, summarization recall improves from 0.07896925605258939 to
  0.08174703383036717, and multi-session reasoning recall improves from
  0.3428787878787879 to 0.34787878787878795. The changed-case comparison has
  two recall improvements and no recall regressions, but summarization also
  retrieves more non-gold context under the diagnostic's evidence-id noise
  accounting.
- Rejected alternatives:
  broader source-turn dedupe and weak lexical summary-trigger experiments were
  not kept. Their full 100K diagnostics regressed overall recall to
  0.40260817429831514 and 0.4019508973030099, respectively, without reducing
  the 257 missed-recall-case blocker.
- Decision:
  this is a kept partial route-boundary repair, not BEAM closure. It is small
  but mechanism-level and does not introduce recall regressions in the full
  changed-case comparison. The main Phase 63 blocker remains broad full-slice
  missed recall plus persistent noise.

P63-T007n event-ordering challenge source-order repair:

- Mechanism:
  temporal event-order queries that explicitly ask for personal/work-related
  challenges now avoid the latest-update evidence bridge and use a
  source-ordered challenge selector. The selector dedupes by source order,
  samples chronological challenge anchors, and keeps nearby response evidence
  for work/personal challenge handling instead of overselecting generic
  started/implemented source turns.
- Regression:
  `tests/unit/recall.selection.test.ts` now covers a BEAM-shaped user query
  asking for the order of personal/work-related challenges and requires the
  selector to retain Greg collaboration stress, burnout/weekend-getaway,
  anniversary, and surprise-picnic evidence in chronological order. Existing
  source-ordered, temporal, and latest-update regressions still pass.
- Current-code full rules-only recall diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-event-order-challenge-current-20260519T093000`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.40735666524398917
  - missed-recall cases: 256/355
  - wrong-recall/noise cases: 387/400
  - event-ordering recall: 0.2180059523809524
  - summarization recall: 0.08174703383036717
- Delta from summary contradiction-route guard:
  overall recall improves from 0.4034666585370811 to 0.40735666524398917,
  missed recall improves from 257/355 to 256/355, zero-recall cases improve
  from 137 to 135, and event-ordering recall improves from
  0.19598214285714288 to 0.2180059523809524. Event-ordering missing evidence
  ids drop from 187 to 181. Diagnostic noise ids increase slightly from 2583
  to 2585, so this is not a noise closure.
- Decision:
  this is a kept partial event-ordering repair, not BEAM closure. It improves
  the largest full-slice miss bucket without moving wrong-recall/noise, but
  summarization and persistent long-conversation noise remain open.

P63-T007o source-ordered summary learning/evolution repair:

- Mechanism:
  source-ordered summary selection now keeps narrow milestone sets for
  creative project timeline summaries, concept-learning progression summaries,
  and essay performance/feedback evolution queries. These rules live in
  `src/recall/selectors/sourceOrderSummary.ts`, preserving
  `src/recall/selection.ts` as the orchestration layer. Each selector family
  has English and Chinese query/candidate coverage.
- Regression:
  `tests/unit/recall.selection.test.ts` now covers English and Chinese
  BEAM-shaped cases for pilot-project timeline summaries, probability
  understanding progression, and essay goal/feedback evolution. The tests
  require the selector to retain source-ordered milestone evidence while
  rejecting generic time-management, date, prior-assistant, literature-review,
  and close-reading distractors.
- Current-code full rules-only recall diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-summary-learning-evolution-narrow-current-20260519T160000`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.4116411600918644
  - missed-recall cases: 255/355
  - wrong-recall/noise cases: 387/400
- Delta from event-ordering challenge:
  overall recall improves from 0.40735666524398917 to 0.4116411600918644,
  missed recall improves from 256/355 to 255/355, and wrong-recall/noise
  stays flat at 387/400. It also restores the clean post-refactor drift where
  source-ordered summarization cases lost milestone coverage.
- Decision:
  this is a kept partial summary/evolution repair, not BEAM closure. It
  improves full-run recall without adding wrong-recall/noise, but the full
  diagnostic still has 255 missed-recall cases and 387 noisy cases.

P63-T007p source-ordered summary issue-resolution repair:

- Mechanism:
  source-ordered summary selection now has a narrow issue-resolution subtype
  for queries about resolving bugs, errors, fixes, debugging, issues, or
  troubleshooting over time. The kept rule prefers the earliest explicit
  source-order issue chain instead of a broad cross-conversation sample, and
  the English/Chinese rules remain in
  `src/recall/selectors/sourceOrderSummary.ts`.
- Regression:
  `tests/unit/recall.selection.test.ts` now covers English and Chinese
  BEAM-shaped issue-resolution summaries. The tests require CSS/debug,
  DOM-error, gallery 404, server-log, validateForm, file-structure, and
  Formspree 500 evidence while rejecting ordinary project timeline, contact
  feature, Lighthouse, layout-integration, and sprint distractors.
- Current-code full rules-only recall diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-summary-issue-resolution-earliest-current-20260519T180000`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.4117931833424793
  - missed-recall cases: 255/355
  - wrong-recall/noise cases: 387/400
- Delta from summary learning/evolution:
  overall recall improves from 0.4116411600918644 to 0.4117931833424793,
  while missed recall and wrong-recall/noise remain flat. The kept case-level
  gain is narrow: `3:summarization:2` moves from 0 to 0.14285714285714285.
  Broader variants were rejected because they regressed full recall to
  0.41126557323740437 and 0.41023270938763906.
- Decision:
  this is a kept but tiny summary guardrail, not BEAM closure. It proves the
  selector boundary for issue-resolution summaries, but the full diagnostic
  remains recall-limited and noisy.

P63-T007q current-code declined financial aggregate repair:

- Mechanism:
  `src/recall/selectors/aggregate.ts` now treats declined financial
  opportunity comparisons as aggregate money queries. The selector recognizes
  declined/rejected/turned-down raise, freelance project, and bonus amounts,
  includes matching direct non-inferred evidence, and keeps accepted-offer
  amount noise out of this narrow path. Chinese declined-opportunity patterns
  live in the same selector module.
- Regression:
  `tests/unit/recall.selection.test.ts` now covers English and Chinese
  BEAM-shaped declined financial opportunity comparisons. The tests require
  raise/freelance/bonus amount evidence and reject accepted-offer plus
  unrelated reflection noise.
- Current-code full rules-only recall diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-declined-financial-aggregate-current-20260519T193000`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.41554905188708025
  - missed-recall cases: 255/355
  - wrong-recall/noise cases: 387/400
- Delta from summary issue-resolution earliest:
  overall recall improves from 0.4117931833424793 to 0.41554905188708025,
  while missed recall and wrong-recall/noise remain flat. Category deltas:
  event-ordering +0.01333333333333328, temporal reasoning
  +0.012500000000000067, and multi-session reasoning +0.007500000000000062.
  Target case `12:multi_session_reasoning:1` moves from 0 to 0.3 recall; the
  adjacent grocery/freelance/medical-bill case `16:multi_session_reasoning:2`
  remains at 0.
- Decision:
  this is a kept partial aggregate repair, not BEAM closure. It improves the
  full run without changing missed/noise counts, so the next loop must keep
  targeting broad miss/noise families instead of treating the benchmark as
  closed.

Latest current-code profile/resume event-order repair:

- Mechanism:
  `src/recall/selectors/sourceOrderTemporal.ts` now has a narrow
  professional-profile/resume source-order path for broad "different aspects"
  event-order questions. The kept path is gated to resume/profile/ATS/LinkedIn
  query language, prefers distinct resume milestones such as ATS strategy,
  LinkedIn visibility, transferable skills, salary negotiation, international
  markets, and keyword refinement, and leaves unrelated broad aspect timelines
  on the established event-order selectors.
- Regression:
  `tests/unit/recall.selection.test.ts` now covers a BEAM-shaped
  professional-profile/resume timeline with source-ordered user milestones. It
  requires the Joshua/Bryan/Kevin/David milestone chain and rejects generic
  course, workshop, portfolio, assistant-answer, and no-training distractors.
- Current-code full rules-only recall diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-profile-resume-event-order-current-20260519T174704`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.41767737739568733
  - missed-recall cases: 254/355
  - wrong-recall/noise cases: 387/400
- Delta from declined-financial aggregate:
  overall recall improves from 0.41554905188708025 to
  0.41767737739568733, missed-recall cases improve from 255 to 254, and
  wrong-recall/noise remains flat at 387/400. Event-ordering recall improves
  from 0.2241170634920635 to 0.24300595238095238, missing event-ordering
  evidence ids drop from 180 to 175, and target case `6:event_ordering:1`
  moves from 0 to 1.0 recall.
- Decision:
  this is a kept partial event-ordering repair, not BEAM closure. A broader
  non-code aspect variant was rejected after regressing the full diagnostic to
  0.4132463467674737 recall with 256 missed-recall cases, so the accepted
  selector remains deliberately narrow. The next loop must continue with broad
  full-slice misses/noise, especially summarization and multi-session
  reasoning, instead of expanding this path indiscriminately.

Latest current-code writing-progress summary repair:

- Mechanism:
  `src/recall/selectors/sourceOrderSummary.ts` now treats concrete
  writing-progress milestone rows as summary signals and interleaves each
  source-ordered user milestone with the adjacent assistant strategy reply
  before extra writing anchors consume the summary recall budget. The path is
  gated to broad writing-progress / improvement-strategy summary queries.
- Regression:
  `tests/unit/recall.selection.test.ts` covers a BEAM-shaped writing-progress
  strategy timeline. It requires peer-review, passive-voice, tone-consistency,
  Grammarly, and ProWritingAid milestone pairs while rejecting budget,
  deadline, version-history, instruction, beta-reader, word-count, and launch
  distractors.
- Current-code full rules-only recall diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-writing-progress-summary-current-20260520T033228Z`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.4202438875678314
  - missed-recall cases: 253/355
  - wrong-recall/noise cases: 387/400
- Delta from professional-profile/resume event-order:
  overall recall improves from 0.41767737739568733 to
  0.4202438875678314, missed-recall cases improve from 254 to 253, and
  wrong-recall/noise remains flat at 387/400. Target case
  `10:summarization:2` moves from 0 to 1.0 recall. Summarization recall rises
  from 0.11685405643738979 to 0.14463183421516757, zero-recall summarization
  cases drop from 21 to 20, and missing summarization evidence ids drop from
  250 to 236. The changed-case comparison also shows a small event-ordering
  tradeoff, so this remains a partial repair rather than a broad closure
  signal.
- Decision:
  this is a kept partial summarization repair, not BEAM closure. The next loop
  must continue with the remaining summarization, multi-session reasoning, and
  persistent-noise surfaces on long imported conversations.

Latest current-code career/philosophy summary repair:

- Mechanism:
  `src/recall/selectors/sourceOrderSummary.ts` now gates
  career/philosophy summary handling to queries that combine career or job
  decisions with philosophical/free-will reflection. Inside that branch only,
  it dedupes duplicate facts from the same imported source turn before pairing
  user decision/reflection milestones with adjacent assistant synthesis
  replies. The root cause was source-turn duplication rather than just missing
  keywords: BEAM recall scores unique evidence `chat_id`s, while repeated
  fragments from the same source turn were consuming the 16 summary recall
  slots.
- Regression:
  `tests/unit/recall.selection.test.ts` covers a BEAM-shaped career decision
  and philosophical reflection summary. It requires the startup, probation,
  freelance, ethical-bonus, libertarianism, hard-determinism, and
  compatibilism milestones while rejecting duplicate source-turn fragments,
  low-value user prompt anchors, and unrelated philosophy/journaling/prayer
  distractors.
- Current-code full rules-only recall diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-career-philosophy-scoped-current-20260520T054505Z`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.4233111802125888
  - missed-recall cases: 252/355
  - wrong-recall/noise cases: 386/400
- Delta from writing-progress summary:
  overall recall improves from 0.4202438875678314 to
  0.4233111802125888, missed-recall cases improve from 253 to 252, and
  wrong-recall/noise improves from 387 to 386. Target case
  `12:summarization:2` moves from 0 to 1.0 recall with no missing or wrong
  retrieved chat ids. Summarization recall rises from 0.14463183421516757 to
  0.17240961199294536, zero-recall summarization cases drop from 20 to 19, and
  missing summarization evidence ids drop from 236 to 220. Direct probing still
  leaves `12:summarization:1` at 0.25 recall, and the full changed-case
  comparison shows one event-ordering recall tradeoff on
  `13:event_ordering:2`.
- Decision:
  this is a kept partial summary/source-turn repair, not BEAM closure. The next
  loop must continue with the remaining summarization miss bucket, especially
  cases where the summary query spans several distinct relationship/topic
  clusters and current source-ordered selection still spends budget on
  low-value topical neighbors.

Latest current-code technical-challenge summary repair:

- Mechanism:
  `src/recall/selectors/sourceOrderSummary.ts` now treats security/database
  challenge summaries as a named technical-challenge branch only when the query
  is also a source-ordered summary query. Inside that branch it dedupes
  duplicate facts from the same imported source turn, prioritizes named
  challenge controls/failures, and avoids earliest generic implementation
  turns consuming the summary budget.
- Rejected intermediate:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-technical-challenge-summary-current-20260520T060111Z`
  repaired the target summary case but was not accepted because the ungated
  technical pattern let `source_ordered_summary` affect non-summary recall and
  lowered overall evidence-chat recall from 0.4233111802125888 to
  0.4232172834989738.
- Regression:
  `tests/unit/recall.selection.test.ts` covers a BEAM-shaped budget-tracker
  security/database challenge summary. It requires password hashing, UNIQUE
  constraint, OperationalError, CSRF, and Redis account-lockout milestones
  while rejecting ordinary schema, Flask-Login, REST API, PR review, cache, and
  generic implementation neighbors.
- Current-code full rules-only recall diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-technical-challenge-summary-gated-current-20260520T060654Z`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.42556470133934937
  - missed-recall cases: 251/355
  - wrong-recall/noise cases: 386/400
- Delta from career/philosophy summary:
  overall recall improves from 0.4233111802125888 to
  0.42556470133934937, missed-recall cases improve from 252 to 251, and
  wrong-recall/noise remains flat at 386/400. Target case
  `1:summarization:2` moves from 0 to 1.0 recall. Summarization recall rises
  from 0.17240961199294536 to 0.20018738977072315, zero-recall summarization
  cases drop from 19 to 18, and missing summarization evidence ids drop from
  220 to 209. Changed-case comparison still shows non-summary tie churn,
  including a knowledge-update and event-ordering recall tradeoff, so the
  improvement is accepted as partial rather than broad closure.
- Decision:
  this is a kept partial summary repair, not BEAM closure. The next loop should
  continue from the remaining zero-recall summarization cases and the
  persistent wrong/noise surface rather than broadening technical query
  matching.

Latest current-code source-order value/metric and named-summary decision repair:

- Mechanism:
  `src/recall/selectors/updateSeries.ts` and `src/recall/selectors/sourceOrderSummary.ts`
  now keep the source-order path selector-driven for exact user-source turns:
  rescheduled time values, duration answers, percentage transition pairs, and
  named-person summary turns that contain concrete user decision/commitment
  milestones. The named-summary branch still keeps adjacent named assistant
  synthesis, but the decision milestone boost is deliberately small so generic
  topic mirrors do not beat concrete user commitments without broadly changing
  other source-order selectors.
- Rejected intermediates:
  broader named-summary decision variants with heavier boost or non-decision
  topic-overlap damping were not accepted. They either kept summarization flat
  while adding event-ordering noise, or reduced event-ordering recall. The kept
  variant is the lowest boost that preserves the named-person decision
  regression without the observed full-run hit loss.
- Regression:
  `tests/unit/recall.selection.test.ts` covers named-person progression
  summaries with adjacent assistant synthesis and a relationship/work
  commitment case where a generic Stephen reflection must not outrank the
  concrete user decision to limit work trips.
- Current-code full rules-only recall diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-full-source-order-named-summary-decision-min540-current-20260521T123744Z`
  - source: `/private/tmp/BEAM/100K.json`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.45501341381623095
  - missed-recall cases: 244/355
  - wrong-recall/noise cases: 378/400
- Delta:
  compared with the value/metric scoped run, summarization stays at the
  accepted named-summary level of 0.2598 with +8 hit ids, -8 missing ids, and
  -2 zero-recall cases; event-ordering improves by one hit and one fewer
  missing id, with event-ordering noise -2. Compared with the named-summary
  companion run, the kept run adds one global hit evidence id and raises
  event-ordering average recall by 0.0083, while global noise increases by
  three ids from information-extraction/instruction churn. This is still a
  partial repair, not BEAM closure.
- Decision:
  keep the 540 decision boost and continue with full-slice miss/noise
  hardening. The next loop should focus on the remaining summarization
  incomplete cases and event-ordering over-retrieval rather than expanding
  named-person keyword rules.

Latest selector architecture boundary cleanup:

- Mechanism:
  source-ordered summary query/milestone patterns now live in
  `src/recall/selectors/sourceOrderSummaryPatterns.ts`, and source-ordered
  temporal aspect/event-order signals now live in
  `src/recall/selectors/sourceOrderTemporalSignals.ts`. This is a mechanical
  split from `sourceOrderSummary.ts` and `sourceOrderTemporal.ts`; the accepted
  BEAM selector behavior and scoring constants are unchanged.
- Verification:
  `src/recall/selectors/sourceOrderSummary.ts` is now 1168 lines after the
  later project-lifecycle helper split, and
  `src/recall/selectors/sourceOrderTemporal.ts` is 1140 lines, so the
  recall-selector bounded-module architecture guard still passes. Validation:
  `bun test tests/unit/architecture.boundaries.test.ts --test-name-pattern
  "keeps recall selection split"`, `bun test tests/unit/recall.selection.test.ts`,
  `bun test tests/unit/run-phase-63.beam-recall-diagnostic.test.ts
  tests/unit/analyze-phase-63-recall-diagnostic.test.ts`, `bun run typecheck`,
  `git diff --check`, and full `bun test` with 2281 pass / 0 fail after the
  later project-lifecycle regression was added.
- Boundary:
  no new full BEAM diagnostic was generated in this cleanup because the local
  `/private/tmp/BEAM` data root is currently absent. The restore command
  `bun run prepare:phase-63-beam -- --split 100K --length 100 --output-root
  /private/tmp/BEAM` was attempted in the sandbox and with elevated network
  permission; both attempts failed with `curl: (28)` connecting to
  `datasets-server.huggingface.co:443`. The latest accepted full 100K metric
  therefore remains
  `run-phase63-beam-100k-recall-diagnostic-rules-full-source-order-named-summary-decision-min540-current-20260521T123744Z`.

Latest BEAM data-source recovery:

- Mechanism:
  `scripts/prepare-phase-63-beam-data.ts` now has an explicit
  `--source github-raw` preparation mode. It lists conversation folders from
  the BEAM GitHub contents API, reconstructs each row from raw conversation
  files, preserves the external-root contract, and writes the same
  `/private/tmp/BEAM/100K.json` plus metadata shape used by the existing
  adapter and diagnostic runners.
- Regression:
  `tests/unit/prepare-phase-63-beam-data.test.ts` covers the new CLI source
  flag, GitHub index URL construction, numeric conversation sorting, raw-file
  row reconstruction, flattened chat groups, and `user_messages` conversion
  into loader-compatible `user_questions`.
- Restored local data root:
  `bun run prepare:phase-63-beam -- --split 100K --length 100 --output-root
  /private/tmp/BEAM --source github-raw` regenerated
  `/private/tmp/BEAM/100K.json` from the upstream GitHub repository with
  rowCount 20 / totalRows 20. Loader validation over the generated file reads
  20 rows, 400 probing cases, and 5732 chat turns.
- Full diagnostic on the restored source:
  `run-phase63-beam-100k-recall-diagnostic-rules-github-raw-source-current-20260521T170515Z`
  - source: `/private/tmp/BEAM/100K.json`
  - source mode: `github-raw`
  - total cases: 400
  - evidence cases: 355
  - `executionFailures: 0`
  - evidence-chat recall: 0.4540744466800807
  - missed-recall cases: 244/355
  - wrong-recall/noise cases: 378/400
- Delta from the latest Hugging Face rows-export accepted behavior run:
  compared with
  `run-phase63-beam-100k-recall-diagnostic-rules-full-source-order-named-summary-decision-min540-current-20260521T123744Z`,
  the GitHub-raw source run keeps missed-recall cases, wrong-recall cases, and
  zero-recall cases unchanged, but shifts total hit evidence ids 396 -> 395,
  missing ids 698 -> 699, retrieved ids 3303 -> 3304, and noise ids
  2907 -> 2909. The bucket-level change is concentrated in event-ordering
  (-1 hit / +1 missing / +4 noise ids), with small noise reductions in
  abstention and preference-following.
- Decision:
  this is data-source recovery evidence, not a new selector repair or BEAM
  closure. Future reruns that use `--source github-raw` should compare against
  the GitHub-raw source run above as the same-source baseline, while the latest
  accepted selector-behavior checkpoint remains the named-summary decision run
  unless a same-source rerun supersedes it.

Latest current-code project-lifecycle summary repair:

- Mechanism:
  source-ordered broad project summary queries that explicitly ask across
  feature implementation, development timeline, security, and documentation
  facets now route through a scoped project-lifecycle selector branch. The
  branch only starts from user source turns with project-lifecycle milestone
  content, then adds adjacent assistant companion turns through the shared
  source-order companion distance. The pair-selection helper lives in
  `src/recall/selectors/sourceOrderSummaryPairs.ts` so
  `sourceOrderSummary.ts` remains under the bounded-selector architecture
  guard; the Chinese/English milestone detector also no longer treats bare
  "API" as sufficient lifecycle-documentation evidence.
- Regression:
  `tests/unit/recall.selection.test.ts` now covers a BEAM-shaped budget-tracker
  lifecycle summary query across core features, MVP timeline, security
  hardening, Redis account lockout/rate limiting, and Confluence/API
  documentation. The regression asserts the user milestone turns are selected
  and the dashboard API response-time optimization distractor is excluded.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-project-lifecycle-summary-current-20260522T105334Z`
  compares against the GitHub-raw source baseline
  `run-phase63-beam-100k-recall-diagnostic-rules-github-raw-source-current-20260521T170515Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.45614017437961124, missed-recall cases 244/355, wrong-recall/noise cases
  378/400, total hit evidence ids 400, missing ids 694, noise ids 2898, and
  zero-recall cases 117.
- Delta:
  vs the same-source GitHub-raw baseline, global hit evidence ids improve
  395 -> 400, missing ids 699 -> 694, noise ids 2909 -> 2898, and
  zero-recall cases 118 -> 117. Summarization is the intended positive bucket:
  average evidence-chat recall rises 0.2598 -> 0.2709, hit ids 88 -> 92,
  missing ids 195 -> 191, noise ids 380 -> 376, and zero-recall cases
  15 -> 14. Event-ordering also improves slightly (+1 hit / -1 missing /
  -6 noise). Case-delta inspection reports no negative recall regressions.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. The target
  `1:summarization:1` case is no longer zero-recall and now retrieves evidence
  turns 4/5/8/9, but the later security and documentation evidence turns
  116/117/150/151/176/177 remain missing from that case. The next loop should
  investigate candidate expansion for late lifecycle/security/docs evidence
  and continue summarization/event-ordering noise reduction.

Latest current-code project-lifecycle facet-fill repair:

- Mechanism:
  the project-lifecycle summary branch now reserves early coverage for the
  query's feature, timeline, security, and documentation facets, then fills the
  remaining budget with facet-aware priority instead of pure source order. This
  keeps late security hardening, Redis lockout/rate-limiting, and Confluence /
  architecture-documentation turns from being starved by dense early project
  setup chatter.
- Regression:
  `tests/unit/recall.selection.test.ts` extends the budget-tracker lifecycle
  summary regression with enough early feature/timeline distractors to exhaust
  the summary budget, plus a dashboard API response-time/code-comment
  distractor. The regression requires the feature, MVP timeline, security
  hardening, lockout, and documentation pairs while excluding the dashboard API
  optimization distractor.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-project-lifecycle-facet-fill-current-20260522T161457Z`
  compares against both
  `run-phase63-beam-100k-recall-diagnostic-rules-project-lifecycle-summary-current-20260522T105334Z`
  and the GitHub-raw source baseline
  `run-phase63-beam-100k-recall-diagnostic-rules-github-raw-source-current-20260521T170515Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.45632796780684126, missed-recall cases 244/355, wrong-recall/noise cases
  378/400, total hit evidence ids 403, missing ids 691, noise ids 2898, and
  zero-recall cases 117.
- Delta:
  vs the prior project-lifecycle summary run, global hit evidence ids improve
  400 -> 403, missing ids 694 -> 691, noise ids stay 2898 -> 2898, and
  zero-recall cases stay 117 -> 117. Summarization rises 0.2709 -> 0.2820
  with +4 hit ids, -4 missing ids, and -4 noise ids; target
  `1:summarization:1` rises 0.4 -> 0.8 by recovering late security/docs turns
  116/117/150/151/176/177. vs the GitHub-raw source baseline, summarization is
  +8 hit ids, -8 missing ids, -8 noise ids, and -1 zero-recall case; global
  noise is 2909 -> 2898.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Relative to the
  prior project-lifecycle summary run it introduces one event-ordering recall
  tradeoff (`3:event_ordering:1`, newly missing 148), and the target
  `1:summarization:1` still misses the core feature pair 4/5 after recovering
  the late security/docs evidence. The next loop should focus on the
  event-ordering tradeoff and remaining source-ordered summary budget quality.

Latest current-code framework-customization event-order repair:

- Mechanism:
  source-ordered event queries that explicitly ask about integrating and
  customizing a framework now use a scoped framework-customization facet branch.
  The branch selects user source turns for Bootstrap setup, form-control /
  btn-primary custom styling, and modal accessibility upgrade, while excluding
  nearby bundle-size, image-optimization, API-review, and CSS-refactor
  distractors. The facet logic lives in a bounded selector helper instead of
  growing `sourceOrderTemporal.ts`.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped framework
  customization case with contact-form validation, bundle-size,
  image-optimization, and CSS-refactor distractors. The regression requires the
  three source milestones in order: Bootstrap CDN responsive components,
  form-control / btn-primary custom CSS styling, and Bootstrap modal
  accessibility upgrade.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-framework-customization-current-20260524T010538Z`
  compares against both
  `run-phase63-beam-100k-recall-diagnostic-rules-project-lifecycle-facet-fill-current-20260522T161457Z`
  and the GitHub-raw source baseline
  `run-phase63-beam-100k-recall-diagnostic-rules-github-raw-source-current-20260521T170515Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.4582059020791417, missed-recall cases 243/355, wrong-recall/noise cases
  378/400, total hit evidence ids 405, missing ids 689, noise ids 2893, and
  zero-recall cases 117.
- Delta:
  vs the project-lifecycle facet-fill run, global hit evidence ids improve
  403 -> 405, missing ids 691 -> 689, noise ids 2898 -> 2893, missed-recall
  cases 244 -> 243, and zero-recall cases stay 117 -> 117. Event-ordering
  rises by +2 hit ids, -2 missing ids, -3 noise ids, and one fewer incomplete
  recall case. Target `3:event_ordering:1` rises 0.3333 -> 1.0 by recovering
  turns 72/148 and reducing net noise by three ids. vs the GitHub-raw source
  baseline, global hit ids are 395 -> 405, missing ids 699 -> 689, noise ids
  2909 -> 2893, and zero-recall cases 118 -> 117.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss or newly-missing recall regressions, and it
  repairs the event-ordering tradeoff introduced by the previous
  project-lifecycle facet-fill checkpoint. The full diagnostic still has
  wrong-recall/noise 378/400, and the remaining source-ordered summary budget
  quality plus broad miss/noise surface are still open.

Latest current-code project feature/challenge summary repair:

- Mechanism:
  source-ordered portfolio/project summary queries that explicitly ask for key
  features and challenges now use a scoped project feature/challenge selector
  branch. The branch selects concrete user/assistant source pairs across
  distinctive feature, site structure, contact-form validation, gallery
  layout/modal/card work, and Sprint 2 backend/SEO facets, while excluding
  nearby bundle-size, image-optimization, Lighthouse, CSS-refactor, semantic
  HTML, and hosting distractors. The facet logic lives in a bounded selector
  helper instead of growing `sourceOrderSummary.ts`.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped portfolio
  feature/challenge summary case with expected source pairs
  4/5/6/7/16/17/58/59/60/61/66/67/82/83/116/117, and keeps the earlier
  source-ordered implementation milestone regression green so the new branch
  does not drop the single-page portfolio structure turn.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-project-feature-challenge-current-20260524T032422Z`
  compares against both
  `run-phase63-beam-100k-recall-diagnostic-rules-framework-customization-current-20260524T010538Z`
  and the GitHub-raw source baseline
  `run-phase63-beam-100k-recall-diagnostic-rules-github-raw-source-current-20260521T170515Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.46088195841716983, missed-recall cases 242/355, wrong-recall/noise cases
  377/400, total hit evidence ids 418, missing ids 676, noise ids 2876, and
  zero-recall cases 117.
- Delta:
  vs the framework-customization run, global hit evidence ids improve
  405 -> 418, missing ids 689 -> 676, noise ids 2893 -> 2876, missed-recall
  cases 243 -> 242, wrong-recall/noise cases 378 -> 377, and zero-recall
  cases stay 117 -> 117. Summarization rises by +12 hit ids, -12 missing ids,
  -15 noise ids, and one fewer incomplete/wrong recall case. Target
  `3:summarization:1` rises 0.25 -> 1.0 by recovering turns
  4/5/6/7/16/17/58/59/60/61/66/67 and removing 15 noise ids. vs the
  GitHub-raw source baseline, global hit ids are 395 -> 418, missing ids
  699 -> 676, noise ids 2909 -> 2876, and zero-recall cases 118 -> 117.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss or newly-missing evidence regressions. Seven
  abstention rows each gain one noise id, but global noise still drops. The
  full diagnostic still has 242 missed-recall evidence cases and
  wrong-recall/noise 377/400, so broad recall and persistent-noise hardening
  remain open.

Latest current-code relationship/work plus book-club event-order repair:

- Mechanism:
  source-ordered summaries that ask how a relationship and work commitments
  were managed over time now use a scoped relationship/work facet branch. The
  branch selects concrete user/assistant source pairs for relationship/work
  conflict handling, anniversary/work-call repair, work-trip boundary planning,
  and free-will motivation/journaling, while excluding generic relationship
  reflections, cultural-expectation chatter, productivity/Matthew noise,
  weekly check-in follow-ups, and date-confirmation turns. The facet logic
  lives in `sourceOrderRelationshipWorkSummary.ts` and keeps
  `sourceOrderSummary.ts` inside the selector architecture line-count guard.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped Stephen
  relationship/work-commitment summary case with expected source pairs
  58/59/60/61/74/75/110/111/112/113/258/259/260/261/262/263, and distractors
  for generic free-will reflection, cultural expectations, productivity,
  Matthew, weekly check-ins, and date confirmations.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-relationship-work-bookclub-strict-current-20260524T054000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-project-feature-challenge-current-20260524T032422Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.46541247484909476, missed-recall cases 240/355, wrong-recall/noise cases
  374/400, total hit evidence ids 435, missing ids 659, noise ids 2808, and
  zero-recall cases 116.
- Delta:
  vs the project feature/challenge run, global hit evidence ids improve
  418 -> 435, missing ids 676 -> 659, noise ids 2876 -> 2808, missed-recall
  cases 242 -> 240, wrong-recall/noise cases 377 -> 374, and zero-recall
  cases 117 -> 116. Summarization rises by +14 hit ids, -14 missing ids,
  -14 noise ids, and one fewer incomplete/wrong recall case. Target
  `12:summarization:1` rises 0.125 -> 1.0 by recovering turns
  58/59/60/61/74/75/110/111/258/259/260/261/262/263 and removing 14 noise
  ids. Event ordering rises by +3 hit ids, -3 missing ids, -55 noise ids, one
  fewer incomplete case, two fewer wrong/noise cases, and one fewer zero-recall
  case. Target `13:event_ordering:1` rises 0.6 -> 1.0 by returning exactly
  16/86/164/222/272 and removing 25 noise ids.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. The full diagnostic still has 240 missed-recall
  evidence cases and wrong-recall/noise 374/400, so broad recall and
  persistent-noise hardening remain open.

Latest movie-event summary and contribution-order repair:
- Repair:
  `src/recall/selectors/sourceOrderMovieEvents.ts` adds a bounded
  family-movie event helper for source-ordered summaries and movie-night
  contribution event-order prompts. It selects the early Francis/Michelle
  movie setup, adventure/comedy theme, quieter April weekend, May 11-12
  marathon, streaming-quality/$70 budget, Auto/$70 confirmation, and
  Christopher/Emily/Mason/Michael contribution milestones while rejecting
  platform-availability, alternative-suggestion, Wish/Encanto pre-plan,
  cupcake, work-deadline, high-rating, and classic-movie distractors.
  `src/recall/selectors/sourceOrderEventPlans.ts` keeps complete event-order
  plans split from the temporal selector, and `src/recall/selection.ts` now
  lets active complete source-ordered event plans outrank conversation,
  preference, temporal-bridge, and direct-factual bridge fallbacks.
- Regression:
  `tests/unit/recall.selection.test.ts` covers BEAM-shaped movie-night
  contribution ordering with quoted `"Klaus"` evidence and family movie event
  summaries with `$70`/`"Auto"` budget confirmations plus Wish/Encanto and
  work-deadline distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-movie-events-tight-current-20260524T071500Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-relationship-work-bookclub-strict-current-20260524T054000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.4716096579476863, missed-recall cases 238/355, wrong-recall/noise cases
  372/400, total hit evidence ids 453, missing ids 641, noise ids 2804, and
  zero-recall cases 113.
- Delta:
  vs the relationship/work plus book-club run, global hit evidence ids improve
  435 -> 453, missing ids 659 -> 641, noise ids 2808 -> 2804, missed-recall
  cases 240 -> 238, wrong-recall/noise cases 374 -> 372, and zero-recall
  cases 116 -> 113. Summarization rises by +12 hit ids, -12 missing ids,
  +4 noise ids, one fewer incomplete case, one fewer wrong/noise case, and
  two fewer zero-recall cases. Target `14:summarization:1` rises 0 -> 1.0 by
  recovering 0/1/2/62/63/168/169/170/171/172/173 and removing 5 noise ids.
  Event ordering rises by +6 hit ids, -6 missing ids, -9 noise ids, one fewer
  incomplete case, one fewer wrong/noise case, and one fewer zero-recall case.
  Target `14:event_ordering:2` rises 0 -> 1.0 by returning
  14/16/72/182/246/130 and removing 8 noise ids.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. The full diagnostic still has 238 missed-recall
  evidence cases and wrong-recall/noise 372/400, so broad recall and
  persistent-noise hardening remain open.

Latest writing-journey event-order repair:
- Repair:
  `src/recall/selectors/sourceOrderWritingJourney.ts` adds a bounded
  writing-journey helper for broad source-ordered event prompts. It selects
  the Michael script-editing tips, first-draft confidence shift, workshop
  nerves, workshop feedback, and revision-plan facets while rejecting
  self-editing, book, tool, deadline, schedule, festival, and final-draft
  distractors. `src/recall/selectors/sourceOrderEventPlans.ts` dispatches it
  as a complete source-ordered event plan.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped writing-journey
  event-order query with self-editing, book, tool, deadline, schedule, and
  final-draft distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-writing-journey-current-20260524T081500Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-movie-events-tight-current-20260524T071500Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.474426559356137, missed-recall cases 237/355, wrong-recall/noise cases
  371/400, total hit evidence ids 458, missing ids 636, noise ids 2781, and
  zero-recall cases 112.
- Delta:
  vs the movie-event run, global hit evidence ids improve 453 -> 458, missing
  ids 641 -> 636, noise ids 2804 -> 2781, missed-recall cases 238 -> 237,
  wrong-recall/noise cases 372 -> 371, and zero-recall cases 113 -> 112.
  Event ordering rises by +5 hit ids, -5 missing ids, -27 noise ids, one fewer
  incomplete case, one fewer wrong/noise case, and one fewer zero-recall case.
  Target `10:event_ordering:1` rises 0 -> 1.0 by returning 6/82/182/238/84
  and removing 25 noise ids.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. The full diagnostic still has 237 missed-recall
  evidence cases and wrong-recall/noise 371/400, so broad recall and
  persistent-noise hardening remain open.

Latest professional-preparation event-order repair:
- Repair:
  `src/recall/selectors/sourceOrderProfessionalPreparation.ts` adds a bounded
  professional-connections/preparation helper for broad source-ordered event
  prompts. It selects mentor networking, Laura/HR cover-letter feedback,
  storytelling interview prep, employee-handbook review, and workshop
  presentation facets while rejecting cover-letter schedule/draft/anecdote
  noise, repeated cover-letter feedback, public-speaking confidence,
  handbook-policy, Zoom/senior-producer, and workshop-logistics distractors.
  `src/recall/selectors/sourceOrderEventPlans.ts` dispatches it as a complete
  source-ordered event plan.
- Tooling:
  `scripts/prepare-phase-63-beam-data.ts` now builds external `curl` requests
  with bounded retry, connect-timeout, and max-time flags. This preserved the
  `/private/tmp/BEAM/100K.json` restore path when the Hugging Face rows
  endpoint and raw-source fetches were transiently unavailable.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped
  professional-preparation event-order query with schedule/draft/anecdote,
  public-speaking, repeated-feedback, handbook-policy, Zoom, and logistics
  distractors. `tests/unit/prepare-phase-63-beam-data.test.ts` covers the
  bounded external curl retry command.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-professional-prep-five-tight-current-20260529T151000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-writing-journey-current-20260524T081500Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.4772434607645877, missed-recall cases 236/355, wrong-recall/noise cases
  370/400, total hit evidence ids 463, missing ids 631, noise ids 2751, and
  zero-recall cases 111.
- Delta:
  vs the writing-journey run, global hit evidence ids improve 458 -> 463,
  missing ids 636 -> 631, noise ids 2781 -> 2751, missed-recall cases
  237 -> 236, wrong-recall/noise cases 371 -> 370, and zero-recall cases
  112 -> 111. Event ordering rises by +5 hit ids, -5 missing ids, -27 noise
  ids, one fewer incomplete case, one fewer wrong/noise case, and one fewer
  zero-recall case. Target `8:event_ordering:2` rises 0 -> 1.0 by returning
  exactly 6/56/114/172/226 and removing 26 noise ids.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. The full diagnostic still has 236 missed-recall
  evidence cases and wrong-recall/noise 370/400, so broad recall and
  persistent-noise hardening remain open.

Latest professional-preparation summary repair:
- Repair:
  `src/recall/selectors/sourceOrderProfessionalPreparationSummary.ts` adds a
  bounded professional-preparation summary helper. It selects the Leslie
  networking mentor, single-column cover-letter format, storytelling interview
  preparation, employee-handbook review, and July 25 workshop presentation
  anchors, then includes adjacent assistant guidance for each. It rejects
  cover-letter CTA, mock-session confidence, calendar/travel logistics,
  workshop checklist, senior-producer follow-up, and other nearby planning
  distractors.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped
  professional-preparation summary query with user/assistant evidence pairs
  and CTA, confidence, calendar, producer-follow-up, and logistics distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-professional-prep-summary-refactor-current-20260529T162000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-professional-prep-five-tight-current-20260529T151000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.48006036217303844, missed-recall cases 235/355, wrong-recall/noise cases
  369/400, total hit evidence ids 473, missing ids 621, noise ids 2742, and
  zero-recall cases 110.
- Delta:
  vs the professional-preparation event-order run, global hit evidence ids
  improve 463 -> 473, missing ids 631 -> 621, noise ids 2751 -> 2742,
  missed-recall cases 236 -> 235, wrong-recall/noise cases 370 -> 369, and
  zero-recall cases 111 -> 110. Summarization rises by +10 hit ids,
  -10 missing ids, -11 noise ids, one fewer incomplete case, one fewer
  wrong/noise case, and one fewer zero-recall case. Target
  `8:summarization:2` rises 0 -> 1.0 by returning exactly
  6/7/78/79/114/115/172/173/226/227 and removing 12 noise ids.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. The full diagnostic still has 235 missed-recall
  evidence cases and wrong-recall/noise 369/400, so broad recall and
  persistent-noise hardening remain open.

- Repair: probability-concepts summary selector for the advanced birthday-
  paradox learning arc.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped probability-
  concepts development summary query with birthday-paradox permutations,
  conditional aces, complement-rule examples, direct/complement counting, and
  mutual-exclusivity milestones while excluding early paint/ratio/coin/dice
  probability basics.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-probability-concepts-summary-current-20260529T174000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-professional-prep-summary-refactor-current-20260529T162000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.48287726358148914, missed-recall cases 234/355, wrong-recall/noise cases
  368/400, total hit evidence ids 483, missing ids 611, noise ids 2727, and
  zero-recall cases 109.
- Delta:
  vs the professional-preparation summary run, global hit evidence ids improve
  473 -> 483, missing ids 621 -> 611, noise ids 2742 -> 2727, missed-recall
  cases 235 -> 234, wrong-recall/noise cases 369 -> 368, and zero-recall
  cases 110 -> 109. Summarization rises by +10 hit ids, -10 missing ids,
  -16 noise ids, one fewer incomplete case, one fewer wrong/noise case, and
  one fewer zero-recall case. Target `5:summarization:2` rises 0 -> 1.0 by
  returning exactly 140/141/146/149/151/153/155/156/180/181 and removing 15
  noise ids.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. The full diagnostic still has 234 missed-recall
  evidence cases and wrong-recall/noise 368/400, so broad recall and
  persistent-noise hardening remain open.

- Repair: household-budget multi-hop reasoning selector.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped household-
  budget reasoning query with shared finances, spending habits, expense
  tracking, Ashlee medical-bill support, and the grocery-budget plus freelance-
  contract update while excluding earlier unrelated medical, vehicle, and
  renovation savings distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-household-budget-reasoning-narrow-current-20260529T184500Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-probability-concepts-summary-current-20260529T174000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.48569416498993984, missed-recall cases 233/355, wrong-recall/noise cases
  367/400, total hit evidence ids 493, missing ids 601, noise ids 2726, and
  zero-recall cases 108.
- Delta:
  vs the probability-concepts summary run, global hit evidence ids improve
  483 -> 493, missing ids 611 -> 601, noise ids 2727 -> 2726, missed-recall
  cases 234 -> 233, wrong-recall/noise cases 368 -> 367, and zero-recall
  cases 109 -> 108. Multi-session reasoning rises by +10 hit ids, -10
  missing ids, -3 noise ids, one fewer incomplete case, one fewer wrong/noise
  case, and one fewer zero-recall case. Target
  `16:multi_session_reasoning:2` rises 0 -> 1.0 by returning exactly
  12/13/14/15/16/17/108/109/126/127 and removing 46/214/310 as target noise.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. The full diagnostic still has 233 missed-recall
  evidence cases and wrong-recall/noise 367/400, so broad recall and
  persistent-noise hardening remain open.

- Repair: sneaker summary selector and summary/source-preference noise guard.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped sneaker option
  and activity-advice summary query with daily options, Ultraboost fit,
  Allbirds comparison/try-on advice, running-vs-casual decisions, and hiking
  moisture/trail guidance while excluding Air Max, five-mile walking, arch-
  support, Boost-midsole TPU, and generic instruction distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-sneaker-summary-current-20260530T010000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-household-budget-reasoning-narrow-current-20260529T184500Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.49132796780684124, missed-recall cases 231/355, wrong-recall/noise cases
  365/400, total hit evidence ids 506, missing ids 588, noise ids 2707, and
  zero-recall cases 106.
- Delta:
  vs the household-budget reasoning run, global hit evidence ids improve
  493 -> 506, missing ids 601 -> 588, noise ids 2726 -> 2707, missed-recall
  cases 233 -> 231, wrong-recall/noise cases 367 -> 365, and zero-recall
  cases 108 -> 106. Summarization rises by +13 hit ids, -13 missing ids,
  -22 noise ids, two fewer incomplete cases, two fewer wrong/noise cases, and
  two fewer zero-recall cases. Target `15:summarization:2` rises 0 -> 1.0 by
  returning exactly 1/3/81/83/141/143/203/205 and removing
  89/194/0/8/214/58/160/9/24/25 as target noise. Target
  `15:summarization:1` rises 0 -> 1.0 by returning exactly 1/3/81/141/203
  and removing 86/87/126/127/184/185/260/261 as target noise.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. The full diagnostic still has 231 missed-recall
  evidence cases and wrong-recall/noise 365/400, so broad recall and
  persistent-noise hardening remain open.

- Repair: free-will personal-reflection source-order event plan.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped free-will and
  personal-reflection event-order query with Daniel Dennett/Freedom Evolves,
  Trolley Problem, soft-determinism journaling, Experience Machine,
  Shelly/incompatibilism accountability, and Ship of Theseus milestones while
  excluding divine intervention, logical reasoning, Tanya moral dilemmas,
  weekly check-ins, fiction journaling, and instruction distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-freewill-reflection-current-20260530T020000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-sneaker-summary-current-20260530T010000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.49414486921529194, missed-recall cases 230/355, wrong-recall/noise cases
  364/400, total hit evidence ids 512, missing ids 582, noise ids 2694, and
  zero-recall cases 105.
- Delta:
  vs the sneaker summary run, global hit evidence ids improve 506 -> 512,
  missing ids 588 -> 582, noise ids 2707 -> 2694, missed-recall cases
  231 -> 230, wrong-recall/noise cases 365 -> 364, and zero-recall cases
  106 -> 105. Event-ordering rises by +6 hit ids, -6 missing ids, -11 noise
  ids, one fewer incomplete case, one fewer wrong/noise case, and one fewer
  zero-recall case. Target `12:event_ordering:2` rises 0 -> 1.0 by returning
  exactly 32/50/78/98/176/218 and removing
  158/322/232/48/328/54/55/152/153 as target noise.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. The full diagnostic still has 230 missed-recall
  evidence cases and wrong-recall/noise 364/400, so broad recall and
  persistent-noise hardening remain open.

- Repair: resume strategy source-order summary selector.
- Regression:
  `tests/unit/recall.selection.test.ts` covers BEAM-shaped resume development
  and job-application strategy summaries for both general improvement and
  past-months progression phrasing. The tests keep age/job-hunt positioning,
  Joshua ATS/budgeting help, Caribbean community experience, Jobscan keyword
  optimization, transferable skills, dated industry tailoring, Canva ATS
  formatting, interview/workshop prioritization, callback optimization,
  rapport-building, and latest certification/promotion milestones while
  excluding quantified-bullets, action-verb-library, generic Canva/Jobscan,
  and cross-cultural communication distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-resume-strategy-summary-refined-current-20260530T033000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-freewill-reflection-current-20260530T020000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.49921529175050317, missed-recall cases 228/355, wrong-recall/noise cases
  362/400, total hit evidence ids 522, missing ids 572, noise ids 2683, and
  zero-recall cases 104.
- Delta:
  vs the free-will reflection run, global hit evidence ids improve
  512 -> 522, missing ids 582 -> 572, noise ids 2694 -> 2683,
  missed-recall cases 230 -> 228, wrong-recall/noise cases 364 -> 362, and
  zero-recall cases 105 -> 104. Summarization rises by +10 hit ids,
  -10 missing ids, -11 noise ids, two fewer incomplete cases, two fewer
  wrong/noise cases, and one fewer zero-recall case. Targets
  `6:summarization:1` and `6:summarization:2` rise to 1.0 by returning
  exactly 1/5/7/57/111 and 15/19/71/93/139/191, with no target noise.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. The full diagnostic still has 228 missed-recall
  evidence cases and wrong-recall/noise 362/400, so broad recall and
  persistent-noise hardening remain open.

- Repair: AI hiring compliance source-order summary selector.
- Regression:
  `tests/unit/recall.selection.test.ts` covers BEAM-shaped legal and policy
  requirement recaps for AI hiring processes. The test keeps the Montserrat
  Data Protection Act / GDPR-like requirements, hiring-policy AI transparency,
  fairness audits, candidate notifications, privacy/security, consent, human
  oversight, June 2024 Employment Act amendments, legal-expert
  compliance-checklist preparation, and current AI usage examples while
  excluding 2FA/security training, meeting-invite, hybrid-approach, metrics,
  and feedback-loop distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-ai-hiring-compliance-current-20260530T043000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-resume-strategy-summary-refined-current-20260530T033000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5020321931589539, missed-recall cases 227/355, wrong-recall/noise cases
  361/400, total hit evidence ids 527, missing ids 567, noise ids 2671, and
  zero-recall cases 103.
- Delta:
  vs the resume strategy summary run, global hit evidence ids improve
  522 -> 527, missing ids 572 -> 567, noise ids 2683 -> 2671,
  missed-recall cases 228 -> 227, wrong-recall/noise cases 362 -> 361, and
  zero-recall cases 104 -> 103. Summarization rises by +5 hit ids,
  -5 missing ids, -12 noise ids, one fewer incomplete case, one fewer
  wrong/noise case, and one fewer zero-recall case. Target
  `11:summarization:2` rises from 0 to 1.0 by returning exactly
  43/99/233/235/237, with no target noise.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. The full diagnostic still has 227 missed-recall
  evidence cases and wrong-recall/noise 361/400, so broad recall and
  persistent-noise hardening remain open.

- Repair: deadline/application source-ordered date-update path.
- Regression:
  `tests/unit/language/service.test.ts` keeps application/deadline mentions of
  `senior producer role` out of the identity-role slot, and
  `tests/unit/recall.selection.test.ts` covers BEAM-shaped senior-producer
  application deadline updates. The recall regression keeps the original
  May 10 application deadline and the May 20 extended deadline while excluding
  date-format instruction, scheduling-instruction, and side-project application
  distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-deadline-application-update-current-20260530T070000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-ai-hiring-compliance-current-20260530T043000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5048490945674046, missed-recall cases 226/355, wrong-recall/noise cases
  361/400, total hit evidence ids 529, missing ids 565, noise ids 2676, and
  zero-recall cases 102.
- Delta:
  vs the AI hiring compliance run, global hit evidence ids improve 527 -> 529,
  missing ids 567 -> 565, noise ids 2671 -> 2676, missed-recall cases
  227 -> 226, wrong-recall/noise cases stay 361 -> 361, and zero-recall cases
  103 -> 102. Knowledge-update rises by +2 hit ids, -2 missing ids, one fewer
  incomplete case, and one fewer zero-recall case. Target
  `18:knowledge_update:2` rises from 0 to 1.0 by returning exactly 170/182,
  with no target noise.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. The full diagnostic still has 226 missed-recall
  evidence cases and wrong-recall/noise 361/400, so broad recall and
  persistent-noise hardening remain open.

2026-05-30 mentor age/role routing and information-extraction repair
--------------------------------------------------------------------

- Repair: mentor age/role source-ordered information-extraction path.
- Regression:
  `tests/unit/language/service.test.ts` and `tests/unit/recall.router.test.ts`
  keep `age and role of the mentor` questions out of the identity-role slot,
  and `tests/unit/recall.selection.test.ts` covers the BEAM-shaped mentor
  workshop evidence with same-session companion distractors. The recall
  regression keeps chat 30 while excluding nearby workshop, senior-producer
  role, and same-session direct-factual companion noise.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-mentor-role-routing-current-20260530T080000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-deadline-application-update-current-20260530T070000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5076659959758553, missed-recall cases 225/355, wrong-recall/noise cases
  361/400, total hit evidence ids 530, missing ids 564, noise ids 2670, and
  zero-recall cases 101.
- Delta:
  vs the deadline/application run, global hit evidence ids improve 529 -> 530,
  missing ids 565 -> 564, noise ids 2676 -> 2670, missed-recall cases
  226 -> 225, wrong-recall/noise cases stay 361 -> 361, and zero-recall cases
  102 -> 101. Information-extraction rises by +1 hit id, -1 missing id, one
  fewer incomplete case, one fewer zero-recall case, and one fewer noise id.
  Target `18:information_extraction:1` rises from 0 to 1.0 by returning
  exactly 30 with no target noise.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. The full diagnostic still has 225 missed-recall
  evidence cases and wrong-recall/noise 361/400, so broad recall and
  persistent-noise hardening remain open.

2026-05-30 API endpoint technologies information-extraction repair
------------------------------------------------------------------

- Repair: API endpoint startup-technology source-ordered extraction path.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped API endpoint
  technology question with same-session project/API distractors. The recall
  regression keeps chat 10, which names vanilla JavaScript ES2021, HTML5, CSS3,
  and the OpenWeather API endpoint, while excluding ESLint, API-key, CI, and
  feature-complete project noise.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-api-endpoint-technologies-current-20260530T090000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-mentor-role-routing-current-20260530T080000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.510482897384306, missed-recall cases 224/355, wrong-recall/noise cases
  360/400, total hit evidence ids 531, missing ids 563, noise ids 2666, and
  zero-recall cases 100.
- Delta:
  vs the mentor age/role run, global hit evidence ids improve 530 -> 531,
  missing ids 564 -> 563, noise ids 2670 -> 2666, missed-recall cases
  225 -> 224, wrong-recall/noise cases improve 361 -> 360, and zero-recall
  cases 101 -> 100. Information-extraction rises by +1 hit id, -1 missing id,
  five fewer noise ids, one fewer incomplete case, one fewer zero-recall case,
  and one fewer wrong-recall/noise case. Target `2:information_extraction:1`
  rises from 0 to 1.0 by returning exactly 10 with no target noise and removing
  target noise 70/186/50/183/58.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. The full diagnostic still has 224 missed-recall
  evidence cases and wrong-recall/noise 360/400, so broad recall and
  persistent-noise hardening remain open.

2026-05-30 single-card probability information-extraction repair
----------------------------------------------------------------

- Repair: single-card probability source-ordered extraction path.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped probability
  question with same-session deck and conditional-probability distractors. The
  recall regression keeps chat 32, which states `P = 4/52 = 1/13` before the
  two-card follow-up, while excluding face-card/spade, two-ace, conditional
  probability, and two-coin distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-single-card-probability-current-20260530T100000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-api-endpoint-technologies-current-20260530T090000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5132997987927567, missed-recall cases 223/355, wrong-recall/noise cases
  359/400, total hit evidence ids 532, missing ids 562, noise ids 2662, and
  zero-recall cases 99.
- Delta:
  vs the API endpoint technologies run, global hit evidence ids improve
  531 -> 532, missing ids 563 -> 562, noise ids 2666 -> 2662,
  missed-recall cases 224 -> 223, wrong-recall/noise cases improve
  360 -> 359, and zero-recall cases 100 -> 99. Information-extraction rises
  by +1 hit id, -1 missing id, six fewer noise ids, one fewer incomplete case,
  one fewer zero-recall case, and one fewer wrong-recall/noise case. Target
  `5:information_extraction:2` rises from 0 to 1.0 by returning exactly 32
  with no target noise and removing target noise 58/134/64/234/72/70.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. The full diagnostic still has 223 missed-recall
  evidence cases and wrong-recall/noise 359/400, so broad recall and
  persistent-noise hardening remain open.

2026-05-30 Laura meeting-location information-extraction repair
---------------------------------------------------------------

- Repair: named meeting-location source-ordered extraction path.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped Laura
  meeting-location question with portfolio, cover-letter, schedule, and
  handbook distractors. The recall regression keeps chat 10, which says Laura
  met the user on set at Blue Horizon Studios in 2019, while excluding
  companion noise.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-laura-meeting-location-current-20260530T110000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-single-card-probability-current-20260530T100000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5161167002012074, missed-recall cases 222/355, wrong-recall/noise cases
  358/400, total hit evidence ids 533, missing ids 561, noise ids 2659, and
  zero-recall cases 98.
- Delta:
  vs the single-card probability run, global hit evidence ids improve
  532 -> 533, missing ids 562 -> 561, noise ids 2662 -> 2659,
  missed-recall cases 223 -> 222, wrong-recall/noise cases improve
  359 -> 358, and zero-recall cases 99 -> 98. Information-extraction rises
  by +1 hit id, -1 missing id, four fewer noise ids, one fewer incomplete case,
  one fewer zero-recall case, and one fewer wrong-recall/noise case. Target
  `8:information_extraction:1` rises from 0 to 1.0 by returning exactly 10
  with no target noise and removing target noise 41/149/78/172/114.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Two already-missed zero-recall cases trade noise ids,
  but global noise still decreases. The full diagnostic still has 222
  missed-recall evidence cases and wrong-recall/noise 358/400, so broad recall
  and persistent-noise hardening remain open.

2026-05-30 partner meeting date/location information-extraction repair
----------------------------------------------------------------------

- Repair: partner meeting date/location source-ordered extraction path.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped partner
  when-and-where question with AI-hiring cost/goal and unrelated
  partner-meeting distractors. The recall regression keeps chat 30, which says
  the user met partner Jessica at ArtSpace Gallery on June 12, 2020.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-partner-meeting-date-location-current-20260530T120000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-laura-meeting-location-current-20260530T110000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5189336016096581, missed-recall cases 221/355, wrong-recall/noise cases
  357/400, total hit evidence ids 534, missing ids 560, noise ids 2654, and
  zero-recall cases 97.
- Delta:
  vs the Laura meeting-location run, global hit evidence ids improve
  533 -> 534, missing ids 561 -> 560, noise ids 2659 -> 2654,
  missed-recall cases 222 -> 221, wrong-recall/noise cases improve
  358 -> 357, and zero-recall cases 98 -> 97. Information-extraction rises
  by +1 hit id, -1 missing id, three fewer noise ids, one fewer incomplete
  case, one fewer zero-recall case, and one fewer wrong-recall/noise case.
  Target `11:information_extraction:1` rises from 0 to 1.0 by returning
  exactly 30 with no target noise and removing target noise 376/139/294/37/101.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Two already-noisy cases add net noise, but global
  noise still decreases. The full diagnostic still has 221 missed-recall
  evidence cases and wrong-recall/noise 357/400, so broad recall and
  persistent-noise hardening remain open.

2026-05-30 Bay Street rent information-extraction repair
--------------------------------------------------------

- Repair: current rent source-ordered extraction path.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped Bay Street rent
  question with monthly-investment, equipment-budget, and debt-management
  distractors. The recall regression keeps chat 30, which says the current
  rent is `$1,200/month` for a 3-bedroom on Bay Street.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-bay-street-rent-current-20260530T130000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-partner-meeting-date-location-current-20260530T120000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5217505030181088, missed-recall cases 220/355, wrong-recall/noise cases
  356/400, total hit evidence ids 535, missing ids 559, noise ids 2647, and
  zero-recall cases 96.
- Delta:
  vs the partner meeting date/location run, global hit evidence ids improve
  534 -> 535, missing ids 560 -> 559, noise ids 2654 -> 2647,
  missed-recall cases 221 -> 220, wrong-recall/noise cases improve
  357 -> 356, and zero-recall cases 97 -> 96. Information-extraction rises
  by +1 hit id, -1 missing id, five fewer noise ids, one fewer incomplete case,
  one fewer zero-recall case, and one fewer wrong-recall/noise case. Target
  `16:information_extraction:1` rises from 0 to 1.0 by returning exactly 30
  with no target noise and removing target noise 138/212/285.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Three unrelated noisy cases add one net noise each,
  but global noise still decreases. The full diagnostic still has 220
  missed-recall evidence cases and wrong-recall/noise 356/400, so broad recall
  and persistent-noise hardening remain open.

2026-05-30 parents distance/town information-extraction repair
--------------------------------------------------------------

- Repair: parent distance/town source-ordered extraction path.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped parent location
  question with family movie-watchlist, animated-musical, and snack-budget
  distractors. The recall regression keeps chat 6, which says Amy and Kyle
  live 15 miles away in West Janethaven.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-parents-distance-town-current-20260530T140000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-bay-street-rent-current-20260530T130000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5245674044265595, missed-recall cases 219/355, wrong-recall/noise cases
  355/400, total hit evidence ids 536, missing ids 558, noise ids 2637, and
  zero-recall cases 95.
- Delta:
  vs the Bay Street rent run, global hit evidence ids improve 535 -> 536,
  missing ids 559 -> 558, noise ids 2647 -> 2637, missed-recall cases
  220 -> 219, wrong-recall/noise cases improve 356 -> 355, and zero-recall
  cases 96 -> 95. Information-extraction rises by +1 hit id, -1 missing id,
  eight fewer noise ids, one fewer incomplete case, one fewer zero-recall case,
  and one fewer wrong-recall/noise case. Target `14:information_extraction:1`
  rises from 0 to 1.0 by returning exactly 6 with no target noise and removing
  target noise 22/23/53/138/139/142/176/192.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. One unrelated abstention case adds one noise id, but
  global noise still decreases. The full diagnostic still has 219
  missed-recall evidence cases and wrong-recall/noise 355/400, so broad recall
  and persistent-noise hardening remain open.

2026-05-30 reading-list count/page-total information-extraction repair
---------------------------------------------------------------------

- Repair: reading-list count/page-total source-ordered extraction path.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped reading-list
  number-recall question with completed-series, library-book, and generic
  book-series distractors. The recall regression keeps chat 26, which says the
  reading list has 7 series, including The Stormlight Archive and The Expanse,
  totaling 4,200 pages.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-reading-list-count-pages-current-20260530T150000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-parents-distance-town-current-20260530T140000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5273843058350102, missed-recall cases 218/355, wrong-recall/noise cases
  354/400, total hit evidence ids 537, missing ids 557, noise ids 2630, and
  zero-recall cases 94.
- Delta:
  vs the parents distance/town run, global hit evidence ids improve 536 -> 537,
  missing ids 558 -> 557, noise ids 2637 -> 2630, missed-recall cases
  219 -> 218, wrong-recall/noise cases improve 355 -> 354, and zero-recall
  cases 95 -> 94. Information-extraction rises by +1 hit id, -1 missing id,
  seven fewer noise ids, one fewer incomplete case, one fewer zero-recall case,
  and one fewer wrong-recall/noise case. Target `13:information_extraction:1`
  rises from 0 to 1.0 by returning exactly 26 with no target noise and removing
  target noise 154/214/284/124/236/60.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. One unrelated knowledge-update case adds one net
  noise id, but global noise still decreases. The full diagnostic still has
  218 missed-recall evidence cases and wrong-recall/noise 354/400, so broad
  recall and persistent-noise hardening remain open.

2026-05-30 kids activity-days information-extraction repair
-----------------------------------------------------------

- Repair: kids school activity-days source-ordered extraction path.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped school
  activity-days question with adjacent time-management, work-hours, and
  monthly-school-meeting distractors. The recall regression keeps chat 18,
  which says Emma, Michelle, and Rachel attend East Janethaven Primary School
  with activities on Tuesdays and Thursdays.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-kids-activity-days-current-20260530T160000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-reading-list-count-pages-current-20260530T150000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5302012072434609, missed-recall cases 217/355, wrong-recall/noise cases
  353/400, total hit evidence ids 538, missing ids 556, noise ids 2626, and
  zero-recall cases 93.
- Delta:
  vs the reading-list count/page-total run, global hit evidence ids improve
  537 -> 538, missing ids 557 -> 556, noise ids 2630 -> 2626,
  missed-recall cases 218 -> 217, wrong-recall/noise cases improve
  354 -> 353, and zero-recall cases 94 -> 93. Information-extraction rises by
  +1 hit id, -1 missing id, eight fewer noise ids, one fewer incomplete case,
  one fewer zero-recall case, and one fewer wrong-recall/noise case. Target
  `17:information_extraction:1` rises from 0 to 1.0 by returning exactly 18
  with no target noise and removing target noise
  19/49/163/168/169/233/264/265.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Four unrelated event-ordering cases add five net
  noise ids, but global noise still decreases. The full diagnostic still has
  217 missed-recall evidence cases and wrong-recall/noise 353/400, so broad
  recall and persistent-noise hardening remain open.

2026-05-30 print-book budget information-extraction repair
----------------------------------------------------------

- Repair: print-book budget source-ordered extraction path.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped print-book
  spending-balance question with completed-series and generic
  book-recommendation distractors. The recall regression keeps chats 34 and 35,
  which say the user allocated $120 for print editions from Montserrat Books on
  Main Street and the assistant suggested budget-fitting fiction series.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-print-book-budget-current-20260530T170000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-kids-activity-days-current-20260530T160000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5339570757880617, missed-recall cases 216/355, wrong-recall/noise cases
  352/400, total hit evidence ids 541, missing ids 553, noise ids 2618, and
  zero-recall cases 91.
- Delta:
  vs the kids activity-days run, global hit evidence ids improve 538 -> 541,
  missing ids 556 -> 553, noise ids 2626 -> 2618, missed-recall cases
  217 -> 216, wrong-recall/noise cases improve 353 -> 352, and zero-recall
  cases 93 -> 91. Information-extraction rises by +2 hit ids, -2 missing ids,
  eight fewer noise ids, one fewer incomplete case, one fewer zero-recall case,
  and one fewer wrong-recall/noise case. Target `13:information_extraction:2`
  rises from 0 to 1.0 by returning exactly 34/35 with no target noise and
  removing target noise 173/177/181/58/62/306/59/188/189. The same run
  recovers chat 34 for `13:multi_session_reasoning:1` and removes one noise id
  there.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Three non-target cases add one net noise id each, but
  global noise still decreases. The full diagnostic still has 216
  missed-recall evidence cases and wrong-recall/noise 352/400, so broad recall
  and persistent-noise hardening remain open.

2026-05-30 Patrick workshop preparation information-extraction repair
--------------------------------------------------------------------

- Repair: Patrick workshop preparation source-ordered extraction path plus
  role-query routing guard.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped mentor workshop
  decision/preparation question and verifies that chats 30/31/32/33/34/35 are
  kept in source order while duplicate snippet facts are not returned.
  `tests/unit/recall.router.test.ts` covers the same wording so `role did ...
  play` no longer becomes an identity-role slot query.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-patrick-workshop-prep-router-dedupe-current-20260530T190000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-print-book-budget-current-20260530T170000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5367739771965124, missed-recall cases 215/355, wrong-recall/noise cases
  352/400, total hit evidence ids 547, missing ids 547, noise ids 2616, and
  zero-recall cases 90.
- Delta:
  vs the print-book budget run, global hit evidence ids improve 541 -> 547,
  missing ids 553 -> 547, noise ids 2618 -> 2616, missed-recall cases
  216 -> 215, wrong-recall/noise cases stay 352/400, and zero-recall cases
  91 -> 90. Information-extraction rises by +6 hit ids, -6 missing ids,
  unchanged noise 133, one fewer incomplete case, and one fewer zero-recall
  case; average information-extraction recall rises 0.5875 -> 0.6125. Target
  `18:information_extraction:2` rises from 0 to 1.0 by returning exactly
  30/31/32/33/34/35 with no target noise.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. One non-target preference case adds one noise id, but
  global noise still decreases. The full diagnostic still has 215
  missed-recall evidence cases and wrong-recall/noise 352/400, so broad recall
  and persistent-noise hardening remain open.

2026-05-30 layout/navigation and Robert mentor-prep information-extraction repair
--------------------------------------------------------------------------------

- Repair: first-sprint layout/navigation schedule source-order path plus
  Robert academic mentor preparation/follow-up routing and selection.
- Regression:
  `tests/unit/recall.router.test.ts` covers `guide my essay writing` wording so
  it no longer routes as a reference-slot query. `tests/unit/recall.selection.test.ts`
  covers the Robert mentor preparation/follow-up pair and the first-sprint
  layout/navigation schedule pair, including later Trello/Lighthouse snippets.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-layout-and-robert-prep-current-20260530T210000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-patrick-workshop-prep-router-dedupe-current-20260530T190000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5424077800134139, missed-recall cases 213/355, wrong-recall/noise cases
  351/400, total hit evidence ids 551, missing ids 543, noise ids 2618, and
  zero-recall cases 88.
- Delta:
  vs the Patrick workshop run, global hit evidence ids improve 547 -> 551,
  missing ids 547 -> 543, noise ids rise 2616 -> 2618, missed-recall cases
  215 -> 213, wrong-recall/noise cases improve 352 -> 351, and zero-recall
  cases 90 -> 88. Information-extraction rises by +4 hit ids, -4 missing ids,
  two fewer noise ids, two fewer incomplete cases, two fewer zero-recall cases,
  and one fewer wrong-recall/noise case; average information-extraction recall
  rises 0.6125 -> 0.6625. Target `3:information_extraction:2` rises from 0 to
  1.0 by returning exactly 12/13 and removing target noise 39/40. Target
  `7:information_extraction:2` rises from 0 to 1.0 by returning exactly 14/15
  with no target noise.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Non-target abstention, knowledge-update, and
  event-ordering cases add four net noise ids, so total noise remains open.
  The full diagnostic still has 213 missed-recall evidence cases and
  wrong-recall/noise 351/400, so broad recall and persistent-noise hardening
  remain open.


2026-05-30 Laura mixer prior-connection information-extraction repair
--------------------------------------------------------------------

- Repair:
  Laura mixer prior-connection selection now keeps the source turns where Laura
  recommended the Coral Bay Hotel mixer and links that decision to the prior
  Blue Horizon Studios connection, ahead of Leslie/Greg networking distractors.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped Laura mixer
  prior-connection extraction case, including Leslie/Greg distractors and a
  storytelling-networking snippet.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-laura-mixer-prior-connection-current-20260530T220000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-layout-and-robert-prep-current-20260530T210000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5452246814218646, missed-recall cases 212/355, wrong-recall/noise cases
  350/400, total hit evidence ids 553, missing ids 541, noise ids 2610, and
  zero-recall cases 87.
- Delta:
  vs the layout/navigation plus Robert mentor-prep run, global hit evidence ids
  improve 551 -> 553, missing ids 543 -> 541, noise ids decrease 2618 -> 2610,
  missed-recall cases improve 213 -> 212, wrong-recall/noise cases improve
  351 -> 350, and zero-recall cases 88 -> 87. Information-extraction rises by
  +2 hit ids, -2 missing ids, two fewer noise ids, one fewer incomplete case,
  one fewer zero-recall case, and one fewer wrong-recall/noise case; average
  information-extraction recall rises 0.6625 -> 0.6875. Target
  `8:information_extraction:2` rises from 0 to 1.0 by returning exactly 10/11
  and removing target noise 25/24.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, no
  negative recall deltas, and no positive noise deltas. The full diagnostic
  still has 212 missed-recall evidence cases and wrong-recall/noise 350/400,
  so broad recall and persistent-noise hardening remain open.


2026-05-30 Laura weekly video-call schedule-advice information-extraction repair
-------------------------------------------------------------------------------

- Repair:
  Laura weekly video-call schedule-advice selection now keeps the six source
  turns for asking Laura about multiple projects, work/personal boundaries, and
  follow-up thank-you summaries, ahead of pilot-plan and Google
  Calendar/Moleskine schedule distractors.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped Laura weekly
  call schedule-advice extraction case, including pilot-plan, calendar/planner,
  and script-finalization distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-laura-weekly-call-schedule-advice-current-20260530T223000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-laura-mixer-prior-connection-current-20260530T220000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5480415828303153, missed-recall cases 211/355, wrong-recall/noise cases
  349/400, total hit evidence ids 559, missing ids 535, noise ids 2608, and
  zero-recall cases 86.
- Delta:
  vs the Laura mixer run, global hit evidence ids improve 553 -> 559, missing
  ids 541 -> 535, noise ids decrease 2610 -> 2608, missed-recall cases improve
  212 -> 211, wrong-recall/noise cases improve 350 -> 349, and zero-recall
  cases 87 -> 86. Information-extraction rises by +6 hit ids, -6 missing ids,
  five fewer noise ids, one fewer incomplete case, one fewer zero-recall case,
  and one fewer wrong-recall/noise case; average information-extraction recall
  rises 0.6875 -> 0.7125. Target `17:information_extraction:2` rises from 0
  to 1.0 by returning exactly 26/27/28/29/30/31 and removing target noise
  35/36/37/38/39.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Four non-target cases add one noise id each, but
  total noise still decreases. The full diagnostic still has 211 missed-recall
  evidence cases and wrong-recall/noise 349/400, so broad recall and
  persistent-noise hardening remain open.


2026-05-30 triangle similarity-ratio information-extraction repair
------------------------------------------------------------------

- Repair:
  triangle similarity-ratio selection now keeps the source pair for verifying
  corresponding-side ratios across the 9/12/15 and 6.75/9/11.25 triangles,
  ahead of triangle-area and broad geometry distractors.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped similarity-ratio
  verification case, including median/altitude, Heron's formula, broad
  similarity, and congruence-error distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-triangle-similarity-ratio-exact-current-20260530T231000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-laura-weekly-call-schedule-advice-current-20260530T223000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5508584842387659, missed-recall cases 210/355, wrong-recall/noise cases
  348/400, total hit evidence ids 561, missing ids 533, noise ids 2600, and
  zero-recall cases 85.
- Delta:
  vs the Laura weekly-call run, global hit evidence ids improve 559 -> 561,
  missing ids 535 -> 533, noise ids decrease 2608 -> 2600, missed-recall cases
  improve 211 -> 210, wrong-recall/noise cases improve 349 -> 348, and
  zero-recall cases 86 -> 85. Information-extraction rises by +2 hit ids, -2
  missing ids, nine fewer noise ids, one fewer incomplete case, one fewer
  zero-recall case, and one fewer wrong-recall/noise case; average
  information-extraction recall rises 0.7125 -> 0.7375. Target
  `4:information_extraction:2` rises from 0 to 1.0 by returning exactly
  166/167 and removing target noise 73/101/117/133/134/135/190/191.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Two non-target cases add one noise id each, but
  total noise still decreases. The full diagnostic still has 210 missed-recall
  evidence cases and wrong-recall/noise 348/400, so broad recall and
  persistent-noise hardening remain open.


2026-05-30 resume keyword integration information-extraction repair
-------------------------------------------------------------------

- Repair:
  resume keyword integration selection now keeps the source pair for
  incorporating `project management` and `budget oversight` naturally across
  the Professional Summary, Work Experience, Skills, Education/Certifications,
  and Portfolio/additional resume sections, ahead of generic
  age-discrimination, resume formatting, and broad career distractors.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped resume keyword
  integration case, including age-discrimination, broad film/TV tailoring,
  remote team leadership, bullet-style, minimalist layout, and international
  resume distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-resume-keyword-integration-full-current-20260530T233000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-triangle-similarity-ratio-exact-current-20260530T231000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5536753856472167, missed-recall cases 209/355, wrong-recall/noise cases
  347/400, total hit evidence ids 563, missing ids 531, noise ids 2589, and
  zero-recall cases 84.
- Delta:
  vs the triangle similarity-ratio run, global hit evidence ids improve
  561 -> 563, missing ids 533 -> 531, noise ids decrease 2600 -> 2589,
  missed-recall cases improve 210 -> 209, wrong-recall/noise cases improve
  348 -> 347, and zero-recall cases 85 -> 84. Information-extraction rises by
  +2 hit ids, -2 missing ids, twelve fewer noise ids, one fewer incomplete
  case, one fewer zero-recall case, and one fewer wrong-recall/noise case;
  average information-extraction recall rises 0.7375 -> 0.7625. Target
  `6:information_extraction:2` rises from 0 to 1.0 by returning exactly 24/25
  and removing target noise 1/15/111/124/125/173/203/94/144/36/37/74/75.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Three non-target cases add noise through
  source-neighbor reshuffles, but total noise still decreases. The full
  diagnostic still has 209 missed-recall evidence cases and wrong-recall/noise
  347/400, so broad recall and persistent-noise hardening remain open.


2026-05-30 emergency-fund savings-plan information-extraction repair
--------------------------------------------------------------------

- Repair:
  emergency-fund savings-plan selection now keeps the source pair for reaching
  a $2,000 emergency-fund goal by June 30, 2024 from $500 already saved,
  including the remaining amount, monthly savings target, automatic transfers,
  expense cuts, income increases, and regular progress reviews.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped emergency-fund
  savings-plan case, including average-income, debt-management,
  investment/car-savings split, freelance-contract, schedule-fit, and
  cash-reserve distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-emergency-fund-savings-plan-current-20260530T235000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-resume-keyword-integration-full-current-20260530T233000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5564922870556674, missed-recall cases 208/355, wrong-recall/noise cases
  346/400, total hit evidence ids 565, missing ids 529, noise ids 2581, and
  zero-recall cases 83.
- Delta:
  vs the resume keyword integration run, global hit evidence ids improve
  563 -> 565, missing ids 531 -> 529, noise ids decrease 2589 -> 2581,
  missed-recall cases improve 209 -> 208, wrong-recall/noise cases improve
  347 -> 346, and zero-recall cases 84 -> 83. Information-extraction rises by
  +2 hit ids, -2 missing ids, six fewer noise ids, one fewer incomplete case,
  one fewer zero-recall case, and one fewer wrong-recall/noise case; average
  information-extraction recall rises 0.7625 -> 0.7875. Target
  `16:information_extraction:2` rises from 0 to 1.0 by returning exactly 34/35
  and removing target noise 27/183/105/79/123/305.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. One non-target event-ordering case adds one noise
  id, but total noise still decreases. The full diagnostic still has 208
  missed-recall evidence cases and wrong-recall/noise 346/400, so broad recall
  and persistent-noise hardening remain open.

## 2026-05-31 retained rate-limit request-flow information-extraction repair

- Mechanism:
  source-ordered information-extraction selection now keeps the three source
  turns for managing OpenWeather API request flow under frequent retries and
  bursts of activity: elapsed-interval counter resets plus queueing, rapid-call
  queue processing, and capped exponential backoff for repeated retries.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped rate-limit
  request-flow case, including weather-app, cache/performance, Node-upgrade,
  API-key/domain, and custom-feature distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-rate-limit-request-flow-current-20260531T000500Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-emergency-fund-savings-plan-current-20260530T235000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.558370221327968, missed-recall cases 207/355, wrong-recall/noise cases
  345/400, total hit evidence ids 567, missing ids 527, noise ids 2573, and
  zero-recall cases 83.
- Delta:
  vs the emergency-fund savings-plan run, global hit evidence ids improve
  565 -> 567, missing ids 529 -> 527, noise ids decrease 2581 -> 2573,
  missed-recall cases improve 208 -> 207, wrong-recall/noise cases improve
  346 -> 345, and zero-recall cases stay at 83. Information-extraction rises
  by +2 hit ids, -2 missing ids, nine fewer noise ids, one fewer incomplete
  case, and one fewer wrong-recall/noise case; average information-extraction
  recall rises 0.7875 -> 0.8042. Target `2:information_extraction:2` rises
  from 0.3333333333333333 to 1.0 by returning exactly 33/35/37, recovering
  35/37, and removing target noise
  32/90/116/117/150/151/154/64/65/122/123.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Five non-target cases add net noise through
  source-neighbor reshuffles, but total noise still decreases. The full
  diagnostic still has 207 missed-recall evidence cases and wrong-recall/noise
  345/400, so broad recall and persistent-noise hardening remain open.

## 2026-05-31 retained partner classic-movie information-extraction repair

- Mechanism:
  source-ordered information-extraction selection now keeps the source pair
  connecting shared classic-film interests with partner Thomas to timeless
  movie recommendations and the Miami film festival meeting context.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped partner
  classic-movie recommendation case, including Soul/theme-discussion,
  Endgame-schedule, movie-rental, guest-list, animated-musical, platform, and
  sneaker-material distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-partner-classic-movie-current-20260531T002000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-rate-limit-request-flow-current-20260531T000500Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5611871227364187, missed-recall cases 206/355, wrong-recall/noise cases
  344/400, total hit evidence ids 569, missing ids 525, noise ids 2568, and
  zero-recall cases 82.
- Delta:
  vs the rate-limit request-flow run, global hit evidence ids improve
  567 -> 569, missing ids 527 -> 525, noise ids decrease 2573 -> 2568,
  missed-recall cases improve 207 -> 206, wrong-recall/noise cases improve
  345 -> 344, and zero-recall cases improve 83 -> 82. Information-extraction
  rises by +2 hit ids, -2 missing ids, nine fewer noise ids, one fewer
  incomplete case, one fewer zero-recall case, and one fewer
  wrong-recall/noise case; average information-extraction recall rises
  0.8042 -> 0.8292. Target `14:information_extraction:2` rises from 0 to 1.0
  by returning exactly 12/13 and removing target noise
  95/126/142/143/187/217/243/52/214.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Five non-target cases add net noise through
  source-neighbor reshuffles, but total noise still decreases. The full
  diagnostic still has 206 missed-recall evidence cases and wrong-recall/noise
  344/400, so broad recall and persistent-noise hardening remain open.

## 2026-05-31 retained colour-technologist profession information-extraction repair

- Mechanism:
  source-ordered information-extraction selection now keeps the source turn
  saying the user is a 44-year-old colour technologist from Port Michael in a
  probability-basics context.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped profession
  question, including independent-events, die-roll, birthday-paradox, and
  unrelated product probability distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-colour-technologist-profession-current-20260531T003500Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-partner-classic-movie-current-20260531T002000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5640040241448694, missed-recall cases 205/355, wrong-recall/noise cases
  343/400, total hit evidence ids 570, missing ids 524, noise ids 2557, and
  zero-recall cases 81.
- Delta:
  vs the partner classic-movie run, global hit evidence ids improve 569 -> 570,
  missing ids 525 -> 524, noise ids decrease 2568 -> 2557, missed-recall cases
  improve 206 -> 205, wrong-recall/noise cases improve 344 -> 343, and
  zero-recall cases improve 82 -> 81. Information-extraction rises by +1 hit
  id, -1 missing id, six fewer noise ids, one fewer incomplete case, one fewer
  zero-recall case, and one fewer wrong-recall/noise case; average
  information-extraction recall rises 0.8292 -> 0.8542. Target
  `5:information_extraction:1` rises from 0 to 1.0 by returning exactly 16 and
  removing target noise 63/14/156/90.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, no
  negative recall deltas, and no positive noise deltas. The full diagnostic
  still has 205 missed-recall evidence cases and wrong-recall/noise 343/400,
  so broad recall and persistent-noise hardening remain open.

## 2026-05-31 retained ASA triangle-congruence proof-plan information-extraction repair

- Mechanism:
  source-ordered information-extraction selection now keeps the assistant
  source turn that labels triangles ABC and DEF, states the matching angle
  pairs plus 7 cm included side, applies ASA, and concludes congruence.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped ASA
  congruence-proof question, including broad similarity, SSA ambiguity,
  diagram-instruction, and proof-outline distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-asa-triangle-congruence-current-20260531T011000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-colour-technologist-profession-current-20260531T003500Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5668209255533201, missed-recall cases 204/355, wrong-recall/noise cases
  342/400, total hit evidence ids 571, missing ids 523, noise ids 2551, and
  zero-recall cases 80.
- Delta:
  vs the colour-technologist profession run, global hit evidence ids improve
  570 -> 571, missing ids 524 -> 523, noise ids decrease 2557 -> 2551,
  missed-recall cases improve 205 -> 204, wrong-recall/noise cases improve
  343 -> 342, and zero-recall cases improve 81 -> 80.
  Information-extraction rises by +1 hit id, -1 missing id, four fewer noise
  ids, one fewer incomplete case, one fewer zero-recall case, and one fewer
  wrong-recall/noise case; average information-extraction recall rises
  0.8542 -> 0.8792. Target `4:information_extraction:1` rises from 0 to 1.0
  by returning exactly 151 and removing target noise 140/196/206/60.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Two non-target cases add one noise id each, but total
  noise still decreases. The full diagnostic still has 204 missed-recall
  evidence cases and wrong-recall/noise 342/400, so broad recall and
  persistent-noise hardening remain open.

## 2026-05-31 retained AI hiring fairness/speed information-extraction repair

- Mechanism:
  source-ordered information-extraction selection now keeps the assistant
  source turn that balances faster candidate screening with fairness through
  vendor transparency checks, bias audits, anonymization, human oversight,
  diversity monitoring, candidate feedback, and structured interviews.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped AI hiring
  fairness/speed question, including pilot, soft-skills, balanced-approach,
  algorithmic-bias, cost-savings, and timeline distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-ai-hiring-fairness-speed-current-20260531T014000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-asa-triangle-congruence-current-20260531T011000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5696378269617708, missed-recall cases 203/355, wrong-recall/noise cases
  341/400, total hit evidence ids 572, missing ids 522, noise ids 2544, and
  zero-recall cases 79.
- Delta:
  vs the ASA triangle-congruence run, global hit evidence ids improve
  571 -> 572, missing ids 523 -> 522, noise ids decrease 2551 -> 2544,
  missed-recall cases improve 204 -> 203, wrong-recall/noise cases improve
  342 -> 341, and zero-recall cases improve 80 -> 79.
  Information-extraction rises by +1 hit id, -1 missing id, ten fewer noise
  ids, one fewer incomplete case, one fewer zero-recall case, and one fewer
  wrong-recall/noise case; average information-extraction recall rises
  0.8792 -> 0.9042. Target `11:information_extraction:2` rises from 0 to 1.0
  by returning exactly 39 and removing target noise
  13/27/68/69/178/179/198/199/36/37.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Four non-target cases add net noise, but total noise
  still decreases. The full diagnostic still has 203 missed-recall evidence
  cases and wrong-recall/noise 341/400, so broad recall and persistent-noise
  hardening remain open.

## 2026-05-31 retained startup transition preparation information-extraction repair

- Mechanism:
  source-ordered information-extraction selection now keeps the source pair for
  the current-job versus streaming-startup decision and the follow-up
  preparation guidance: company research, current-employee conversations,
  workload and pressure clarification, startup-colleague advice,
  support-network planning, compensation/equity review, budgeting, and
  professional development.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped startup
  transition preparation question, including startup-interest, meeting,
  philosophical-reflection, career-planning, and writing-schedule distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-startup-transition-prep-current-20260531T020500Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-ai-hiring-fairness-speed-current-20260531T014000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5710462776659961, missed-recall cases 202/355, wrong-recall/noise cases
  340/400, total hit evidence ids 573, missing ids 521, noise ids 2532, and
  zero-recall cases 79.
- Delta:
  vs the AI hiring fairness/speed run, global hit evidence ids improve
  572 -> 573, missing ids 522 -> 521, noise ids decrease 2544 -> 2532,
  missed-recall cases improve 203 -> 202, wrong-recall/noise cases improve
  341 -> 340, and zero-recall cases stay at 79. Information-extraction rises
  by +1 hit id, -1 missing id, eleven fewer noise ids, one fewer incomplete
  case, and one fewer wrong-recall/noise case; instruction-following also
  loses one noise id; average information-extraction recall rises
  0.9042 -> 0.9167. Target `12:information_extraction:2` rises from 0.5 to
  1.0 by returning exactly 39/41, recovering 39, and removing target noise
  40/64/65/75/87/205/243/102/103/310/311.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, no
  negative recall deltas, and no positive noise deltas. The full diagnostic
  still has 202 missed-recall evidence cases and wrong-recall/noise 340/400,
  so broad recall and persistent-noise hardening remain open.

## 2026-05-31 retained son patent-guidance resource-plan information-extraction repair

- Mechanism:
  source-ordered information-extraction selection now keeps one entry per
  evidence chat id for the six-turn plan around Francis's engineering studies
  and patent guidance: Montserrat Community College context, relevant patent
  options, provisional/documentation planning, college/bar-association/online
  resources, the user's local resource plan, and final attorney-search steps.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped son
  patent-guidance resource-plan question with duplicate chat 11 snippets so
  later source turns 14/15 cannot be crowded out.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-son-patent-guidance-current-20260531T035000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-startup-transition-prep-current-20260531T020500Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5719852448021464, missed-recall cases 201/355, wrong-recall/noise cases
  340/400, total hit evidence ids 575, missing ids 519, noise ids 2531, and
  zero-recall cases 79.
- Delta:
  vs the startup transition preparation run, global hit evidence ids improve
  573 -> 575, missing ids 521 -> 519, noise ids decrease 2532 -> 2531,
  missed-recall cases improve 202 -> 201, wrong-recall/noise cases stay at
  340, and zero-recall cases stay at 79. Information-extraction rises by +2
  hit ids, -2 missing ids, unchanged total information-extraction noise, and
  one fewer incomplete case; average information-extraction recall rises
  0.9167 -> 0.925. Target `20:information_extraction:2` rises from
  0.6666666666666666 to 1.0 by returning exactly 10/11/12/13/14/15 and
  recovering 14/15 with no target noise.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Three non-target cases add one noise id each, but
  four non-target cases remove one noise id each and total noise still
  decreases. The full diagnostic still has 201 missed-recall evidence cases
  and wrong-recall/noise 340/400, so broad recall and persistent-noise
  hardening remain open.

## 2026-05-31 retained shoe-size cross-session count repair

- Mechanism:
  source-ordered information-extraction selection now keeps the BEAM user
  source pair needed for the cross-session shoe-size count: the earlier Adidas
  Ultraboost vs Nike React Infinity Run daily-wear choice context and the
  later Adidas Ultraboost size 11 / size 11.5 fit turn. The selector requires
  user source-message evidence so the adjacent assistant shoe-comparison advice
  cannot crowd out the size-value turn.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped shoe-size count
  question with nearby Foot Locker and neutral-color sneaker distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-shoe-size-count-current-20260531T050000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-son-patent-guidance-current-20260531T035000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5748021462105971, missed-recall cases 200/355, wrong-recall/noise cases
  340/400, total hit evidence ids 577, missing ids 517, noise ids 2534, and
  zero-recall cases 78.
- Delta:
  vs the son patent-guidance run, global hit evidence ids improve 575 -> 577,
  missing ids 519 -> 517, noise ids increase 2531 -> 2534, missed-recall cases
  improve 201 -> 200, wrong-recall/noise cases stay at 340, and zero-recall
  cases improve 79 -> 78. Multi-session reasoning rises by +2 hit ids,
  -2 missing ids, unchanged bucket noise, one fewer incomplete case, and one
  fewer zero-recall case; average multi-session-reasoning recall rises to
  0.4452. Target `15:multi_session_reasoning:1` rises from 0 to 1.0 by
  returning exactly 32/116 and recovering both required turns with no target
  noise.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Five non-target cases add net six noise ids while
  three remove net three noise ids, so this retained boundary carries a +3
  total-noise tradeoff. The full diagnostic still has 200 missed-recall
  evidence cases and wrong-recall/noise 340/400, so broad recall and
  persistent-noise hardening remain open.

## 2026-05-31 retained senior-producer preparation priority repair

- Mechanism:
  source-ordered reasoning selection now recognizes the strategic preparation
  synthesis question that combines cover-letter draft/revision deadlines,
  Leslie's creative-director Zoom call, Greg's interview-clarity improvement,
  and the follow-up STAR/specificity/active-listening practice plan. The
  selector bypasses the identity-role slot only for this senior-producer
  preparation-priority query and suppresses adjacent source-preference
  companions only after that exact reasoning plan is selected, so broader
  instruction/preference repairs remain intact.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped question with
  the real role-slot routing shape, the four target source turns, and nearby
  cover-letter/interview/preference companion distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-senior-producer-prep-narrow-current-20260531T071500Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-shoe-size-count-current-20260531T050000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5776190476190478, missed-recall cases 199/355, wrong-recall/noise cases
  340/400, total hit evidence ids 581, missing ids 513, noise ids 2532, and
  zero-recall cases 77.
- Delta:
  vs the shoe-size count run, global hit evidence ids improve 577 -> 581,
  missing ids 517 -> 513, noise ids decrease 2534 -> 2532, missed-recall cases
  improve 200 -> 199, wrong-recall/noise cases stay at 340, and zero-recall
  cases improve 78 -> 77. Multi-session reasoning rises by +4 hit ids,
  -4 missing ids, unchanged bucket noise, one fewer incomplete case, and one
  fewer zero-recall case; average multi-session-reasoning recall rises to
  0.4702. Target `8:multi_session_reasoning:2` rises from 0 to 1.0 by
  returning exactly 28/92/150/152 and recovering all four required turns with
  no target noise.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Non-target noise swaps net to a two-id total-noise
  decrease. The full diagnostic still has 199 missed-recall evidence cases and
  wrong-recall/noise 340/400, so broad recall and persistent-noise hardening
  remain open.

## 2026-05-31 retained weather-app latency comparison repair

- Mechanism:
  source-ordered reasoning selection now recognizes the cross-session weather
  app speed comparison that asks whether fetch-call latency or autocomplete API
  response time is currently faster based on tests. The selector requires the
  two measured user-evidence facets: fetch-call latency currently averaging
  250ms and autocomplete API response time averaging 280ms with the tested
  autocomplete context. It returns only when both facets are present, so generic
  weather-app implementation, debounce, error-handling, and load-test chatter
  remain excluded.
- Regression:
  `tests/unit/run-phase-63.beam-recall-diagnostic.test.ts` covers a synthetic
  BEAM row through the diagnostic runner, reproducing the pre-fix retrieval of
  implementation/debounce distractors instead of the measured latency turns.
  `tests/unit/recall.selection.test.ts` covers the pure selector shape with the
  target source turns and nearby source-order distractors.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-weather-latency-comparison-current-20260531T083000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-senior-producer-prep-narrow-current-20260531T071500Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5804359490274985, missed-recall cases 198/355, wrong-recall/noise cases
  339/400, total hit evidence ids 583, missing ids 511, noise ids 2525, and
  zero-recall cases 76.
- Delta:
  vs the senior-producer preparation run, global hit evidence ids improve
  581 -> 583, missing ids 513 -> 511, noise ids decrease 2532 -> 2525,
  missed-recall cases improve 199 -> 198, wrong-recall/noise cases improve
  340 -> 339, and zero-recall cases improve 77 -> 76. Multi-session reasoning
  rises by +2 hit ids, -2 missing ids, eight fewer bucket noise ids, one fewer
  incomplete case, one fewer zero-recall case, and one fewer wrong-recall/noise
  case; average multi-session-reasoning recall rises to 0.4952. Target
  `2:multi_session_reasoning:2` rises from 0 to 1.0 by returning exactly 38/80,
  recovering both required turns and removing target noise
  44/45/94/95/124/125/133/187.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Three non-target cases add one noise id each, but the
  target removes eight noise ids and total noise decreases by seven. The full
  diagnostic still has 198 missed-recall evidence cases and wrong-recall/noise
  339/400, so broad recall and persistent-noise hardening remain open.

## 2026-05-31 retained API daily quota update repair

- Mechanism:
  source-ordered value-update selection now recognizes daily API call quota
  questions. The bounded quota value kind requires `calls/day` or
  `calls per day` evidence and is scoped to API-key daily quota queries, so it
  keeps the earlier OpenWeather rate-limit context and the later `1,200 calls
  per day` API-key update while excluding CORS, autocomplete-caching, uptime
  monitoring, and generic quota-breach chatter.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped daily quota
  query with the old rate-limit source turn, the new quota update source turn,
  and nearby debounce/CORS/autocomplete/uptime distractors. The test was red
  before the repair: selection returned the quota update plus debounce/uptime
  noise and only later included the old rate-limit context.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-api-daily-quota-current-20260531T094500Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-weather-latency-comparison-current-20260531T083000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5832528504359492, missed-recall cases 197/355, wrong-recall/noise cases
  338/400, total hit evidence ids 585, missing ids 509, noise ids 2523, and
  zero-recall cases 75.
- Delta:
  vs the weather-latency comparison run, global hit evidence ids improve
  583 -> 585, missing ids 511 -> 509, noise ids decrease 2525 -> 2523,
  missed-recall cases improve 198 -> 197, wrong-recall/noise cases improve
  339 -> 338, and zero-recall cases improve 76 -> 75. Knowledge-update rises
  by +2 hit ids, -2 missing ids, four fewer bucket noise ids, one fewer
  incomplete case, one fewer zero-recall case, and one fewer wrong-recall/noise
  case; average knowledge-update recall rises to 0.5771. Target
  `2:knowledge_update:1` rises from 0 to 1.0 by returning exactly 32/66,
  recovering both required turns and removing target noise 48/152/95/8.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Two non-target cases add noise, but the target
  removes four noise ids and total noise decreases by two. The full diagnostic
  still has 197 missed-recall evidence cases and wrong-recall/noise 338/400, so
  broad recall and persistent-noise hardening remain open.

## 2026-05-31 retained weekly writing target update repair

- Mechanism:
  source-ordered value-update selection now recognizes weekly writing
  word-count target questions. The bounded word-count value kind accepts the
  original `words per week` target and the explicit weekly word-count
  `adjusted to` update, while excluding later final-draft `1,800-word targets`
  and other writing-session, progress-calculation, and writing-group
  distractors.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped weekly writing
  target query with the old 1,200-words-per-week source turn, the updated
  1,350-word source turn, and the real later 1,500-word and 1,800-word
  distractors. The test was red before the repair: selection returned the
  later 1,800-word distractor plus the updated target, old target, progress
  calculation, and schedule noise; after the first broad word-count pattern,
  the real-row check exposed chat 126 as an additional benchmark-wrong
  distractor, so the retained pattern was tightened to the explicit
  `adjusted to` source update.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-weekly-word-target-current-20260531T101500Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-api-daily-quota-current-20260531T094500Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5860697518443999, missed-recall cases 196/355, wrong-recall/noise cases
  337/400, total hit evidence ids 587, missing ids 507, noise ids 2512, and
  zero-recall cases 74.
- Delta:
  vs the API daily quota run, global hit evidence ids improve 585 -> 587,
  missing ids 509 -> 507, noise ids decrease 2523 -> 2512, missed-recall
  cases improve 197 -> 196, wrong-recall/noise cases improve 338 -> 337, and
  zero-recall cases improve 75 -> 74. Knowledge-update rises by +2 hit ids,
  -2 missing ids, seven fewer bucket noise ids, one fewer incomplete case, one
  fewer zero-recall case, and one fewer wrong-recall/noise case; average
  knowledge-update recall rises to 0.6021. Target `10:knowledge_update:1`
  rises from 0 to 1.0 by returning exactly 22/64, recovering both required
  turns and removing target noise 296/24/301/55/151/153/155.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. One non-target event-ordering case adds noise, but
  the target removes seven noise ids and total noise decreases by eleven. The
  full diagnostic still has 196 missed-recall evidence cases and
  wrong-recall/noise 337/400, so broad recall and persistent-noise hardening
  remain open.

## 2026-05-31 retained ASA proof preference repair

- Mechanism:
  source-ordered preference evidence now treats ASA/congruence proof
  preference queries as exclusive when the source says the user wants detailed
  proofs with diagrams. The repair keeps the user preference source turn while
  excluding generic triangle-classification visual-learning context, an old
  ASA angle-error proof, broad congruence/similarity explanations, and the
  adjacent assistant proof.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped ASA proof query
  with real-noise-shaped triangle-classification, old ASA proof, and broad
  congruence/similarity distractors. The test was red before the repair:
  selection returned the target preference plus triangle-classification
  preference and assistant noise. The real seeded row then exposed adjacent
  assistant proof leakage, so the retained source-preference path was made
  exclusive and anchor-only for ASA proof preference questions.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-asa-proof-preference-current-20260531T104500Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-weekly-word-target-current-20260531T101500Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5888866532528506, missed-recall cases 195/355, wrong-recall/noise cases
  336/400, total hit evidence ids 588, missing ids 506, noise ids 2508, and
  zero-recall cases 73.
- Delta:
  vs the weekly writing target run, global hit evidence ids improve
  587 -> 588, missing ids 507 -> 506, noise ids decrease 2512 -> 2508,
  missed-recall cases improve 196 -> 195, wrong-recall/noise cases improve
  337 -> 336, and zero-recall cases improve 74 -> 73. Preference-following
  rises by +1 hit id, -1 missing id, five fewer bucket noise ids, one fewer
  incomplete case, one fewer zero-recall case, and one fewer wrong-recall/noise
  case; average preference-following recall rises to 0.5171. Target
  `4:preference_following:2` rises from 0 to 1.0 by returning exactly 198,
  recovering the required turn and removing target noise 169/52/53/190/191.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Four non-target cases add one noise id each, but the
  target removes five noise ids and total noise decreases by four. The full
  diagnostic still has 195 missed-recall evidence cases and wrong-recall/noise
  336/400, so broad recall and persistent-noise hardening remain open.

## 2026-05-31 retained automated deployment monitoring preference repair

- Mechanism:
  source-ordered preference evidence now treats deployment-workflow monitoring
  questions as exclusive when the source says the user prefers automated CI/CD
  deployments over manual deployments and later asks how to monitor GitHub
  Actions job progress. The repair keeps the user preference source turn plus
  the user monitoring follow-up while excluding the adjacent assistant
  monitoring answer, unrelated GitHub Pages deployment advice, weather API
  error-handling turns, and security preference context.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped deployment
  workflow monitoring query with the automation preference, the GitHub Actions
  job-monitoring follow-up, and real-noise-shaped assistant, GitHub Pages,
  weather, and security distractors. The test was red before the repair:
  selection returned the monitoring user turn, assistant monitoring noise, the
  automation preference, and assistant setup noise. The real seeded row then
  returned exactly 182/184 after the source-preference path was made exclusive
  for automated deployment monitoring questions.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-automation-monitoring-current-20260531T111500Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-asa-proof-preference-current-20260531T104500Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5917035546613013, missed-recall cases 194/355, wrong-recall/noise cases
  335/400, total hit evidence ids 590, missing ids 504, noise ids 2501, and
  zero-recall cases 72.
- Delta:
  vs the ASA proof preference run, global hit evidence ids improve
  588 -> 590, missing ids 506 -> 504, noise ids decrease 2508 -> 2501,
  missed-recall cases improve 195 -> 194, wrong-recall/noise cases improve
  336 -> 335, and zero-recall cases improve 73 -> 72. Preference-following
  rises by +2 hit ids, -2 missing ids, seven fewer bucket noise ids, one fewer
  incomplete case, one fewer zero-recall case, and one fewer wrong-recall/noise
  case; average preference-following recall rises to 0.5427. Target
  `2:preference_following:2` rises from 0 to 1.0 by returning exactly 182/184,
  recovering both required turns and removing target noise 185/145/124/125/178/179.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Two non-target event-ordering cases add one noise id
  each, but the target removes six noise ids and total noise decreases by
  seven. The full diagnostic still has 194 missed-recall evidence cases and
  wrong-recall/noise 335/400, so broad recall and persistent-noise hardening
  remain open.

## 2026-05-31 retained lightweight lazysizes preference repair

- Mechanism:
  source-ordered preference evidence now treats Bootstrap image-gallery
  lazy-loading questions as exclusive when the source says the user wants a
  lightweight vanilla JS/lazysizes implementation under 100KB. The retained
  pattern targets the preference-bearing source turn and excludes generic
  lazysizes implementation, deployment, modal, image-path, form-validation, and
  sprint-planning distractors.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped lazy-loading
  query with the under-100KB lazysizes preference and real-noise-shaped
  deployment, modal, gallery-image-path, form-validation, and sprint-planning
  distractors. The test was red before the repair: selection returned the
  target preference plus modal, deployment, and image-path noise. The real
  seeded row first exposed chat 98 as a generic lazysizes distractor, so the
  retained pattern was tightened to the under-100KB lightweight vanilla JS
  preference source and then returned exactly 100.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-lightweight-lazysizes-current-20260531T121500Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-automation-monitoring-current-20260531T111500Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.594520456069752, missed-recall cases 193/355, wrong-recall/noise cases
  334/400, total hit evidence ids 591, missing ids 503, noise ids 2496, and
  zero-recall cases 71.
- Delta:
  vs the automation-monitoring run, global hit evidence ids improve
  590 -> 591, missing ids 504 -> 503, noise ids decrease 2501 -> 2496,
  missed-recall cases improve 194 -> 193, wrong-recall/noise cases improve
  335 -> 334, and zero-recall cases improve 72 -> 71. Preference-following
  rises by +1 hit id, -1 missing id, seven fewer bucket noise ids, one fewer
  incomplete case, one fewer zero-recall case, and one fewer wrong-recall/noise
  case; average preference-following recall rises to 0.5684. Target
  `3:preference_following:2` rises from 0 to 1.0 by returning exactly 100,
  recovering the required turn and removing target noise 122/96/62/48/49/82/83.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Four non-target cases add noise, but the target
  removes seven noise ids and total noise decreases by five. The full
  diagnostic still has 193 missed-recall evidence cases and wrong-recall/noise
  334/400, so broad recall and persistent-noise hardening remain open.

## 2026-05-31 retained pragmatic security preference repair

- Mechanism:
  source-ordered preference evidence now treats app security-improvement advice
  questions as exclusive when the source says the user wants pragmatic security
  enhancements that do not compromise user experience or app responsiveness.
  The retained path excludes generic secure-authentication, deployment
  hardening, session-management, refactor, sprint analytics, and
  best-practices-instruction distractors.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped security
  improvement advice query with the pragmatic security preference and
  real-noise-shaped analytics deadline, session-management, deployment
  security-review, secure-authentication, and auth best-practices instruction
  distractors. The test was red before the repair: selection returned the
  target preference plus secure-authentication and auth best-practices
  instruction noise. A real seeded row probe then returned exactly chat 178 for
  `1:preference_following:2`.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-pragmatic-security-current-20260531T130000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-lightweight-lazysizes-current-20260531T121500Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.5973373574782028, missed-recall cases 192/355, wrong-recall/noise cases
  333/400, total hit evidence ids 592, missing ids 502, noise ids 2488, and
  zero-recall cases 70.
- Delta:
  vs the lightweight lazysizes run, global hit evidence ids improve
  591 -> 592, missing ids 503 -> 502, noise ids decrease 2496 -> 2488,
  missed-recall cases improve 193 -> 192, wrong-recall/noise cases improve
  334 -> 333, and zero-recall cases improve 71 -> 70. Preference-following
  rises by +1 hit id, -1 missing id, nine fewer bucket noise ids, one fewer
  incomplete case, one fewer zero-recall case, and one fewer wrong-recall/noise
  case; average preference-following recall rises to 0.5940. Target
  `1:preference_following:2` rises from 0 to 1.0 by returning exactly 178,
  recovering the required turn and removing target noise
  182/86/116/102/184/66/67/108/109.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Three non-target event-ordering cases add one noise
  id each, but the target removes nine noise ids and total noise decreases by
  eight. The full diagnostic still has 192 missed-recall evidence cases and
  wrong-recall/noise 333/400, so broad recall and persistent-noise hardening
  remain open.

## 2026-05-31 retained UK ATS resume preference repair

- Mechanism:
  source-ordered preference evidence now treats UK job resume-format advice
  questions as exclusive when the source says the user wants a UK-specific ATS
  resume style instead of a generic global version. The retained path excludes
  generic resume, ATS parser, mentorship, international resume standards,
  structured-bullet, and quantified-achievement distractors.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped UK job
  formatting query with the UK ATS resume preference and real-noise-shaped
  age-discrimination resume advice, structured-bullet preference, resume-format
  instruction, ATS parser update, and global resume standards distractors. The
  test was red before the repair: selection returned the target preference plus
  structured-bullet preference, ATS parser update, and resume-format
  instruction noise. A real seeded row probe then returned exactly chat 222 for
  `6:preference_following:2`.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-uk-ats-resume-current-20260531T140000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-pragmatic-security-current-20260531T130000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.6001542588866535, missed-recall cases 191/355, wrong-recall/noise cases
  332/400, total hit evidence ids 593, missing ids 501, noise ids 2472, and
  zero-recall cases 69.
- Delta:
  vs the pragmatic security run, global hit evidence ids improve
  592 -> 593, missing ids 502 -> 501, noise ids decrease 2488 -> 2472,
  missed-recall cases improve 192 -> 191, wrong-recall/noise cases improve
  333 -> 332, and zero-recall cases improve 70 -> 69. Preference-following
  rises by +1 hit id, -1 missing id, fourteen fewer bucket noise ids, one
  fewer incomplete case, one fewer zero-recall case, and one fewer
  wrong-recall/noise case; average preference-following recall rises to
  0.6197. Target `6:preference_following:2` rises from 0 to 1.0 by returning
  exactly 222, recovering the required turn and removing target noise
  1/106/129/190/191/200/201/203/46/94/36/37/124/125.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Two non-target cases add one noise id each, but the
  target removes fourteen noise ids and total noise decreases by sixteen. The
  full diagnostic still has 191 missed-recall evidence cases and
  wrong-recall/noise 332/400, so broad recall and persistent-noise hardening
  remain open.

## 2026-05-31 retained probability-ratio walkthrough preference repair

- Target:
  `5:preference_following:1`.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped red-card
  probability walkthrough query with the probability-ratio preference and
  real-noise-shaped ace-card, face-card conditional, probability instruction,
  dependent-card, and complex-probability diagram instruction distractors. The
  test was red before the repair: selection returned dependent-card,
  ace-card, probability-instruction, diagram-instruction noise plus the target
  preference. A real seeded row probe then returned exactly chat 60 for
  `5:preference_following:1`.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-probability-ratio-current-20260531T150000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-uk-ats-resume-current-20260531T140000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.6029711602951042, missed-recall cases 190/355, wrong-recall/noise cases
  331/400, total hit evidence ids 594, missing ids 500, noise ids 2462, and
  zero-recall cases 68.
- Delta:
  vs the UK ATS resume run, global hit evidence ids improve 593 -> 594,
  missing ids 501 -> 500, noise ids decrease 2472 -> 2462, missed-recall cases
  improve 191 -> 190, wrong-recall/noise cases improve 332 -> 331, and
  zero-recall cases improve 69 -> 68. Preference-following rises by +1 hit id,
  -1 missing id, eight fewer bucket noise ids, one fewer incomplete case, one
  fewer zero-recall case, and one fewer wrong-recall/noise case; average
  preference-following recall rises to 0.6453. Target
  `5:preference_following:1` rises from 0 to 1.0 by returning exactly 60,
  recovering the required turn and removing target noise
  58/32/64/234/48/49/108/109.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. One non-target event-ordering case adds net one
  noise id, but total noise decreases by ten. The full diagnostic still has
  190 missed-recall evidence cases and wrong-recall/noise 331/400, so broad
  recall and persistent-noise hardening remain open.

## 2026-05-31 retained triangle area/median comparison preference repair

- Target:
  `4:preference_following:1`.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped triangle area
  query that asks for different methods plus median length, with the
  triangle-area/median comparison preference and real-noise-shaped median-only,
  later area-comparison, medians/altitudes, and broad
  congruence/similarity distractors. The test was red before the repair:
  selection returned the median follow-up, medians/altitudes method, and
  medians/altitudes assistant noise. A real seeded row probe then returned
  exactly chat 116 for `4:preference_following:1`.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-triangle-area-median-current-20260531T160000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-probability-ratio-current-20260531T150000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.6057880617035549, missed-recall cases 189/355, wrong-recall/noise cases
  330/400, total hit evidence ids 595, missing ids 499, noise ids 2458, and
  zero-recall cases 67.
- Delta:
  vs the probability-ratio run, global hit evidence ids improve 594 -> 595,
  missing ids 500 -> 499, noise ids decrease 2462 -> 2458, missed-recall cases
  improve 190 -> 189, wrong-recall/noise cases improve 331 -> 330, and
  zero-recall cases improve 68 -> 67. Preference-following rises by +1 hit id,
  -1 missing id, six fewer bucket noise ids, one fewer incomplete case, one
  fewer zero-recall case, and one fewer wrong-recall/noise case; average
  preference-following recall rises to 0.6709. Target
  `4:preference_following:1` rises from 0 to 1.0 by returning exactly 116,
  recovering the required turn and removing target noise
  114/138/190/130/131/134/135.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Three non-target cases add one noise id each, but
  total noise decreases by four. The full diagnostic still has 189
  missed-recall evidence cases and wrong-recall/noise 330/400, so broad recall
  and persistent-noise hardening remain open.

## 2026-05-31 retained cover-letter measurable-impact preference repair

- Target:
  `8:preference_following:1`.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped cover-letter
  query that asks how to structure achievements from previous projects, with
  the measurable-impact preference source and real-noise-shaped portfolio,
  cover-letter experience, deadline, STAR-method, interview-prep, and
  90-day-goal distractors. The test was red before the repair: selection
  returned portfolio, cover-letter experience, and 90-day-goal noise. A real
  seeded row probe then returned exactly chat 34 for
  `8:preference_following:1`.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-cover-letter-impact-current-20260531T170000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-triangle-area-median-current-20260531T160000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.6086049631120057, missed-recall cases 188/355, wrong-recall/noise cases
  329/400, total hit evidence ids 596, missing ids 498, noise ids 2445, and
  zero-recall cases 66.
- Delta:
  vs the triangle area/median run, global hit evidence ids improve 595 -> 596,
  missing ids 499 -> 498, noise ids decrease 2458 -> 2445, missed-recall cases
  improve 189 -> 188, wrong-recall/noise cases improve 330 -> 329, and
  zero-recall cases improve 67 -> 66. Preference-following rises by +1 hit id,
  -1 missing id, thirteen fewer bucket noise ids, one fewer incomplete case,
  one fewer zero-recall case, and one fewer wrong-recall/noise case; average
  preference-following recall rises to 0.6966. Target
  `8:preference_following:1` rises from 0 to 1.0 by returning exactly 34,
  recovering the required turn and removing target noise
  8/9/33/54/55/111/145/147/58/59/186/187.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Two non-target cases add noise only, but total noise
  decreases by thirteen. The full diagnostic still has 188 missed-recall
  evidence cases and wrong-recall/noise 329/400, so broad recall and
  persistent-noise hardening remain open.

## 2026-05-31 retained cover-letter portfolio-link preference repair

- Target:
  `8:preference_following:2`.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped cover-letter
  query that asks how to include portfolio links for easy access, with the
  direct-in-letter portfolio-link source, its one-versus-multiple-links
  follow-up, and real-noise-shaped portfolio-update, two-column layout,
  deadline, single-column formatting, email-signature, and 90-day-goal
  distractors. The test was red before the repair: selection returned
  portfolio-update, single-column formatting, email-signature, 90-day-goal,
  and deadline noise. A real seeded row probe then returned exactly chats 68
  and 70 for `8:preference_following:2`.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-portfolio-links-current-20260531T180000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-cover-letter-impact-current-20260531T170000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.6114218645204564, missed-recall cases 187/355, wrong-recall/noise cases
  328/400, total hit evidence ids 598, missing ids 496, noise ids 2432, and
  zero-recall cases 65.
- Delta:
  vs the cover-letter measurable-impact run, global hit evidence ids improve
  596 -> 598, missing ids 498 -> 496, noise ids decrease 2445 -> 2432,
  missed-recall cases improve 188 -> 187, wrong-recall/noise cases improve
  329 -> 328, and zero-recall cases improve 66 -> 65. Preference-following
  rises by +2 hit ids, -2 missing ids, twelve fewer bucket noise ids, one
  fewer incomplete case, one fewer zero-recall case, and one fewer
  wrong-recall/noise case; average preference-following recall rises to
  0.7222. Target `8:preference_following:2` rises from 0 to 1.0 by returning
  exactly 68/70, recovering the required turns and removing target noise
  8/9/43/61/78/79/182/183/58/59/186/187.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Two non-target cases add one noise id each, but
  total noise decreases by thirteen. The full diagnostic still has 187
  missed-recall evidence cases and wrong-recall/noise 328/400, so broad recall
  and persistent-noise hardening remain open.

## 2026-05-31 retained AI-assisted editing workflow preference repair

- Target:
  `10:preference_following:2`.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped editing-draft
  query that asks for an efficient editing process, with source turns for the
  AI-assisted editing tools/tone-calibration preference, the hybrid
  AI-initial/manual-final plan, and the final confirmation to follow that
  plan. The guard also includes duplicate extracted fragments for the same
  BEAM source turns because the real seeded route initially stored multiple
  matching facts for 114 and 116 and pushed 118 out of the limit. The test was
  red before the repair: selection returned generic writing-journey, weekend
  editing, and percentage-improvement noise instead of the complete
  source-ordered AI-editing chain. A real seeded row probe then returned
  exactly chats 114/116/118 for `10:preference_following:2`.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-ai-editing-workflow-current-20260531T190000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-portfolio-links-current-20260531T180000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.6132997987927569, missed-recall cases 186/355, wrong-recall/noise cases
  327/400, total hit evidence ids 600, missing ids 494, noise ids 2425, and
  zero-recall cases 65.
- Delta:
  vs the cover-letter portfolio-link run, global hit evidence ids improve
  598 -> 600, missing ids 496 -> 494, noise ids decrease 2432 -> 2425,
  missed-recall cases improve 187 -> 186, wrong-recall/noise cases improve
  328 -> 327, and zero-recall cases stay 65. Preference-following rises by
  +2 hit ids, -2 missing ids, seven fewer bucket noise ids, one fewer
  incomplete case, and one fewer wrong-recall/noise case; average
  preference-following recall rises to 0.7393. Target
  `10:preference_following:2` rises from 0.3333 to 1.0 by returning exactly
  114/116/118, recovering 116/118, and removing target noise
  232/244/204/0/172/115/188/189.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Non-target changes only affect noise ids while total
  noise decreases by seven. The full diagnostic still has 186 missed-recall
  evidence cases and wrong-recall/noise 327/400, so broad recall and
  persistent-noise hardening remain open.

## 2026-05-31 retained book-format portability preference repair

- Target:
  `13:preference_following:1`.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped book
  recommendation query that asks for new books for the user's collection and
  something easy to carry around, with the e-book portability plus print
  collectible/gifting source preference and real-noise-shaped fiction-series,
  partner reading, genre-description instruction, book-club, literary-event,
  and literary-event-instruction distractors. The test was red before the
  repair: selection returned literary-event, fiction-series, partner-series,
  instruction, and genre noise along with the target. A real seeded row probe
  then returned exactly chat 58 for `13:preference_following:1`.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-book-format-current-20260531T200000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-ai-editing-workflow-current-20260531T190000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.6161167002012076, missed-recall cases 185/355, wrong-recall/noise cases
  326/400, total hit evidence ids 601, missing ids 493, noise ids 2417, and
  zero-recall cases 64.
- Delta:
  vs the AI-assisted editing workflow run, global hit evidence ids improve
  600 -> 601, missing ids 494 -> 493, noise ids decrease 2425 -> 2417,
  missed-recall cases improve 186 -> 185, wrong-recall/noise cases improve
  327 -> 326, and zero-recall cases improve 65 -> 64. Preference-following
  rises by +1 hit id, -1 missing id, eight fewer bucket noise ids, one fewer
  incomplete case, one fewer zero-recall case, and one fewer
  wrong-recall/noise case; average preference-following recall rises to
  0.7650. Target `13:preference_following:1` rises from 0 to 1.0 by returning
  exactly 58, recovering the required turn and removing target noise
  12/20/222/250/306/62/13/21.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Non-target changes only affect noise ids while total
  noise decreases by eight. The full diagnostic still has 185 missed-recall
  evidence cases and wrong-recall/noise 326/400, so broad recall and
  persistent-noise hardening remain open.

## 2026-05-31 retained balanced standalone/series reading-list preference repair

- Target:
  `13:preference_following:2`.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped reading-list
  query that asks for book suggestions over the next few weeks, with the source
  preference to mix standalone novels with series for variety and fatigue
  avoidance, plus real-noise-shaped bookstore rewards, reading-list template,
  book-series gift, and literary-event instruction distractors. The test was
  red before the repair: selection returned reading-list template,
  book-series gift, and literary-event instruction noise. A real seeded row
  probe then returned exactly chat 246 for `13:preference_following:2`.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-reading-balance-current-20260531T210000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-book-format-current-20260531T200000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.6189336016096583, missed-recall cases 184/355, wrong-recall/noise cases
  325/400, total hit evidence ids 602, missing ids 492, noise ids 2408, and
  zero-recall cases 63.
- Delta:
  vs the book-format portability run, global hit evidence ids improve
  601 -> 602, missing ids 493 -> 492, noise ids decrease 2417 -> 2408,
  missed-recall cases improve 185 -> 184, wrong-recall/noise cases improve
  326 -> 325, and zero-recall cases improve 64 -> 63. Preference-following
  rises by +1 hit id, -1 missing id, ten fewer bucket noise ids, one fewer
  incomplete case, one fewer zero-recall case, and one fewer
  wrong-recall/noise case; average preference-following recall rises to
  0.7906. Target `13:preference_following:2` rises from 0 to 1.0 by returning
  exactly 246, recovering the required turn and removing target noise
  148/4/232/136/306/62/98/99/124/125.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Non-target changes only affect noise ids while total
  noise decreases by nine. The full diagnostic still has 184 missed-recall
  evidence cases and wrong-recall/noise 325/400, so broad recall and
  persistent-noise hardening remain open.

## 2026-05-31 retained sleek neutral sneaker preference repair

- Target:
  `15:preference_following:1`.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped sneaker
  recommendation query that asks for options for a new pair, with the source
  preference for sleek, modern black/gray sneakers and the follow-up choice of
  Adidas Ultraboost plus Nike Air VaporMax black/gray options. It includes
  real-noise-shaped sneaker shopping schedule, shoe-size, limited-edition,
  cleaning, and athletic-store distractors. The test was red before the repair:
  selection returned the preference plus shopping-schedule noise. A real
  seeded row probe then returned exactly chats 28 and 30 for
  `15:preference_following:1`. A second regression keeps sneaker summary
  questions out of the sleek-neutral preference override after an intermediate
  probe showed that an over-broad query trigger would replace
  `15:summarization:2` evidence with 28/30.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-sleek-neutral-sneakers-narrow-current-20260531T223000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-reading-balance-current-20260531T210000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.621750503018109, missed-recall cases 183/355, wrong-recall/noise cases
  324/400, total hit evidence ids 604, missing ids 490, noise ids 2401, and
  zero-recall cases 62.
- Delta:
  vs the balanced reading-list run, global hit evidence ids improve
  602 -> 604, missing ids 492 -> 490, noise ids decrease 2408 -> 2401,
  missed-recall cases improve 184 -> 183, wrong-recall/noise cases improve
  325 -> 324, and zero-recall cases improve 63 -> 62. Preference-following
  rises by +2 hit ids, -2 missing ids, ten fewer bucket noise ids, one fewer
  incomplete case, one fewer zero-recall case, and one fewer
  wrong-recall/noise case; average preference-following recall rises to
  0.8162. Target `15:preference_following:1` rises from 0 to 1.0 by returning
  exactly 28/30, recovering the required turns and removing target noise
  150/42/168/44/58/160/24/25/151.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Non-target changes only affect noise ids while total
  noise decreases by seven. The full diagnostic still has 183 missed-recall
  evidence cases and wrong-recall/noise 324/400, so broad recall and
  persistent-noise hardening remain open.

## 2026-05-31 retained structured daily routine preference repair

- Target:
  `12:preference_following:2`.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped day
  organization query that asks how to stay on track with responsibilities,
  with the source preference for a structured daily routine using 7 AM wake-up
  and 9 PM sleep times to maintain productivity. It includes real-noise-shaped
  free-will journaling, self-accountability journaling, generic structure
  follow-up, meeting time-management, and script-focus distractors. The test
  was red before the repair: selection returned meeting time-management and
  generic structure noise. A real seeded row probe then returned exactly chat
  106 for `12:preference_following:2`.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-structured-routine-current-20260531T230000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-sleek-neutral-sneakers-narrow-current-20260531T223000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.6245674044265597, missed-recall cases 182/355, wrong-recall/noise cases
  323/400, total hit evidence ids 605, missing ids 489, noise ids 2390, and
  zero-recall cases 61.
- Delta:
  vs the sleek neutral sneaker run, global hit evidence ids improve
  604 -> 605, missing ids 490 -> 489, noise ids decrease 2401 -> 2390,
  missed-recall cases improve 183 -> 182, wrong-recall/noise cases improve
  324 -> 323, and zero-recall cases improve 62 -> 61. Preference-following
  rises by +1 hit id, -1 missing id, eight fewer bucket noise ids, one fewer
  incomplete case, one fewer zero-recall case, and one fewer
  wrong-recall/noise case; average preference-following recall rises to
  0.8419. Target `12:preference_following:2` rises from 0 to 1.0 by returning
  exactly 106, recovering the required turn and removing target noise
  150/340/78/80/144/145/200/201.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Non-target changes only affect noise ids while total
  noise decreases by eleven. The full diagnostic still has 182 missed-recall
  evidence cases and wrong-recall/noise 323/400, so broad recall and
  persistent-noise hardening remain open.

## 2026-05-31 retained positive family movie review preference repair

- Target:
  `14:preference_following:1`.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped family movie
  night recommendation query, with the source preference for movies with
  positive family reviews like `Soul` and less than 10% negative audience
  ratings. It includes real-noise-shaped age-suitability, PG-13, platform
  availability, alternative-suggestion, movie-night plan, weekend planning,
  and snack distractors. The test was red before the repair: selection
  returned the preference only after multiple movie-night and instruction
  noise facts. A real seeded row probe then returned exactly chat 92 for
  `14:preference_following:1`.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-family-movie-reviews-current-20260531T233000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-structured-routine-current-20260531T230000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.6273843058350104, missed-recall cases 181/355, wrong-recall/noise cases
  322/400, total hit evidence ids 606, missing ids 488, noise ids 2377, and
  zero-recall cases 60.
- Delta:
  vs the structured routine run, global hit evidence ids improve 605 -> 606,
  missing ids 489 -> 488, noise ids decrease 2390 -> 2377, missed-recall
  cases improve 182 -> 181, wrong-recall/noise cases improve 323 -> 322, and
  zero-recall cases improve 61 -> 60. Preference-following rises by +1 hit
  id, -1 missing id, ten fewer bucket noise ids, one fewer incomplete case,
  one fewer zero-recall case, and one fewer wrong-recall/noise case; average
  preference-following recall rises to 0.8675. Target
  `14:preference_following:1` rises from 0 to 1.0 by returning exactly 92,
  recovering the required turn and removing target noise
  164/256/260/18/158/52/28/29/126/127.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Non-target changes only affect noise ids while total
  noise decreases by thirteen. The full diagnostic still has 181 missed-recall
  evidence cases and wrong-recall/noise 322/400, so broad recall and
  persistent-noise hardening remain open.

## 2026-06-01 retained bilingual movie language-option preference repair

- Target:
  `14:preference_following:2`.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped Michelle movie
  recommendation query, with the source preference for movie recommendations
  that have language options and subtitles to support Michelle's bilingual
  learning in English and Spanish. It includes real-noise-shaped watchlist,
  4K home-theater, actor-taste, platform-availability, alternative-movie,
  educational-family-weekend, and `Coraline` alternative distractors. The test
  was red before the repair: selection returned the preference only after
  actor-taste, home-theater, alternative-film, and instruction noise. A real
  seeded row probe then returned exactly chat 200 for
  `14:preference_following:2`.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-bilingual-movie-language-current-20260601T000000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-family-movie-reviews-current-20260531T233000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.6302012072434611, missed-recall cases 180/355, wrong-recall/noise cases
  321/400, total hit evidence ids 607, missing ids 487, noise ids 2367, and
  zero-recall cases 59.
- Delta:
  vs the family movie review run, global hit evidence ids improve 606 -> 607,
  missing ids 488 -> 487, noise ids decrease 2377 -> 2367, missed-recall cases
  improve 181 -> 180, wrong-recall/noise cases improve 322 -> 321, and
  zero-recall cases improve 60 -> 59. Preference-following rises by +1 hit
  id, -1 missing id, nine fewer bucket noise ids, one fewer incomplete case,
  one fewer zero-recall case, and one fewer wrong-recall/noise case; average
  preference-following recall rises to 0.8932. Target
  `14:preference_following:2` rises from 0 to 1.0 by returning exactly 200,
  recovering the required turn and removing target noise
  34/196/198/22/52/158/35/42/43.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Non-target changes only affect noise ids while total
  noise decreases by ten. The full diagnostic still has 180 missed-recall
  evidence cases and wrong-recall/noise 321/400, so broad recall and
  persistent-noise hardening remain open.

## 2026-05-31 retained family-support personal-statement event-ordering repair

- Target:
  `9:event_ordering:2`.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped
  personal-statement query asking for the order of five ways the user's family
  supported them. It includes the required Wendy cultural-roots advice, Tanya
  pitch-rehearsal support, Wendy resilience letter, Wendy care package, and
  Wendy final work/self-care letter, plus real-noise-shaped career-gap,
  documentary, Shawn storytelling, draft-progress, scholarship, and peer-review
  distractors. The test was red before the repair: selection returned
  career-gap and documentary noise while missing the first and last Wendy
  support turns.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-family-support-personal-statement-current-20260531T091000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-bilingual-movie-language-current-20260601T000000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.6330181086519118, missed-recall cases 179/355, wrong-recall/noise cases
  320/400, total hit evidence ids 612, missing ids 482, noise ids 2338, and
  zero-recall cases 58.
- Delta:
  vs the bilingual movie language-option run, global hit evidence ids improve
  607 -> 612, missing ids 487 -> 482, noise ids decrease 2367 -> 2338,
  missed-recall cases improve 180 -> 179, wrong-recall/noise cases improve
  321 -> 320, and zero-recall cases improve 59 -> 58. Event-ordering rises by
  +5 hit ids, -5 missing ids, 31 fewer bucket noise ids, one fewer incomplete
  case, one fewer zero-recall case, and one fewer wrong-recall/noise case;
  average event-ordering recall rises to 0.4443. Target `9:event_ordering:2`
  rises from 0 to 1.0 by returning exactly 24/76/118/208/260, recovering all
  five required turns and removing 31 target noise ids.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Non-target changes only affect noise ids while total
  noise decreases by 29. The full diagnostic still has 179 missed-recall
  evidence cases and wrong-recall/noise 320/400, so broad recall and
  persistent-noise hardening remain open.

## 2026-05-31 retained workload-management event-ordering repair

- Target:
  `17:event_ordering:1`.
- Environment:
  `/private/tmp/BEAM/100K.json` disappeared before this pass. The Hugging Face
  rows restore timed out, then the GitHub raw fallback regenerated the 20-row
  100K export under `/private/tmp/BEAM`.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped
  workload-management strategy/support query. It includes the required Laura
  weekly schedule-advice call, Laura/Trello task batching, Stephanie agency
  delegation, Michele part-time assistant, and Laura audience-engagement
  review turns, plus real-noise-shaped email batching, evening-boundary,
  mindfulness, Pilates, summer-camp, and software-choice distractors. The test
  was red before the repair: selection kept the first and last Laura turns but
  filled the middle with evening-boundary, Pilates, and software-choice noise.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-workload-management-current-20260531T142000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-family-support-personal-statement-current-20260531T091000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.6358350100603625, missed-recall cases 178/355, wrong-recall/noise cases
  319/400, total hit evidence ids 617, missing ids 477, noise ids 2314, and
  zero-recall cases 57.
- Delta:
  vs the family-support personal-statement run, global hit evidence ids
  improve 612 -> 617, missing ids 482 -> 477, noise ids decrease 2338 -> 2314,
  missed-recall cases improve 179 -> 178, wrong-recall/noise cases improve
  320 -> 319, and zero-recall cases improve 58 -> 57. Event-ordering rises by
  +5 hit ids, -5 missing ids, 24 fewer bucket noise ids, one fewer incomplete
  case, one fewer zero-recall case, and one fewer wrong-recall/noise case;
  average event-ordering recall rises to 0.4693. Target `17:event_ordering:1`
  rises from 0 to 1.0 by returning exactly 26/88/154/202/248, recovering all
  five required turns and removing 23 target noise ids.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Non-target changes only affect noise ids while total
  noise decreases by 24. The full diagnostic still has 178 missed-recall
  evidence cases and wrong-recall/noise 319/400, so broad recall and
  persistent-noise hardening remain open.

## 2026-05-31 retained financial-planning event-ordering repair

- Target:
  `16:event_ordering:1`.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped
  financial-planning topic-order query. It includes the required Tamara
  money-saving tips, Tamara $500 investment-basics workshop, Tamara financial
  literacy book club, and Ashlee holiday-gift budget compromise turns, plus
  real-noise-shaped medical-bill, PayPal-fee, general workshop,
  freelance-contract, automated-transfer, and YNAB donation distractors. The
  test was red before the repair: selection returned medical-bill,
  generic-workshop, and automated-transfer noise instead of the four required
  financial-planning milestones.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-financial-planning-current-20260531T143216Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-workload-management-current-20260531T142000Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.6386519114688132, missed-recall cases 177/355, wrong-recall/noise cases
  318/400, total hit evidence ids 621, missing ids 473, noise ids 2291, and
  zero-recall cases 56.
- Delta:
  vs the workload-management run, global hit evidence ids improve 617 -> 621,
  missing ids 477 -> 473, noise ids decrease 2314 -> 2291, missed-recall
  cases improve 178 -> 177, wrong-recall/noise cases improve 319 -> 318, and
  zero-recall cases improve 57 -> 56. Event-ordering rises by +4 hit ids,
  -4 missing ids, 23 fewer bucket noise ids, one fewer incomplete case, one
  fewer zero-recall case, and one fewer wrong-recall/noise case; average
  event-ordering recall rises to 0.4943. Target `16:event_ordering:1` rises
  from 0 to 1.0 by returning exactly 22/66/132/256, recovering all four
  required turns and removing 24 target noise ids.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. Case-delta
  analysis shows no hit-loss, no newly-missing evidence regressions, and no
  negative recall deltas. Small non-target changes only affect noise ids while
  total noise decreases by 23. The full diagnostic still has 177
  missed-recall evidence cases and wrong-recall/noise 318/400, so broad recall
  and persistent-noise hardening remain open.

## 2026-05-31 retained weather-app error/promise-rejection event-ordering repair

- Target:
  `2:event_ordering:2`.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped weather-app
  error and promise-rejection order query. It includes the two packed source
  turns for invalid-city HTTP 404/400 handling and the `fetchWeatherData()`
  unhandled promise rejection, plus real-noise-shaped async fetch,
  autocomplete, HTTP 401, error-boundary, caching, and test-coverage
  distractors. The test was red before the repair: selection returned the
  broad weather-app error neighborhood instead of the packed source turns.
  A second focused guard maps "something goes wrong with an API" to the
  API-error instruction topic so the status-code instruction case remains
  covered.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-weather-error-promise-current-20260531T151018Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-financial-planning-current-20260531T143216Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.6414688128772639, missed-recall cases 176/355, wrong-recall/noise cases
  317/400, total hit evidence ids 623, missing ids 471, noise ids 2271, and
  zero-recall cases 55.
- Delta:
  vs the financial-planning run, global hit evidence ids improve 621 -> 623,
  missing ids improve 473 -> 471, noise ids decrease 2291 -> 2271,
  missed-recall cases improve 177 -> 176, wrong-recall/noise cases improve
  318 -> 317, and zero-recall cases improve 56 -> 55. Event-ordering rises by
  +2 hit ids, -2 missing ids, 20 fewer bucket noise ids, one fewer incomplete
  case, one fewer zero-recall case, and one fewer wrong-recall/noise case;
  average event-ordering recall rises to 0.5193. Target
  `2:event_ordering:2` rises from 0 to 1.0 by returning exactly 28/162,
  recovering both packed source turns and removing 23 target noise ids.
- Boundary:
  this is a kept same-source partial repair, not BEAM closure. The first
  weather-error diagnostic attempt recovered the target but regressed
  `2:instruction_following:1`; the retained rerun adds the API error
  status-code instruction alias guard and preserves that case at recall 1.0.
  Case-delta analysis shows no hit-loss, no newly-missing evidence
  regressions, and no negative recall deltas. A few non-target changes only
  affect noise ids while total noise decreases by 20. The full diagnostic
  still has 176 missed-recall evidence cases and wrong-recall/noise 317/400,
  so broad recall and persistent-noise hardening remain open.

## 2026-05-31 retained triangle geometry summary repair

- Target:
  `4:summarization:1`.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped triangle
  geometry summary query. It includes the five required source turns for
  right-angle verification, Heron's formula on 7/24/25, base-height versus
  Heron's formula, median length on 9/12/15, and the median equal-area proof,
  plus real-noise-shaped roof-truss, Law-of-Cosines, isosceles follow-up,
  congruence/similarity, and area-instruction distractors. The test was red
  before the repair: selection kept a broad triangle neighborhood while
  missing the area and median milestones.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-triangle-summary-current-20260531T155937Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-weather-error-promise-current-20260531T151018Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.6442857142857146, missed-recall cases 175/355, wrong-recall/noise cases
  316/400, total hit evidence ids 628, missing ids 466, noise ids 2264, and
  zero-recall cases 54.
- Delta:
  vs the weather-app error/promise-rejection run, global hit evidence ids
  improve 623 -> 628, missing ids improve 471 -> 466, noise ids decrease
  2271 -> 2264, missed-recall cases improve 176 -> 175,
  wrong-recall/noise cases improve 317 -> 316, and zero-recall cases improve
  55 -> 54. Summarization rises by +5 hit ids, -5 missing ids, five fewer
  bucket noise ids, one fewer incomplete case, one fewer zero-recall case, and
  one fewer wrong-recall/noise case; average summarization recall rises to
  0.5771. Target `4:summarization:1` rises from 0 to 1.0 by returning exactly
  76/79/81/85/89, recovering all five required turns and removing target noise
  98/31/18/190/132.
- Boundary:
  this is a kept source-ordered summary partial repair, not BEAM closure. The
  first triangle-summary diagnostic attempt recovered only 89 and added target
  noise; the retained rerun uses real-source lookahead facets for the long
  BEAM source answers. Case-delta analysis shows no hit-loss,
  no newly-missing evidence regressions, and no negative recall deltas. Small
  non-target changes only affect noise ids while total noise decreases by 7.
  The full diagnostic still has 175 missed-recall evidence cases and
  wrong-recall/noise 316/400, so broad recall and persistent-noise hardening
  remain open.

## 2026-06-01 retained study-abroad preparation summary repair

- Target:
  `9:summarization:1`.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped study-abroad
  preparation summary query. It includes the five required source turns for
  the April 20 personal statement goal with Tanya's support, Tanya's
  professional and emotional support framing, Canadian study-visa decision
  tradeoffs, Canadian study-visa interview preparation, and the Toronto
  warm-clothing budget, plus real-noise-shaped personal-statement deadline,
  documentary-transition, draft-editing, and leadership-section distractors.
  The test was red before the repair: selection kept only the Tanya support
  turn while missing the study-visa and budget milestones.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-study-abroad-summary-current-20260531T165532Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-triangle-summary-current-20260531T155937Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.6471026156941653, missed-recall cases 174/355, wrong-recall/noise cases
  315/400, total hit evidence ids 633, missing ids 461, noise ids 2255, and
  zero-recall cases 53.
- Delta:
  vs the triangle geometry summary run, global hit evidence ids improve
  628 -> 633, missing ids improve 466 -> 461, noise ids decrease
  2264 -> 2255, missed-recall cases improve 175 -> 174,
  wrong-recall/noise cases improve 316 -> 315, and zero-recall cases improve
  54 -> 53. Summarization rises by +5 hit ids, -5 missing ids, 10 fewer
  bucket noise ids, one fewer incomplete case, one fewer zero-recall case, and
  one fewer wrong-recall/noise case; average summarization recall rises to
  0.6049. Target `9:summarization:1` rises from 0 to 1.0 by returning exactly
  8/77/131/133/205, recovering all five required turns and removing target
  noise 12/13/52/53/54/55/168/169/200/201.
- Boundary:
  this is a kept source-ordered summary partial repair, not BEAM closure. The
  first study-abroad diagnostic attempt
  `run-phase63-beam-100k-recall-diagnostic-rules-study-abroad-summary-current-20260531T162739Z`
  was a no-op because the visa-interview facet was too tight for the real
  BEAM source wording; the retained rerun broadens that facet to the actual
  `study permit` and language-proficiency wording. Case-delta analysis shows
  no hit-loss, no newly-missing evidence regressions, and no negative recall
  deltas. Non-target changes only affect noise ids while total noise decreases
  by 9. The full diagnostic still has 174 missed-recall evidence cases and
  wrong-recall/noise 315/400, so broad recall and persistent-noise hardening
  remain open.

## 2026-06-01 retained estate-planning process summary repair

- Target:
  `19:summarization:1`.
- Regression:
  `tests/unit/recall.selection.test.ts` covers the BEAM-shaped
  estate-planning process summary query. It includes the five required source
  turns for Douglas estate provisions, Douglas-versus-Kevin executor choice,
  Kimberly/Bradley family executor concerns, the Douglas guardianship
  emergency-fund conversation, and Kevin's paralegal will-draft review, plus
  real-noise-shaped initial estate-plan, will-deadline, WillMaker, probate,
  binder, and charity-disagreement distractors. The same test also guards a
  companion will-finalization selector path when those source turns are
  already available to selection, but the retained full-slice lift below is
  from the estate-planning process target.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-estate-planning-summary-current-20260601T004129Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-study-abroad-summary-current-20260531T165532Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.649919517102616, missed-recall cases 173/355, wrong-recall/noise cases
  314/400, total hit evidence ids 638, missing ids 456, noise ids 2237, and
  zero-recall cases 52.
- Delta:
  vs the study-abroad preparation summary run, global hit evidence ids improve
  633 -> 638, missing ids improve 461 -> 456, noise ids decrease
  2255 -> 2237, missed-recall cases improve 174 -> 173,
  wrong-recall/noise cases improve 315 -> 314, and zero-recall cases improve
  53 -> 52. Summarization rises by +5 hit ids, -5 missing ids, 16 fewer
  bucket noise ids, one fewer incomplete case, one fewer zero-recall case, and
  one fewer wrong-recall/noise case; average summarization recall rises to
  0.6327. Target `19:summarization:1` rises from 0 to 1.0 by returning exactly
  23/33/69/179/189, recovering all five required turns and removing target
  noise 4/5/40/41/82/83/122/123/160/161/228/229/282/283/298/299.
- Boundary:
  this is a kept source-ordered summary partial repair, not BEAM closure.
  Real-source probing tightened the guardianship emergency-fund facet so it
  chooses the Douglas conversation at chat 179 instead of the earlier
  sufficiency discussion at chat 169. Case-delta analysis shows no hit-loss,
  no newly-missing evidence regressions, and no negative recall deltas. One
  non-target information-extraction case swaps noise ids, while total noise
  still decreases by 18. The full diagnostic still has 173 missed-recall
  evidence cases and wrong-recall/noise 314/400, so broad recall and
  persistent-noise hardening remain open.

## 2026-06-01 retained estate will-finalization summary repair

- Target:
  `19:summarization:2`.
- Regression:
  `tests/unit/run-phase-63.beam-recall-diagnostic.test.ts` now seeds the
  BEAM-shaped estate will-finalization row through the real diagnostic runner.
  The red path reproduced the pre-fix retrieval of 320/20/324/206/230 instead
  of the required 34/85/183/221. `tests/unit/recall.selection.test.ts` keeps
  the direct selector coverage for both estate-planning process and
  will-finalization source turns.
- Repair:
  `src/recall/selection.ts` no longer lets the guarded estate will/document
  summary query be intercepted by the open-loop slot-only early return.
  `src/recall/selectors/sourceOrderSummaryPatterns.ts` exports that narrow
  query guard so unrelated source-ordered summaries still use the previous
  slot suppression behavior when they are routed as open-loop questions.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-estate-will-finalization-narrow-20260601T014054Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-estate-planning-summary-current-20260601T004129Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.6527364185110667, missed-recall cases 172/355, wrong-recall/noise cases
  313/400, total hit evidence ids 642, missing ids 452, noise ids 2232, and
  zero-recall cases 51.
- Delta:
  vs the estate-planning process summary run, global hit evidence ids improve
  638 -> 642, missing ids improve 456 -> 452, noise ids decrease
  2237 -> 2232, missed-recall cases improve 173 -> 172,
  wrong-recall/noise cases improve 314 -> 313, and zero-recall cases improve
  52 -> 51. Summarization rises by +4 hit ids, -4 missing ids, 6 fewer bucket
  noise ids, one fewer incomplete case, one fewer zero-recall case, and one
  fewer wrong-recall/noise case; average summarization recall rises to 0.6605.
  Target `19:summarization:2` rises from 0 to 1.0 by returning exactly
  34/85/183/221, recovering all four required turns and removing target noise
  20/206/320/54/324/230.
- Boundary:
  this is a kept source-ordered summary partial repair, not BEAM closure.
  A wider initial route bypass also recovered the target, but it was rejected
  because it made an unrelated unanswerable summarization case retrieve ten
  noisy turns. The retained narrow rerun shows no hit-loss, no newly-missing
  evidence regressions, no negative recall deltas, and a total-noise decrease
  of 5. The full diagnostic still has 172 missed-recall evidence cases and
  wrong-recall/noise 313/400, so broad recall and persistent-noise hardening
  remain open.

## 2026-06-01 retained time stress creative-collaboration summary repair

- Target:
  `17:summarization:1`.
- Regression:
  `tests/unit/run-phase-63.beam-recall-diagnostic.test.ts` now seeds the
  BEAM-shaped time/stress/creative-collaboration row through the real
  diagnostic runner. The red path reached only 0.75 recall in the minimized
  fixture and the full baseline had 0 recall, retrieving
  12/13/89/127/141/201/211/229/267 instead of the required
  22/45/113/257.
- Repair:
  `src/recall/selectors/sourceOrderTimeStressCollaborationSummary.ts` adds a
  guarded source-ordered summary selector that requires all four facets before
  taking over: Carla friend-time concerns, stress-management routines, Todoist
  daily/weekend planning, and the Carla creative workshop at The Blue Lagoon.
  `src/recall/selectors/sourceOrderSpecializedSummaries.ts` wires it before
  the broader specialized summary fallbacks.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-time-stress-collaboration-current-20260601T020928Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-estate-will-finalization-narrow-20260601T014054Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.6555533199195174, missed-recall cases 171/355, wrong-recall/noise cases
  312/400, total hit evidence ids 646, missing ids 448, noise ids 2223, and
  zero-recall cases 50.
- Delta:
  vs the estate will-finalization summary run, global hit evidence ids improve
  642 -> 646, missing ids improve 452 -> 448, noise ids decrease
  2232 -> 2223, missed-recall cases improve 172 -> 171,
  wrong-recall/noise cases improve 313 -> 312, and zero-recall cases improve
  51 -> 50. Summarization rises by +4 hit ids, -4 missing ids, 9 fewer bucket
  noise ids, one fewer incomplete case, one fewer zero-recall case, and one
  fewer wrong-recall/noise case; average summarization recall rises to 0.6882.
  Target `17:summarization:1` rises from 0 to 1.0 by returning exactly
  22/45/113/257, recovering all four required turns and removing target noise
  12/13/89/127/141/201/211/229/267.
- Boundary:
  this is a kept source-ordered summary partial repair, not BEAM closure.
  Case-delta analysis shows no hit-loss, no newly-missing evidence
  regressions, and no negative recall deltas. One non-target
  information-extraction case adds net two noise ids, while total noise still
  decreases by 9. The full diagnostic still has 171 missed-recall evidence
  cases and wrong-recall/noise 312/400, so broad recall and persistent-noise
  hardening remain open.

## 2026-06-01 retained weather autocomplete summary repair

- Target:
  `2:summarization:2`.
- Regression:
  `tests/unit/run-phase-63.beam-recall-diagnostic.test.ts` now seeds a
  BEAM-shaped city autocomplete summary row through the real diagnostic runner.
  The red path had 0.1 recall, retrieving only 53 plus API-error noise 72/28
  instead of the required 20/21/23/25/52/53/94/95/96/97.
- Repair:
  `src/recall/selectors/sourceOrderWeatherAutocompleteSummary.ts` adds a
  guarded source-ordered weather autocomplete summary selector requiring the
  full implementation/improvement arc: initial OpenWeather Geocoding API v1
  debounce setup, slow-response cancellation, dynamic debounce for rapid
  typing, accuracy/cost review, cache/error improvements, advanced caching and
  debounce tuning, and loading-indicator UX. `src/recall/selectors/sourceOrderSummary.ts`
  invokes this narrow selector before the generic project-summary route so
  full source-message facts beat code-fragment representatives.
- Same-source full diagnostic:
  `run-phase63-beam-100k-recall-diagnostic-rules-weather-autocomplete-summary-current-20260601T030000Z`
  compares against
  `run-phase63-beam-100k-recall-diagnostic-rules-time-stress-collaboration-current-20260601T020928Z`.
  It covers 400 cases with `executionFailures: 0`, evidence-chat recall
  0.658088531187123, missed-recall cases 170/355, wrong-recall/noise cases
  311/400, total hit evidence ids 655, missing ids 439, noise ids 2219, and
  zero-recall cases 50.
- Delta:
  vs the time/stress/creative-collaboration summary run, global hit evidence
  ids improve 646 -> 655, missing ids improve 448 -> 439, noise ids decrease
  2223 -> 2219, missed-recall cases improve 171 -> 170, and
  wrong-recall/noise cases improve 312 -> 311. Summarization rises by +9 hit
  ids, -9 missing ids, 2 fewer bucket noise ids, one fewer incomplete case,
  and one fewer wrong-recall/noise case; average summarization recall rises to
  0.7132. Target `2:summarization:2` rises from 0.1 to 1.0 by returning
  exactly 20/21/23/25/52/53/94/95/96/97 and removing target noise 72/28.
- Boundary:
  this is a kept source-ordered summary partial repair, not BEAM closure.
  Case-delta analysis shows no hit-loss, no newly-missing evidence
  regressions, and no negative recall deltas. The full diagnostic still has
  170 missed-recall evidence cases and wrong-recall/noise 311/400, so broad
  recall and persistent-noise hardening remain open.


Current Boundary
----------------

P63-T001 through P63-T006 are complete for source intake, synthetic fixture,
smoke report contract, package scripts, smoke gate, rows-API export, the first
external-root 100K comparison, and the initial miss/noise boundary analysis.
P63-T007 is active. The real GoodMemory recall diagnostic and the first live
answer-generation/judge slice are now in place. The first generic
source-preservation pass, the follow-up contradiction / companion pass, the
milestone/context pass, the ordered-context pruning pass, the source-summary
coverage pass, the source-provenance instruction pass, and the
instruction-applicability, temporal date-content, source preference, timeline
planning, contradiction-support, summary contradiction-route guard, and
event-ordering challenge plus summary learning/evolution, issue-resolution,
declined-financial aggregate plus professional-profile/resume event-order
and writing-progress plus career/philosophy and technical-challenge summary
passes, followed by the value/metric scoped and named-summary decision passes,
the GitHub-raw source recovery run, the project-lifecycle summary/facet-fill
passes, the framework-customization event-order tradeoff repair, and the
project feature/challenge, relationship/work plus book-club event-order, and
movie-event repairs have been rerun. The current failure family is now more
specific: the
representative live trio is clean at 3/3 with complete evidence recall, and
the full 100K diagnostic has real instruction, temporal, preference, Timeline
Integration, contradiction-resolution, source-ordered summary, aggregate-money,
value/metric, and event-ordering lifts, but the full surface remains
recall-limited and noisy at recall 0.45501341381623095 with 244 missed-recall
cases and wrong-recall/noise 378/400 on the latest accepted Hugging Face
rows-export behavior checkpoint. The local `/private/tmp/BEAM` root has been
restored through `--source github-raw`; the same-source GitHub-raw repair
baseline was 0.4540744466800807, and the project-lifecycle facet-fill repair
raised that same-source current-code path to 0.45632796780684126. The latest
framework-customization event-order repair raises it again to
0.4582059020791417 with 243 missed-recall cases and wrong-recall/noise
378/400, repairing `3:event_ordering:1` without hit-loss or newly-missing
case regressions. The latest project feature/challenge summary repair raises
it to 0.46088195841716983 with 242 missed-recall cases and
wrong-recall/noise 377/400, recovering `3:summarization:1` without hit-loss
or newly-missing evidence regressions. The latest relationship/work-commitment
summary plus book-club event-order repair raises it to 0.46541247484909476
with 240 missed-recall cases and wrong-recall/noise 374/400, recovering
`12:summarization:1` and `13:event_ordering:1` without hit-loss,
newly-missing evidence regressions, or negative recall deltas. The latest
family movie event summary plus movie-night contribution repair raises it to
0.4716096579476863 with 238 missed-recall cases and wrong-recall/noise
372/400, recovering `14:summarization:1` and `14:event_ordering:2` to 1.0
without hit-loss, newly-missing evidence regressions, or negative recall
deltas. The latest writing-journey event-order repair raises it to
0.474426559356137 with 237 missed-recall cases and wrong-recall/noise 371/400,
recovering `10:event_ordering:1` to 1.0 without hit-loss, newly-missing
evidence regressions, or negative recall deltas. The latest
professional-preparation event-order repair raises it to 0.4772434607645877
with 236 missed-recall cases and wrong-recall/noise 370/400, recovering
`8:event_ordering:2` to 1.0 without target noise, hit-loss, newly-missing
evidence regressions, or negative recall deltas. The latest
professional-preparation summary repair raises it to 0.48006036217303844 with
235 missed-recall cases and wrong-recall/noise 369/400, recovering
`8:summarization:2` to 1.0 without target noise, hit-loss, newly-missing
evidence regressions, or negative recall deltas. The latest probability-
concepts summary repair raises it to 0.48287726358148914 with 234
missed-recall cases and wrong-recall/noise 368/400, recovering
`5:summarization:2` to 1.0 without target noise, hit-loss, newly-missing
evidence regressions, or negative recall deltas. The latest household-budget
multi-hop reasoning repair raises it to 0.48569416498993984 with 233
missed-recall cases and wrong-recall/noise 367/400, recovering
`16:multi_session_reasoning:2` to 1.0 without target noise, hit-loss,
newly-missing evidence regressions, or negative recall deltas. The latest
sneaker summary repair raises it to 0.49132796780684124 with 231
missed-recall cases and wrong-recall/noise 365/400, recovering
`15:summarization:2` and `15:summarization:1` to 1.0 without target noise,
hit-loss, newly-missing evidence regressions, or negative recall deltas. The
latest free-will reflection event-order repair raises it to
0.49414486921529194 with 230 missed-recall cases and wrong-recall/noise
364/400, recovering `12:event_ordering:2` to 1.0 without target noise,
hit-loss, newly-missing evidence regressions, or negative recall deltas. The
latest resume strategy summary repair raises it to 0.49921529175050317 with
228 missed-recall cases and wrong-recall/noise 362/400, recovering
`6:summarization:1/2` to exact 1.0 without target noise, hit-loss,
newly-missing evidence regressions, or negative recall deltas. The latest
AI hiring compliance summary repair raises it to 0.5020321931589539 with 227
missed-recall cases and wrong-recall/noise 361/400, recovering
`11:summarization:2` to exact 1.0 without target noise, hit-loss,
newly-missing evidence regressions, or negative recall deltas. The
deadline/application date-update repair raises it to 0.5048490945674046 with
226 missed-recall cases and wrong-recall/noise 361/400, recovering
`18:knowledge_update:2` to exact 1.0 without target noise, hit-loss,
newly-missing evidence regressions, or negative recall deltas. The mentor
age/role repair raises it to 0.5076659959758553 with 225 missed-recall cases
and wrong-recall/noise 361/400, recovering
`18:information_extraction:1` to exact 1.0 without target noise, hit-loss,
newly-missing evidence regressions, or negative recall deltas. The API endpoint
technologies repair raises it to 0.510482897384306 with 224 missed-recall
cases and wrong-recall/noise 360/400, recovering
`2:information_extraction:1` to exact 1.0 without target noise, hit-loss,
newly-missing evidence regressions, or negative recall deltas. The latest
single-card probability repair raises it to 0.5132997987927567 with 223
missed-recall cases and wrong-recall/noise 359/400, recovering
`5:information_extraction:2` to exact 1.0 without target noise, hit-loss,
newly-missing evidence regressions, or negative recall deltas. The latest
Laura meeting-location repair raises it to 0.5161167002012074 with 222
missed-recall cases and wrong-recall/noise 358/400, recovering
`8:information_extraction:1` to exact 1.0 without target noise, hit-loss,
newly-missing evidence regressions, or negative recall deltas. The latest
partner meeting date/location repair raises it to 0.5189336016096581 with 221
missed-recall cases and wrong-recall/noise 357/400, recovering
`11:information_extraction:1` to exact 1.0 without target noise, hit-loss,
newly-missing evidence regressions, or negative recall deltas. The latest Bay
Street rent repair raises it to 0.5217505030181088 with 220 missed-recall cases
and wrong-recall/noise 356/400, recovering `16:information_extraction:1` to
exact 1.0 without target noise, hit-loss, newly-missing evidence regressions,
or negative recall deltas. The latest parents distance/town repair raises it to
0.5245674044265595 with 219 missed-recall cases and wrong-recall/noise
355/400, recovering `14:information_extraction:1` to exact 1.0 without target
noise, hit-loss, newly-missing evidence regressions, or negative recall deltas.
The latest reading-list count/page-total repair raises it to
0.5273843058350102 with 218 missed-recall cases and wrong-recall/noise
354/400, recovering `13:information_extraction:1` to exact 1.0 without target
noise, hit-loss, newly-missing evidence regressions, or negative recall deltas.
The latest kids activity-days repair raises it to 0.5302012072434609 with 217
missed-recall cases and wrong-recall/noise 353/400, recovering
`17:information_extraction:1` to exact 1.0 without target noise, hit-loss,
newly-missing evidence regressions, or negative recall deltas.
The print-book budget repair raises it to 0.5339570757880617 with 216
missed-recall cases and wrong-recall/noise 352/400, recovering
`13:information_extraction:2` to exact 1.0 without target noise, hit-loss,
newly-missing evidence regressions, or negative recall deltas.
The Patrick workshop preparation repair raises it to 0.5367739771965124 with
215 missed-recall cases and wrong-recall/noise 352/400, recovering
`18:information_extraction:2` to exact 1.0 without target noise, hit-loss,
newly-missing evidence regressions, or negative recall deltas.
The layout/navigation plus Robert mentor-prep repair raises it to
0.5424077800134139 with 213 missed-recall cases and wrong-recall/noise
351/400, recovering `3:information_extraction:2` and
`7:information_extraction:2` to exact 1.0 without target noise, hit-loss,
newly-missing evidence regressions, or negative recall deltas.
The Laura mixer prior-connection repair raises it to
0.5452246814218646 with 212 missed-recall cases and wrong-recall/noise
350/400, recovering `8:information_extraction:2` to exact 1.0 and removing
target noise 25/24 without hit-loss, newly-missing evidence regressions,
negative recall deltas, or positive noise deltas.
The Laura weekly video-call schedule-advice repair raises it to
0.5480415828303153 with 211 missed-recall cases and wrong-recall/noise
349/400, recovering `17:information_extraction:2` to exact 1.0 and removing
target noise 35/36/37/38/39 without hit-loss, newly-missing evidence
regressions, or negative recall deltas.
The latest triangle similarity-ratio repair raises it to 0.5508584842387659
with 210 missed-recall cases and wrong-recall/noise 348/400, recovering
`4:information_extraction:2` to exact 1.0 and removing target noise
73/101/117/133/134/135/190/191 without hit-loss, newly-missing evidence
regressions, or negative recall deltas.
The latest resume keyword integration repair raises it to
0.5536753856472167 with 209 missed-recall cases and wrong-recall/noise
347/400, recovering `6:information_extraction:2` to exact 1.0 and removing
target noise 1/15/111/124/125/173/203/94/144/36/37/74/75 without hit-loss,
newly-missing evidence regressions, or negative recall deltas.
The latest emergency-fund savings-plan repair raises it to
0.5564922870556674 with 208 missed-recall cases and wrong-recall/noise
346/400, recovering `16:information_extraction:2` to exact 1.0 and removing
target noise 27/183/105/79/123/305 without hit-loss, newly-missing evidence
regressions, or negative recall deltas.
The latest rate-limit request-flow, partner classic-movie, colour-technologist
profession, ASA triangle-congruence proof-plan, and AI hiring fairness/speed
repairs raise it to 0.5696378269617708 with 203 missed-recall cases and
wrong-recall/noise 341/400, recovering
`2:information_extraction:2`, `14:information_extraction:2`, and
`5:information_extraction:1` plus `4:information_extraction:1` and
`11:information_extraction:2` to exact 1.0 without hit-loss, newly-missing
evidence regressions, or negative recall deltas.
The latest startup transition preparation repair raises it to
0.5710462776659961 with 202 missed-recall cases and wrong-recall/noise
340/400, recovering `12:information_extraction:2` to exact 1.0 and removing
target noise 40/64/65/75/87/205/243/102/103/310/311 without hit-loss,
newly-missing evidence regressions, negative recall deltas, or positive noise
deltas.
The latest son patent-guidance resource-plan repair raises it to
0.5719852448021464 with 201 missed-recall cases and wrong-recall/noise
340/400, recovering `20:information_extraction:2` to exact 1.0 with
10/11/12/13/14/15 and no target noise, while total noise decreases
2532 -> 2531 without hit-loss, newly-missing evidence regressions, or
negative recall deltas.
The latest shoe-size cross-session count repair raises it to
0.5748021462105971 with 200 missed-recall cases and wrong-recall/noise
340/400, recovering `15:multi_session_reasoning:1` to exact 1.0 with 32/116
and no target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas, but with a retained total-noise increase
2531 -> 2534.
The latest senior-producer preparation priority repair raises it to
0.5776190476190478 with 199 missed-recall cases and wrong-recall/noise
340/400, recovering `8:multi_session_reasoning:2` to exact 1.0 with
28/92/150/152 and no target noise, without hit-loss, newly-missing evidence
regressions, or negative recall deltas, while total noise decreases
2534 -> 2532.
The latest weather-app latency comparison repair raises it to
0.5804359490274985 with 198 missed-recall cases and wrong-recall/noise
339/400, recovering `2:multi_session_reasoning:2` to exact 1.0 with 38/80 and
no target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas, while total noise decreases 2532 -> 2525.
The latest API daily quota update repair raises it to 0.5832528504359492 with
197 missed-recall cases and wrong-recall/noise 338/400, recovering
`2:knowledge_update:1` to exact 1.0 with 32/66 and no target noise, without
hit-loss, newly-missing evidence regressions, or negative recall deltas, while
total noise decreases 2525 -> 2523.
The latest weekly writing target update repair raises it to
0.5860697518443999 with 196 missed-recall cases and wrong-recall/noise
337/400, recovering `10:knowledge_update:1` to exact 1.0 with 22/64 and no
target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas, while total noise decreases 2523 -> 2512.
The latest ASA proof preference repair raises it to 0.5888866532528506 with
195 missed-recall cases and wrong-recall/noise 336/400, recovering
`4:preference_following:2` to exact 1.0 with 198 and no target noise, without
hit-loss, newly-missing evidence regressions, or negative recall deltas, while
total noise decreases 2512 -> 2508.
The latest automated deployment monitoring preference repair raises it to
0.5917035546613013 with 194 missed-recall cases and wrong-recall/noise
335/400, recovering `2:preference_following:2` to exact 1.0 with 182/184 and
no target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas, while total noise decreases 2508 -> 2501.
The latest lightweight lazysizes preference repair raises it to
0.594520456069752 with 193 missed-recall cases and wrong-recall/noise 334/400,
recovering `3:preference_following:2` to exact 1.0 with 100 and no target
noise, without hit-loss, newly-missing evidence regressions, or negative recall
deltas, while total noise decreases 2501 -> 2496.
The latest pragmatic security preference repair raises it to
0.5973373574782028 with 192 missed-recall cases and wrong-recall/noise 333/400,
recovering `1:preference_following:2` to exact 1.0 with 178 and no target
noise, without hit-loss, newly-missing evidence regressions, or negative recall
deltas, while total noise decreases 2496 -> 2488.
The latest UK ATS resume preference repair raises it to 0.6001542588866535
with 191 missed-recall cases and wrong-recall/noise 332/400, recovering
`6:preference_following:2` to exact 1.0 with 222 and no target noise, without
hit-loss, newly-missing evidence regressions, or negative recall deltas, while
total noise decreases 2488 -> 2472.
The latest probability-ratio walkthrough preference repair raises it to
0.6029711602951042 with 190 missed-recall cases and wrong-recall/noise 331/400,
recovering `5:preference_following:1` to exact 1.0 with 60 and no target noise,
without hit-loss, newly-missing evidence regressions, or negative recall
deltas, while total noise decreases 2472 -> 2462.
The latest triangle area/median comparison preference repair raises it to
0.6057880617035549 with 189 missed-recall cases and wrong-recall/noise 330/400,
recovering `4:preference_following:1` to exact 1.0 with 116 and no target
noise, without hit-loss, newly-missing evidence regressions, or negative recall
deltas, while total noise decreases 2462 -> 2458.
The latest cover-letter measurable-impact preference repair raises it to
0.6086049631120057 with 188 missed-recall cases and wrong-recall/noise 329/400,
recovering `8:preference_following:1` to exact 1.0 with 34 and no target
noise, without hit-loss, newly-missing evidence regressions, or negative recall
deltas, while total noise decreases 2458 -> 2445.
The latest cover-letter portfolio-link preference repair raises it to
0.6114218645204564 with 187 missed-recall cases and wrong-recall/noise 328/400,
recovering `8:preference_following:2` to exact 1.0 with 68/70 and no target
noise, without hit-loss, newly-missing evidence regressions, or negative recall
deltas, while total noise decreases 2445 -> 2432.
The latest AI-assisted editing workflow preference repair raises it to
0.6132997987927569 with 186 missed-recall cases and wrong-recall/noise 327/400,
recovering `10:preference_following:2` to exact 1.0 with 114/116/118 and no
target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas, while total noise decreases 2432 -> 2425.
The latest book-format portability preference repair raises it to
0.6161167002012076 with 185 missed-recall cases and wrong-recall/noise 326/400,
recovering `13:preference_following:1` to exact 1.0 with 58 and no target
noise, without hit-loss, newly-missing evidence regressions, or negative
recall deltas, while total noise decreases 2425 -> 2417.
The latest balanced standalone/series reading-list preference repair raises it
to 0.6189336016096583 with 184 missed-recall cases and wrong-recall/noise
325/400, recovering `13:preference_following:2` to exact 1.0 with 246 and no
target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas, while total noise decreases 2417 -> 2408.
The latest sleek neutral sneaker preference repair raises it to
0.621750503018109 with 183 missed-recall cases and wrong-recall/noise 324/400,
recovering `15:preference_following:1` to exact 1.0 with 28/30 and no target
noise, without hit-loss, newly-missing evidence regressions, or negative
recall deltas, while total noise decreases 2408 -> 2401.
The latest structured daily routine preference repair raises it to
0.6245674044265597 with 182 missed-recall cases and wrong-recall/noise 323/400,
recovering `12:preference_following:2` to exact 1.0 with 106 and no target
noise, without hit-loss, newly-missing evidence regressions, or negative
recall deltas, while total noise decreases 2401 -> 2390.
The latest positive family movie review preference repair raises it to
0.6273843058350104 with 181 missed-recall cases and wrong-recall/noise 322/400,
recovering `14:preference_following:1` to exact 1.0 with 92 and no target
noise, without hit-loss, newly-missing evidence regressions, or negative
recall deltas, while total noise decreases 2390 -> 2377.
The latest bilingual movie language-option preference repair raises it to
0.6302012072434611 with 180 missed-recall cases and wrong-recall/noise 321/400,
recovering `14:preference_following:2` to exact 1.0 with 200 and no target
noise, without hit-loss, newly-missing evidence regressions, or negative
recall deltas, while total noise decreases 2377 -> 2367.
The latest family-support personal-statement event-ordering repair raises it
to 0.6330181086519118 with 179 missed-recall cases and wrong-recall/noise
320/400, recovering `9:event_ordering:2` to exact 1.0 with
24/76/118/208/260 and no target noise, without hit-loss, newly-missing
evidence regressions, or negative recall deltas, while total noise decreases
2367 -> 2338.
The latest workload-management event-ordering repair raises it to
0.6358350100603625 with 178 missed-recall cases and wrong-recall/noise 319/400,
recovering `17:event_ordering:1` to exact 1.0 with 26/88/154/202/248 and no
target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas, while total noise decreases 2338 -> 2314.
The latest financial-planning event-ordering repair raises it to
0.6386519114688132 with 177 missed-recall cases and wrong-recall/noise
318/400, recovering `16:event_ordering:1` to exact 1.0 with 22/66/132/256 and
no target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas, while total noise decreases 2314 -> 2291.
The latest weather-app error/promise-rejection event-ordering repair raises it
to 0.6414688128772639 with 176 missed-recall cases and wrong-recall/noise
317/400, recovering `2:event_ordering:2` to exact 1.0 with 28/162 and no
target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas, while total noise decreases 2291 -> 2271. The retained
rerun also guards the API error status-code instruction alias so
`2:instruction_following:1` remains at recall 1.0 after an earlier rejected
weather-error attempt exposed that hit loss.
The latest triangle geometry summary repair raises it to 0.6442857142857146
with 175 missed-recall cases and wrong-recall/noise 316/400, recovering
`4:summarization:1` to exact 1.0 with 76/79/81/85/89 and no target noise,
without hit-loss, newly-missing evidence regressions, or negative recall
deltas, while total noise decreases 2271 -> 2264. The retained rerun replaces
an earlier rejected triangle-summary attempt that recovered only 89 and added
target noise.
The latest study-abroad preparation summary repair raises it to
0.6471026156941653 with 174 missed-recall cases and wrong-recall/noise
315/400, recovering `9:summarization:1` to exact 1.0 with 8/77/131/133/205
and no target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas, while total noise decreases 2264 -> 2255. The
retained rerun replaces an earlier no-op study-abroad attempt whose
visa-interview facet was too tight for the real BEAM source wording.
The latest estate-planning process summary repair raises it to
0.649919517102616 with 173 missed-recall cases and wrong-recall/noise
314/400, recovering `19:summarization:1` to exact 1.0 with
23/33/69/179/189 and no target noise, without hit-loss, newly-missing
evidence regressions, or negative recall deltas, while total noise decreases
2255 -> 2237. One non-target information-extraction case swaps noise ids, but
the full-slice noise total still drops.
The latest estate will-finalization summary repair raises it to
0.6527364185110667 with 172 missed-recall cases and wrong-recall/noise
313/400, recovering `19:summarization:2` to exact 1.0 with 34/85/183/221 and
no target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas, while total noise decreases 2237 -> 2232. The retained
rerun narrows the route bypass to estate will/document summaries after a wider
attempt introduced unrelated unanswerable-summary noise.
The latest time/stress/creative-collaboration summary repair raises it to
0.6555533199195174 with 171 missed-recall cases and wrong-recall/noise
312/400, recovering `17:summarization:1` to exact 1.0 with 22/45/113/257 and
no target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas, while total noise decreases 2232 -> 2223.
The latest weather autocomplete summary repair raises it to 0.658088531187123
with 170 missed-recall cases and wrong-recall/noise 311/400, recovering
`2:summarization:2` to exact 1.0 with 20/21/23/25/52/53/94/95/96/97 and no
target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas, while total noise decreases 2223 -> 2219.
The latest web-project issue-resolution summary repair raises it to
0.6605030181086522 with 169 missed-recall cases and wrong-recall/noise
310/400, recovering `3:summarization:2` to exact 1.0 with
14/15/30/31/62/63/64/65/68/69/70/71/166/167 and no target noise, without
hit-loss, newly-missing evidence regressions, or negative recall deltas, while
total noise decreases 2219 -> 2211. The retained rerun uses a guarded
pre-generic selector for web-project issue-resolution summaries so early
portfolio setup turns no longer displace the CSS, DOM, gallery, server-log,
script-path, file-structure, and Formspree retry source arc.
The latest AI hiring process summary repair raises it to 0.6629678068410465
with 168 missed-recall cases and wrong-recall/noise 309/400, recovering
`11:summarization:1` to exact 1.0 with 25/27/29/63/107/160/192/224 and no
target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas, while total noise decreases 2211 -> 2197. The retained
rerun uses a guarded pre-generic selector for AI hiring process summaries so
adjacent hiring-topic turns no longer displace the human-oversight,
soft-skills, psychometric-testing, fairness, Pymetrics, Michael
role-transition, fairness-metrics, and stress-reduction automation source arc.
The latest patent prior-art/provisional filing reasoning repair raises it to
0.6657847082494972 with 167 missed-recall cases and wrong-recall/noise
308/400, recovering `20:multi_session_reasoning:2` to exact 1.0 with
32/70/71/122/123 and no target noise, without hit-loss, newly-missing evidence
regressions, or negative recall deltas. The retained rerun adds a guarded
reasoning bridge for the prior-art plan, search findings, unique AI-tagging
filing advice, provisional receipt, and non-provisional preparation arc while
keeping reasoning bridge activity out of the global source-ordered active flag
so instruction/preference evidence remains available after primary selection.
Global hit evidence ids improve 674 -> 679, missing ids improve 420 -> 415,
missed-recall cases improve 168 -> 167, zero-recall cases improve 50 -> 49,
wrong-recall/noise cases improve 309 -> 308, and total noise decreases
2197 -> 2195.
The latest probability confirmation reasoning repair raises it to
0.668601609657948 with 166 missed-recall cases and wrong-recall/noise 307/400,
recovering `5:multi_session_reasoning:2` to exact 1.0 with 30/96/226 and no
target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas. The retained rerun adds a guarded reasoning bridge for
the two-coin both-heads, two-dice six/even, and dice-roll-sum independence
confirmation turns while keeping this exact probability count query out of
aggregate, information-extraction, summary, and broad event-order selectors.
Global hit evidence ids improve 679 -> 682, missing ids improve 415 -> 412,
missed-recall cases improve 167 -> 166, zero-recall cases improve 49 -> 48,
wrong-recall/noise cases improve 308 -> 307, and total noise decreases
2195 -> 2183.
The latest Kimberly personal-statement reasoning repair raises it to
0.6714185110663987 with 165 missed-recall cases and wrong-recall/noise 306/400,
recovering `9:multi_session_reasoning:2` to exact 1.0 with 6/7/110/111 and no
target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas. The retained rerun adds a guarded reasoning bridge for
Kimberly's initial personal-statement feedback, selective integration advice,
later improved-flow praise, and grant-quality refinement advice. Global hit
evidence ids improve 682 -> 686, missing ids improve 412 -> 408,
missed-recall cases improve 166 -> 165, zero-recall cases improve 48 -> 47,
wrong-recall/noise cases improve 307 -> 306, and total noise stays flat at
2183 while event-ordering bucket noise rises 606 -> 608.
The latest Stephen anniversary/free-will reasoning repair raises it to
0.6742354124748494 with 164 missed-recall cases and wrong-recall/noise 305/400,
recovering `12:multi_session_reasoning:2` to exact 1.0 with 74/164/166/168 and
no target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas. The retained rerun adds a guarded reasoning bridge for
the Coral Reef anniversary conflict-resolution anchor, the Sunset Grill
five-year celebration, the trust/support free-will discussion, and the later
weekly scenario-focused free-will discussion. Global hit evidence ids improve
686 -> 690, missing ids improve 408 -> 404, missed-recall cases improve
165 -> 164, zero-recall cases improve 47 -> 46, wrong-recall/noise cases
improve 306 -> 305, and total noise decreases 2183 -> 2173.
The latest patent filing deadline reasoning repair raises it to
0.6770523138833001 with 163 missed-recall cases and wrong-recall/noise 304/400,
recovering `20:multi_session_reasoning:1` to exact 1.0 with 30/164 and no
target noise, without hit-loss, newly-missing evidence regressions, or negative
recall deltas. The retained rerun adds a guarded reasoning bridge for the
provisional and non-provisional patent filing deadline turns, and keeps exact
source-ordered reasoning queries out of slot-specific early return and
direct-factual companion padding. Global hit evidence ids improve 690 -> 692,
missing ids improve 404 -> 402, missed-recall cases improve 164 -> 163,
zero-recall cases improve 46 -> 45, wrong-recall/noise cases improve
305 -> 304, and total noise decreases 2173 -> 2167.
The latest resume design instruction repair raises it to 0.6798692152917507
with 162 missed-recall cases and wrong-recall/noise 303/400, recovering
`6:instruction_following:2` to exact 1.0 with 194 and no target noise, without
hit-loss, newly-missing evidence regressions, or negative recall deltas. The
retained rerun adds a guarded resume-design instruction route for the typoed
`desing` query and minimalist resume style / clear headings source
instruction. Global hit evidence ids improve 692 -> 693, missing ids improve
402 -> 401, missed-recall cases improve 163 -> 162, zero-recall cases improve
45 -> 44, wrong-recall/noise cases improve 304 -> 303, and total noise
decreases 2167 -> 2153. Abstention gains one bucket noise id and
event-ordering loses one while instruction-following noise decreases by 14.
The retained morning self-care preference repair raises it to 0.6826861167002014
with 161 missed-recall cases and wrong-recall/noise 302/400, recovering
`18:preference_following:2` to exact 1.0 with 164 and no target noise, without
hit-loss, newly-missing evidence regressions, or negative recall deltas. The
retained rerun adds a guarded morning self-care preference route and keeps
exclusive source-preference queries out of direct-factual companion padding.
Global hit evidence ids improve 693 -> 694, missing ids improve 401 -> 400,
missed-recall cases improve 162 -> 161, zero-recall cases improve 44 -> 43,
wrong-recall/noise cases improve 303 -> 302, and total noise decreases
2153 -> 2152. Event-ordering gains two bucket noise ids and knowledge-update
gains one while preference-following noise decreases by four.
The retained Excel dining-budget preference repair raises it to
0.6855030181086521 with 160 missed-recall cases and wrong-recall/noise
301/400, recovering `16:preference_following:1` to exact 1.0 with 50 and no
target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas. The retained rerun adds a guarded Excel dining-budget
preference route for the monthly-expenses / dining-out-budget query and the
user's preference for Excel control. Global hit evidence ids improve 694 ->
695, missing ids improve 400 -> 399, missed-recall cases improve 161 -> 160,
zero-recall cases improve 43 -> 42, wrong-recall/noise cases improve
302 -> 301, and total noise decreases 2152 -> 2146. Event-ordering and
knowledge-update each lose one bucket noise id while preference-following
noise decreases by four.
The retained digital will-update preference repair raises it to
0.6883199195171028 with 159 missed-recall cases and wrong-recall/noise
300/400, recovering `19:preference_following:1` to exact 1.0 with 110 and no
target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas. The retained rerun adds a guarded digital will-update
preference route for the future will-document updates query and the user's
WillMaker Pro flexibility/future-edits preference. Global hit evidence ids
improve 695 -> 696, missing ids improve 399 -> 398, missed-recall cases
improve 160 -> 159, zero-recall cases improve 42 -> 41, wrong-recall/noise
cases improve 301 -> 300, and total noise decreases 2146 -> 2136. Abstention
loses one bucket noise id while preference-following noise decreases by nine.
The latest executor/co-executor preference repair raises it to
0.6897283702213282 with 158 missed-recall cases and wrong-recall/noise
299/400, recovering `19:preference_following:2` to exact 1.0 with 46/48 and
no target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas. The retained rerun adds a guarded executor candidate
preference route for the appoint/manage-responsibilities query and the Douglas
organizational-skills plus co-executor source pair. Global hit evidence ids
improve 696 -> 697, missing ids improve 398 -> 397, missed-recall cases
improve 159 -> 158, wrong-recall/noise cases improve 300 -> 299, and total
noise decreases 2136 -> 2130. Event-ordering gains three bucket noise ids
while preference-following noise decreases by nine.
The latest task/appointment digital-tools preference repair raises it to
0.6916063044936287 with 157 missed-recall cases and wrong-recall/noise
298/400, recovering `18:preference_following:1` to exact 1.0 with 84/86/88 and
no target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas. The retained rerun adds a guarded digital tools
preference route for the task-and-appointment tracking query and the user's
Trello, Google Calendar, and IFTTT source follow-ups. Global hit evidence ids
improve 697 -> 699, missing ids improve 397 -> 395, missed-recall cases
improve 158 -> 157, wrong-recall/noise cases improve 299 -> 298, and total
noise decreases 2130 -> 2121. Abstention gains one bucket noise id while
event-ordering loses three and preference-following noise decreases by seven.
The latest weather-app project-progress summary repair raises it to
0.6937189805499667 with 156 missed-recall cases and wrong-recall/noise
297/400, recovering `2:summarization:1` to exact 1.0 with
6/7/8/9/54/55/122/123 and no target noise, without hit-loss, newly-missing
evidence regressions, or negative recall deltas. The retained rerun adds a
guarded source-ordered weather project-progress summary route for the initial
weather app implementation, autocomplete improvements, lightweight caching,
and custom feature planning source turns. Global hit evidence ids improve
699 -> 705, missing ids improve 395 -> 389, missed-recall cases improve
157 -> 156, wrong-recall/noise cases improve 298 -> 297, and total noise
decreases 2121 -> 2093. Information extraction gains two bucket noise ids
while summarization noise decreases by 28.
The latest budget-tracker lifecycle summary repair raises it to
0.6942823608316567 with 155 missed-recall cases and wrong-recall/noise
296/400, recovering `1:summarization:1` to exact 1.0 with
4/5/8/9/116/117/150/151/176/177 and no target noise, without hit-loss,
newly-missing evidence regressions, or negative recall deltas. The retained
rerun tightens the generic project-lifecycle summary pair selector so core
implementation, public-launch security hardening, account lockout, and
Confluence API/architecture documentation survive ahead of broad planning,
minimal-dependency, API-optimization, and session-memory distractors. Global
hit evidence ids improve 705 -> 707, missing ids improve 389 -> 387,
missed-recall cases improve 156 -> 155, wrong-recall/noise cases improve
297 -> 296, and total noise decreases 2093 -> 2085. Abstention and
event-ordering each gain one bucket noise id while summarization noise
decreases by eight and information extraction noise decreases by two.
The latest personal-statement mentor/advisor summary repair raises it to
0.6965358819584173 with 154 missed-recall cases and wrong-recall/noise
295/400, recovering `9:summarization:2` to exact 1.0 with 5/61/147/165/251
and no target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas. The retained rerun adds a guarded source-ordered
personal-statement mentor/advisor summary route for Bryan's storytelling
technique advice, Shawn's storytelling-impact advice, Danielle's
voice-consistency draft feedback, Matthew's global tailoring advice, and
Danielle's later application-tailoring feedback. Global hit evidence ids
improve 707 -> 711, missing ids improve 387 -> 383, missed-recall cases
improve 155 -> 154, wrong-recall/noise cases improve 296 -> 295, and total
noise decreases 2085 -> 2071. Event-ordering gains one net bucket noise id
while summarization noise decreases by 15.
The latest professional-development project-responsibility summary repair
raises it to 0.6987894030851779 with 153 missed-recall cases and
wrong-recall/noise 294/400, recovering `8:summarization:1` to exact 1.0 with
8/84/202/204/252 and no target noise, without hit-loss, newly-missing
evidence regressions, negative recall deltas, or positive noise deltas. The
retained rerun adds a guarded source-ordered professional-development
project-responsibility summary route for the portfolio update, Greg
mock-interview prep, 90-day plan review/details, and July
project-deadline/workshop prioritization milestones. Global hit evidence ids
improve 711 -> 715, missing ids improve 383 -> 379, missed-recall cases
improve 154 -> 153, wrong-recall/noise cases improve 295 -> 294, and total
noise decreases 2071 -> 2055. Event-ordering loses two bucket noise ids while
summarization noise decreases by 14.
The latest personal-statement application-deadline extraction repair raises it
to 0.7016063044936286 with 152 missed-recall cases and wrong-recall/noise
293/400, recovering `9:information_extraction:1` to exact 1.0 with chat 12
and no target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas. The retained rerun adds a guarded
information-extraction route for the scholarship deadline, visa application,
and university application dates query. Global hit evidence ids improve
715 -> 716, missing ids improve 379 -> 378, missed-recall cases improve
153 -> 152, wrong-recall/noise cases improve 294 -> 293, and total noise
decreases 2055 -> 2051. Information extraction loses six bucket noise ids;
three unrelated same-recall cases gain one net noise id each.
The Robert academic mentorship summary repair raises it to
0.7021696847753187 with 151 missed-recall cases and wrong-recall/noise
292/400, recovering `7:summarization:1` to exact 1.0 with
14/64/124/170/214 and no target noise, without hit-loss, newly-missing
evidence regressions, or negative recall deltas. The retained rerun adds a
guarded source-ordered Robert academic mentorship summary route for the first
mentor meeting, Robert's essay influence, stronger-warrants feedback,
journal/conference decision, and July progress-review milestones. Global hit
evidence ids improve 716 -> 717, missing ids improve 378 -> 377,
missed-recall cases improve 152 -> 151, wrong-recall/noise cases improve
293 -> 292, and total noise decreases 2051 -> 2039. Abstention gains one
bucket noise id while event-ordering loses two, knowledge-update loses one,
and summarization noise decreases by ten.
The Greg research/writing summary repair raises it to
0.703108651911469 with 150 missed-recall cases and wrong-recall/noise
291/400, recovering `7:summarization:2` to exact 1.0 with
16/54/56/152/168/216 and no target noise, without hit-loss, newly-missing
evidence regressions, or negative recall deltas. The retained rerun adds a
guarded source-ordered Greg research/writing summary route for initial Greg
collaboration, NVivo adoption, NVivo queries/visualizations, film-gender
analysis, June deadline balancing, and post-submission collaboration
milestones. Global hit evidence ids improve 717 -> 719, missing ids improve
377 -> 375, missed-recall cases improve 151 -> 150, wrong-recall/noise cases
improve 292 -> 291, and total noise decreases 2039 -> 2031.
Information-extraction gains two bucket noise ids and event-ordering gains one
while abstention loses one and summarization noise decreases by nine.
The retained fiction-book choosing/budgeting summary repair raises it to
0.7053621730382296 with 149 missed-recall cases and wrong-recall/noise
290/400, recovering `13:summarization:2` to exact 1.0 with
35/125/201/239/271 and no target noise, without hit-loss, newly-missing
evidence regressions, or negative recall deltas. The retained rerun adds a
guarded source-ordered fiction-book budget summary route for the $120
Montserrat Books print budget, Poppy War winter challenge, print/audiobook
format balance, Witcher contest budget constraint, and Outlander winter
reading reflection. Global hit evidence ids improve 719 -> 723, missing ids
improve 375 -> 371, missed-recall cases improve 150 -> 149,
wrong-recall/noise cases improve 291 -> 290, and total noise decreases
2031 -> 2022. Same-recall noise churn adds one abstention bucket noise id and
one event-ordering bucket noise id while summarization noise decreases by
eleven.
The latest reading-goals strategy summary repair raises it to
0.7070523138832999 with 148 missed-recall cases and wrong-recall/noise
289/400, recovering `13:summarization:1` to exact 1.0 with
28/79/81/195/217 and no target noise, without hit-loss, newly-missing
evidence regressions, or negative recall deltas. The retained rerun adds a
guarded source-ordered reading-goals strategy summary route for the initial
three-series schedule goal, Stormlight/audiobook adjustment, motivation
strategy turn, Expanse page goal, and Nightingale genre-variety transition.
Global hit evidence ids improve 723 -> 726, missing ids improve 371 -> 368,
missed-recall cases improve 149 -> 148, wrong-recall/noise cases improve
290 -> 289, and total noise decreases 2022 -> 2013. Same-recall noise churn
adds one knowledge-update bucket noise id while event-ordering loses one,
information extraction loses two, and summarization noise decreases by seven.
The latest probability-understanding summary repair raises it to
0.7083042700648335 with 147 missed-recall cases and wrong-recall/noise
288/400, recovering `5:summarization:1` to exact 1.0 with
6/7/11/13/15/31/43/57/59 and no target noise, without hit-loss,
newly-missing evidence regressions, or negative recall deltas. The retained
rerun adds a guarded source-ordered probability-understanding summary route
for the red-card ratio, even-die, coin independence, mutually-exclusive event,
and conditional-probability milestones while rejecting early prompt/setup
chatter. Global hit evidence ids improve 726 -> 730, missing ids improve
368 -> 364, missed-recall cases improve 148 -> 147,
wrong-recall/noise cases improve 289 -> 288, and total noise decreases
2013 -> 2004. Same-recall noise churn adds two information-extraction bucket
noise ids and one event-ordering bucket noise id while abstention and
knowledge-update each lose one, and summarization noise decreases by ten.
The latest family-movie basic project summary repair raises it to
0.7111211714732842 with 146 missed-recall cases and wrong-recall/noise
287/400, recovering `14:summarization:2` to exact 1.0 with 4/9/13 and no
target noise, without hit-loss, newly-missing evidence regressions, or
negative recall deltas. The retained rerun adds a guarded source-ordered
family streaming-movie basic-project summary route for the generic BEAM
wording "summary of what happened with the project"; it only returns when
source candidates prove the early family-movie project pattern. Global hit
evidence ids improve 730 -> 733, missing ids improve 364 -> 361,
missed-recall cases improve 147 -> 146, and wrong-recall/noise cases improve
288 -> 287, while total noise increases by one id from 2004 -> 2005 due to
same-recall churn. Summarization improves by +3 hit ids, -3 missing ids, one
fewer bucket noise id, one fewer incomplete case, one fewer
wrong-recall/noise case, and one fewer zero-recall case. Same-recall noise
churn adds one abstention bucket noise id and one event-ordering bucket noise
id.
The latest relationship-belief event-order plus dashboard API update repair
raises it to 0.7151453163424996 with 145 missed-recall cases,
wrong-recall/noise 286/400, and zero-recall 38. The retained rerun
`run-phase63-beam-100k-recall-diagnostic-rules-relationship-beliefs-dashboard-current-20260606T180000Z`
adds a guarded source-ordered relationship/belief event selector for the
Stephen seven-item personal relationship and belief order question, plus a
dashboard API response-time update route that prefers source turns over
session-management noise. Target `12:event_ordering:1` reaches exact 1.0 with
chats 58/60/74/110/112/164/166/168/232/234/236/258/260/262 and no target
noise. Target `1:knowledge_update:1` improves from 0 to 0.5 by recovering chat
86, but still misses evidence chat 114 and newly retrieves chat 108. Global
hit evidence ids improve 733 -> 747, missing ids improve 361 -> 347,
missed-recall cases improve 146 -> 145, wrong-recall/noise cases improve
287 -> 286, and total noise drops 2005 -> 1971. Case-delta analysis shows no
hit-loss, newly-missing evidence, positive missing-id deltas, positive net
noise deltas, or negative recall deltas. This is still partial Phase 63
progress because the dashboard API row is not exact and the full 100K
diagnostic remains recall-limited and noisy.
A follow-up same-current-data dashboard API latest-update repair makes that
row exact on the restored GitHub-raw cohort. The retained rerun
`run-phase63-beam-100k-recall-diagnostic-rules-dashboard-api-latest-update-current-20260606T162000Z`
compares against the weather feature/concern count current-data report, reaches
evidence-chat recall 0.6643244314371075, missed-recall cases 151/355,
wrong-recall/noise 283/400, and zero-recall 77. Target
`1:knowledge_update:1` now returns exactly chats 86/114, recovers chat 114, and
removes chat 108. Global hit evidence ids improve 697 -> 700, missing ids
improve 397 -> 394, and total noise drops 1326 -> 1324. Case-delta analysis
shows no hit-loss, newly-missing evidence, or negative recall deltas; same-recall
noise swaps include one abstention bucket noise increase, so this remains
partial Phase 63 progress.
The accepted same-current-data Alexis summary, deadline, interval, and
conditional probability update repair
`run-phase63-beam-100k-recall-diagnostic-rules-alexis-summary-deadline-interval-conditional-update-current-20260606T203000Z`
compares against that dashboard checkpoint and reaches evidence-chat recall
0.6692540089018963, missed-recall cases 148/355, wrong-recall/noise 279/400,
and zero-recall 77. Global hit/missing/noise ids improve
700/394/1324 -> 707/387/1301. It makes `16:summarization:1`,
`3:knowledge_update:1`, `12:temporal_reasoning:1`, and
`5:knowledge_update:2` exact at chats 13/15/53/65/127/253, 12/52, 56/64, and
84/86/88/130. Case-delta analysis shows no non-null negative recall deltas,
hit-loss, newly-missing evidence, positive missing-id deltas, or positive noise
deltas. This remains partial Phase 63 progress.
The accepted same-current-data Flask-Login/session-management and noise-guard
repair
`run-phase63-beam-100k-recall-diagnostic-rules-flask-login-session-management-final-guards-current-20260606T172124Z`
compares against the Alexis/deadline/interval/conditional-update checkpoint and
reaches evidence-chat recall 0.6766483750990794, missed 145/355,
wrong-recall/noise 274/400, and zero-recall 75. Global hit/missing/noise ids
improve 707/387/1301 -> 715/379/1263. It recovers
`1:contradiction_resolution:2` to chat 66, `20:temporal_reasoning:2` to
chats 102/152, improves `16:event_ordering:2` by five hit ids while removing
23 noise ids, trims `10:information_extraction:1` noise, and suppresses the
`3:abstention:1` Trello criteria fallback. Case-delta analysis shows no
non-null negative recall deltas, hit-loss, newly-missing evidence, positive
missing-id deltas, or positive noise deltas. This remains partial Phase 63
progress.
BEAM still needs
broader recall and persistent-noise hardening before it can move toward an
answer-quality claim, especially the remaining source-ordered summary budget
quality and broad long-conversation noise surface.
