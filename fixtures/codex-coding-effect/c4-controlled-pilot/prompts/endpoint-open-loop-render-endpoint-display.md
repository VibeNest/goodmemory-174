# TypeScript utility task

Apply the accepted endpoint-display policy to renderEndpointDisplay. Keep the exported signature unchanged.

Keep the implementation dependency-free and run the visible test.
