# stale-time-unit-update / stage-2

Apply the current configuration-unit contract to both fields returned by scheduleToMs without changing its object shape.

Keep the implementation dependency-free and run the visible test.
