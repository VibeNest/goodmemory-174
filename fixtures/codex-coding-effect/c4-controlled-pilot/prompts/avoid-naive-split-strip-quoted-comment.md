# avoid-naive-split / stage-1

Repair stripConfigComment so comment markers inside quoted values remain data while unquoted trailing comments are removed.

Keep the implementation dependency-free and run the visible test.
