# TypeScript utility task

Make parseCsvUnique trim fields, drop empty entries, and preserve first-seen order while removing duplicates.

Keep the implementation dependency-free and run the visible test.
