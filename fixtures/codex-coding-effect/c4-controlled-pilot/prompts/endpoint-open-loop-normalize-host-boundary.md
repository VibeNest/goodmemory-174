# endpoint-open-loop / stage-1

Finish normalizeHost so surrounding input whitespace is normalized while its signature and already-valid host behavior stay unchanged.

Keep the implementation dependency-free and run the visible test.
