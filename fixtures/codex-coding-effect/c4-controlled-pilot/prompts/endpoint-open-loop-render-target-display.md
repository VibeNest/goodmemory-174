# TypeScript utility task

Apply the accepted endpoint-display policy to renderTargetDisplay. Keep the exported signature unchanged.

Keep the implementation dependency-free and run the visible test.
