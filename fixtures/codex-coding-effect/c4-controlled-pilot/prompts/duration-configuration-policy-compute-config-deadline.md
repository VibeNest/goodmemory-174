# TypeScript utility task

Apply the accepted duration-boundary policy to deadlineFromConfig. Keep the exported signature unchanged.

Keep the implementation dependency-free and run the visible test.
