# endpoint-open-loop / stage-3

Make formatEndpoint render literal IPv6 hosts unambiguously without changing hostname output or the public function signature.

Keep the implementation dependency-free and run the visible test.
