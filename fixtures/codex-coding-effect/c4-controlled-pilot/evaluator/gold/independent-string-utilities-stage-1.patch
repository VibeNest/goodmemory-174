diff --git a/src/tasks.ts b/src/tasks.ts
index 248330e0f0e46970b52f56af725edac918e5beee..9e63073bbcd9601869d781e7d4cbbc48a8cc0d46 100644
--- a/src/tasks.ts
+++ b/src/tasks.ts
@@ -31,7 +31,11 @@ export function deadlineFromConfig(input: { skewMs: number; startMs: number; tim
 }
 
 export function slugify(value: string): string {
-  return value.toLowerCase().replace(" ", "-");
+  return value
+    .trim()
+    .toLowerCase()
+    .replace(/[^a-z0-9]+/gu, "-")
+    .replace(/^-|-$/gu, "");
 }
 
 export function parseCsvUnique(input: string): string[] {
