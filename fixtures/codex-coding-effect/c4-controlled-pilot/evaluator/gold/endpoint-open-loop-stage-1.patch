diff --git a/src/tasks.ts b/src/tasks.ts
index a2aedf47cba4bdfda96157a97231f8d6a2df2866..1c9e371ec456ae3d2e30658996d1ddfb556093dc 100644
--- a/src/tasks.ts
+++ b/src/tasks.ts
@@ -1,5 +1,7 @@
 export function renderHostDisplay(host: string): string {
-  return host;
+  return host.includes(":") && !(host.startsWith("(") && host.endsWith(")"))
+    ? `(${host})`
+    : host;
 }
 
 export function renderEndpointDisplay(host: string, port: number): string {
