diff --git a/src/tasks.ts b/src/tasks.ts
index 10dcd2304122fb6f8f3faef1c52edd6a657b2d08..baea746dd8ff8a35a04960025a20c98c41e12429 100644
--- a/src/tasks.ts
+++ b/src/tasks.ts
@@ -29,7 +29,16 @@ export function splitRoute(input: string): [string, string] | null {
 }
 
 export function stripConfigComment(input: string): string {
-  return input.split("#")[0]!.trimEnd();
+  let quote: string | null = null;
+  for (let index = 0; index < input.length; index += 1) {
+    const character = input[index]!;
+    if (character === "\"" || character === "'") {
+      quote = quote === character ? null : quote ?? character;
+    } else if (character === "#" && quote === null) {
+      return input.slice(0, index).trimEnd();
+    }
+  }
+  return input.trimEnd();
 }
 
 export function tokenizeCommand(input: string): string[] {
