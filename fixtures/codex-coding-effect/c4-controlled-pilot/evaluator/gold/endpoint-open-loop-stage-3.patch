diff --git a/src/tasks.ts b/src/tasks.ts
index a2aedf47cba4bdfda96157a97231f8d6a2df2866..6069621341512368acc7b36d3e4c9f111084262f 100644
--- a/src/tasks.ts
+++ b/src/tasks.ts
@@ -7,7 +7,10 @@ export function renderEndpointDisplay(host: string, port: number): string {
 }
 
 export function renderTargetDisplay(target: string, host: string, port: number): string {
-  return `${target} ${host}:${port}`;
+  const renderedHost = host.includes(":") && !(host.startsWith("(") && host.endsWith(")"))
+    ? `(${host})`
+    : host;
+  return `${target} ${renderedHost}:${port}`;
 }
 
 export function splitAssignment(input: string): [string, string] | null {
